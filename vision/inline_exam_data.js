/* Generated from verified standalone inline tests. */
window.INLINE_EXAM_MAP = {"01_ResNet18_CIFAR10.ipynb": {"page": "vision_resnet18_subjective_test", "cells": [{"number": 1, "code_number": null, "type": "markdown", "source": "# **ResNet18 with CIFAR-10 — 학습/추론/시각화**\n\n## 학습 목표\n- Residual Connection(잔차 연결)의 구조와 원리를 이해하고, ResNet-18 모델을 CIFAR-10 이미지 분류 작업에 맞게 구현 및 최적화할 수 있도록 실습합니다.\n- 데이터 전처리부터 모델 학습, 성능 평가, 그리고 Class Activation Map(CAM) 등을 활용한 추론 결과 시각화까지의 ResNet 파이프라인 전 과정을 익힙니다.\n\n---\n\n### 구성 개요\n1. **CIFAR-10 로드** + 입력 이미지 크기 확인\n2. **ResNet18(CIFAR-friendly)** 구성\n3. **학습(Training)** + 체크포인트 저장/재개(있으면 +10 epoch, 없으면 20 epoch)\n4. **추론(Inference)** + 예측/신뢰도 확인\n5. **Confusion Matrix / 클래스별 정확도 / 오분류 샘플** 분석\n6. **Activation Map** 시각화 (layer1 vs layer4 비교)\n7. **Conv Kernel** 시각화 (초기/중간 레이어)\n8. **Grad-CAM** 시각화 (Top-K 정분류 + Top-K 오분류: 원본과 Overlay 함께)\n9. (추가 실습) **Receptive Field** 근사 계산 + 의미 설명\n10. (추가 실습) **Embedding(t-SNE)** 시각화\n11. (추가 실습) **ONNX Export + 모델 구조 출력** (Netron 비교용)\n\n> 권장 실행: 위에서 아래로 순차 실행"}, {"number": 2, "code_number": null, "type": "markdown", "source": "---\n## 0) Setup (환경 준비)\n- **목적:** 실습에 필요한 패키지를 로드하고, 디바이스를 출력합니다.\n- **관찰 포인트**\n  - 실습에 필요한 유틸 패키지(`torch`, `torchvision`, `matplotlib`, `onnx` 등) 준비\n  - GPU 사용 여부(`cuda`)와 제품명 확인"}, {"number": 3, "code_number": 1, "type": "code", "source": "import importlib.util\nimport subprocess\nimport sys\n\ndef ensure_package(pkg_name: str, import_name=None):\n    \"\"\"\n    패키지 설치 여부를 확인하고, 설치되어 있지 않으면 설치합니다.\n    \"\"\"\n    name = import_name or pkg_name\n    # 패키지가 설치되어 있는지 확인\n    if importlib.util.find_spec(name) is None:\n        print(f\"[install] {pkg_name} 라이브러리를 설치 중입니다... (import name: {name})\")\n        try:\n            # -q 옵션을 추가하여 설치 과정을 간결하게 유지할 수 있습니다.\n            subprocess.check_call([sys.executable, \"-m\", \"pip\", \"install\", \"-q\", pkg_name])\n            print(f\"[success] {pkg_name} 설치 완료.\")\n        except subprocess.CalledProcessError as e:\n            print(f\"[error] {pkg_name} 설치 실패: {e}\")\n    else:\n        print(f\"[ok] {pkg_name} 이미 설치되어 있습니다.\")\n\n# 설치가 필요한 패키지 리스트 (패키지명, 임포트명)\n# 임포트명이 패키지명과 다른 경우 튜플로 지정합니다.\npackages = [\n    (\"torch\", \"torch\"),\n    (\"torchvision\", \"torchvision\"),\n    (\"matplotlib\", \"matplotlib\"),\n    (\"onnx\", \"onnx\"),\n]\n\n# 루프를 돌며 확인 및 설치\nfor pkg, imp in packages:\n    ensure_package(pkg, imp)"}, {"number": 4, "code_number": 2, "type": "code", "source": "import os  # path/환경 확인\nimport random  # seed 고정\nimport time  # epoch 시간 측정\nimport numpy as np  # 수치 계산\nimport torch  # PyTorch 본체\nimport torch.nn as nn  # 모델 레이어\nimport torch.nn.functional as F  # softmax 등 함수\nfrom torch.utils.data import DataLoader  # 데이터 로더\nimport torchvision  # vision dataset/util\nimport torchvision.transforms as T  # transform\nimport torchvision.models as models  # resnet18\nimport matplotlib.pyplot as plt  # 시각화\n\nprint('torch:', torch.__version__)  # torch 버전 출력\nprint('torchvision:', torchvision.__version__)  # torchvision 버전 출력\nprint('cuda available:', torch.cuda.is_available())  # GPU 사용 가능 여부\nif torch.cuda.is_available():\n    print('gpu:', torch.cuda.get_device_name(0))  # GPU 이름 출력\n\ntry:\n    get_ipython().system('nvidia-smi -L')  # GPU 목록 확인(가능한 경우)\nexcept Exception as e:\n    print('nvidia-smi not available:', e)  # nvidia-smi가 없는 환경이면 무시\n\ndef seed_everything(seed: int = 42):\n    random.seed(seed)  # python random seed\n    np.random.seed(seed)  # numpy seed\n    torch.manual_seed(seed)  # torch CPU seed\n    torch.cuda.manual_seed_all(seed)  # torch GPU seed\n    torch.backends.cudnn.deterministic = False  # 성능 우선(완전 재현성 X)\n    torch.backends.cudnn.benchmark = True  # 입력 크기 고정이면 속도 향상\n\nseed_everything(42)  # seed 고정(실험 재현성)\ndevice = torch.device('cuda' if torch.cuda.is_available() else 'cpu')  # 실행 디바이스 선택\ndevice  # 디바이스 출력"}, {"number": 5, "code_number": null, "type": "markdown", "source": "---\n## 1) Dataset: CIFAR-10 로드 + 입력 크기 확인\n- **목적:** 학습/평가에 사용할 데이터셋을 준비합니다.\n- **관찰 포인트**\n    - CNN 입력은 **(C,H,W)** 텐서이며, CIFAR-10은 **32×32** 입니다.\n    - augmentation(RandomCrop/Flip)을 적용합니다.\n    - CIFAR-10 데이터셋 구성\n       - Number of Classes: 10 (e.g., airplane, dog, cat, frog, etc.)\n       - Image Distribution: The dataset is class-balanced, with 6,000 images per class (5,000 for training and 1,000 for testing).\n       - Image Format: All images are 32x32 pixel color images (3 RGB channels)."}, {"number": 6, "code_number": 3, "type": "code", "source": "# CIFAR-10에서 널리 쓰는 normalize 값\nCIFAR10_MEAN = (0.4914, 0.4822, 0.4465)  # 채널별 평균(R,G,B)\nCIFAR10_STD  = (0.2023, 0.1994, 0.2010)  # 채널별 표준편차(R,G,B)\n\ntrain_tfms = T.Compose([  # train transform(augmentation 포함)\n    [[BLANK:blank-random-crop]],  # 32x32를 padding 후 random crop\n    T.RandomHorizontalFlip(),  # 좌우 반전\n    T.ToTensor(),  # PIL -> torch tensor (C,H,W), 0~1\n    [[BLANK:blank-normalize]],  # 정규화, -2.5~2.5\n])\n\ntest_tfms = T.Compose([  # test transform(augmentation 없음)\n    T.ToTensor(),  # 텐서 변환\n    T.Normalize(CIFAR10_MEAN, CIFAR10_STD),  # 정규화\n])\n\ndata_root = './data'  # 데이터 저장 경로\ntrain_set = torchvision.datasets.CIFAR10(root=data_root, train=True, download=True, transform=train_tfms)  # train set\ntest_set  = torchvision.datasets.CIFAR10(root=data_root, train=False, download=True, transform=test_tfms)  # test set\n\nclass_names = train_set.classes  # 클래스 이름 목록\nnum_classes = len(class_names)  # 클래스 개수(10)\nprint('classes:', class_names)  # 클래스 출력\n\n# 이미지 사이즈 출력\nx0, y0 = train_set[0]  # 한 샘플 로드(이미 transform 적용된 텐서)\nprint('Sample image tensor shape (C,H,W):', tuple(x0.shape))  # (3,32,32)\n\nbatch_size = 256  # 배치 크기\nnum_workers = min(8, os.cpu_count() or 2)  # dataloader worker 수(환경에 맞게)\n\ntrain_loader = DataLoader(train_set, batch_size=batch_size, shuffle=True, num_workers=num_workers, pin_memory=True)  # train loader\ntest_loader  = DataLoader(test_set, batch_size=batch_size, shuffle=False, num_workers=num_workers, pin_memory=True)  # test loader\n\nprint('Total batch size (train: %d, test: %d)' % (len(train_loader), len(test_loader)))  # 배치 개수 확인"}, {"number": 7, "code_number": 4, "type": "code", "source": "\ndef denorm(x): # -2.5~2.5\n    mean = torch.tensor(CIFAR10_MEAN).view(1,3,1,1)  # (1,3,1,1) 형태로 broadcast\n    std  = torch.tensor(CIFAR10_STD).view(1,3,1,1)  # (1,3,1,1) 형태로 broadcast\n    return x * std + mean  # 정규화 역변환, 0~1\n\nimages, labels = next(iter(train_loader))  # 한 배치 가져오기\nimages_dn = denorm(images[:16]).clamp(0,1)  # 앞 16장만 역정규화 후 [0,1]로 clip\n\ngrid = torchvision.utils.make_grid(images_dn, nrow=8)  # 그리드로 묶기(C,H,W)\nplt.figure(figsize=(12,4))\nplt.imshow(grid.permute(1,2,0))  # CHW -> HWC 로 변환 후 표시\nplt.axis('off')\nplt.title('CIFAR-10 samples (denormalized)')\nplt.show()\n\nprint('labels:', [class_names[int(x)] for x in labels[:16]])  # 라벨 이름 출력"}, {"number": 8, "code_number": null, "type": "markdown", "source": "---\n## 2) ResNet18 모델 준비\n- **목적:** CIFAR-10(32×32)에 맞는 ResNet18을 구성합니다.\n- **관찰 포인트**\n    - ImageNet용 stem(7×7 stride 2 + maxpool)은\n      - `conv1`을 **3×3 stride 1**로 바꾸고\n      - `maxpool`을 제거합니다."}, {"number": 9, "code_number": 5, "type": "code", "source": "def build_resnet18_for_cifar10(num_classes: int = 10):\n    model = models.resnet18(weights=None)  # ImageNet pretrained 없이 ResNet18 생성 (from scratch)\n    print(\"\\n\" + \"=\"*40)\n    print(\"=== PyTorch model structure (Before) ===\")  # 모델 구조 출력\n    print(\"=\"*40 + \"\\n\")\n    print(model)  # 구조 출력\n\n    model.conv1 = [[BLANK:blank-cifar-stem]]  # CIFAR용 stem\n    model.maxpool = nn.Identity()  # maxpool 제거\n    model.fc = nn.Linear(model.fc.in_features, num_classes)  # 최종 분류기 출력=10\n    return model\n\nmodel = build_resnet18_for_cifar10(num_classes).to(device)  # 모델 생성 후 device 이동\n\nprint(\"\\n\" + \"=\"*40)\nprint(\"=== PyTorch model structure (After) ===\")  # 모델 구조 출력\nprint(\"=\"*40 + \"\\n\")\nprint(model)  # 구조 출력\n\nnum_params = sum(p.numel() for p in model.parameters())  # 파라미터 개수\nprint(f'params: {num_params:,}')  # 출력"}, {"number": 10, "code_number": null, "type": "markdown", "source": "---\n## 3) 학습 유틸리티\n- **목적:** 학습/평가 루프를 재사용 가능하게 만들어 실험을 단순화합니다.\n- **관찰 포인트**\n  - `model.train()` vs `model.eval()` 차이\n  - AMP(Automatic Mixed Precision) 사용\n     - 계산이 복잡한 곳은 FP32를 쓰고, 단순한 곳은 FP16을 섞어서 사용\n     - GradScaler의 역할: FP16을 쓰면 기울기(Gradient) 값이 너무 작아져서 사라질 위험이 있음 (언더플로우)\n        - Scaling: 손실(Loss) 값에 아주 큰 값을 곱해 기울기를 크게 키움\n        - Backprop: 커진 상태로 역전파를 수행하여 값이 사라지지 않게 보호\n        - Unscaling: 업데이트 직전에 다시 원래 비율로 줄여서 가중치에 반영"}, {"number": 11, "code_number": 6, "type": "code", "source": "from dataclasses import dataclass  # 설정을 깔끔하게 묶기 위한 dataclass\n\n@dataclass\nclass TrainConfig:\n    base_epochs_if_new: int = 20  # 처음 실험 시 epoch 수\n    extra_epochs_if_resume: int = 10  # 체크포인트 존재 시 추가 실험 epoch 수\n    lr: float = 0.1  # learning rate\n    weight_decay: float = 5e-4  # L2 정규화\n    momentum: float = 0.9  # SGD momentum\n    label_smoothing: float = 0.0  # 필요 시 label smoothing, 예) [0, 1, 0] ->  [0.05, 0.9, 0.05]\n\ncfg = TrainConfig()  # config 인스턴스 생성\n\ndef accuracy_top1(logits, targets):\n    preds = logits.argmax(dim=1)  # 가장 큰 logit의 클래스 선택, logits = [B, C], preds = [B]\n    return (preds == targets).float().mean().item()  # top-1 accuracy, scalar\n\nscaler = torch.amp.GradScaler(enabled=torch.cuda.is_available())  # AMP용 scaler, 학습 속도를 높이고 메모리 사용량을 줄임\nprint(scaler)\n\ndef train_one_epoch(model, loader, optimizer, criterion):\n    model.train()  # train mode(dropout/bn 동작 변경)\n    total_loss, total_acc = 0.0, 0.0  # 누적 loss/acc\n    n = 0  # 샘플 수 누적\n    t0 = time.time()  # 시간 측정 시작\n    for images, labels in loader:\n        images = images.to(device, non_blocking=True)  # 입력을 GPU로 이동 (Asynchronous transfer)\n        labels = labels.to(device, non_blocking=True)  # 라벨을 GPU로 이동 (Asynchronous transfer)\n\n        optimizer.zero_grad(set_to_none=True)  # gradient 초기화\n        with torch.amp.autocast(device_type=device.type, enabled=torch.cuda.is_available()):  # AMP autocast\n            logits = model(images)  # forward\n            loss = criterion(logits, labels)  # loss 계산\n        scaler.scale(loss).backward()  # scaled backward\n        scaler.step(optimizer)  # optimizer step\n        scaler.update()  # scaler 업데이트\n\n        bs = images.size(0)  # batch size\n        total_loss += loss.item() * bs  # batch loss 누적\n        total_acc  += accuracy_top1(logits.detach(), labels) * bs  # logits값만 복사, batch acc 누적\n        n += bs  # 샘플 수 누적\n\n    dt = time.time() - t0  # epoch 수행 시간\n    return total_loss / n, total_acc / n, dt  # 평균 loss/acc/시간\n\n@torch.no_grad()   # Autograd off, 함수 내부에서 기울기 계산 멈춤\ndef evaluate(model, loader, criterion):\n    model.eval()  # eval mode, i.e., Dropout 비활성화, BN Running Stats 고정\n    total_loss, total_acc = 0.0, 0.0  # 누적\n    n = 0  # 샘플 수\n    for images, labels in loader:\n        images = images.to(device, non_blocking=True)  # GPU 이동\n        labels = labels.to(device, non_blocking=True)  # GPU 이동\n        logits = model(images)  # forward\n        loss = criterion(logits, labels)  # loss\n        bs = images.size(0)  # batch size\n        total_loss += loss.item() * bs  # 누적\n        total_acc  += accuracy_top1(logits, labels) * bs  # 누적\n        n += bs  # 누적\n    return total_loss / n, total_acc / n  # 평균 loss/acc\n\ndef plot_history(hist):\n    epochs = np.arange(1, len(hist['train_loss'])+1)  # epoch index\n    plt.figure(figsize=(12,4))\n    plt.plot(epochs, hist['train_loss'], label='train_loss')  # train loss\n    plt.plot(epochs, hist['val_loss'], label='val_loss')  # val loss\n    plt.xlabel('epoch'); plt.ylabel('loss'); plt.legend(); plt.grid(True)\n    plt.show()\n\n    plt.figure(figsize=(12,4))\n    plt.plot(epochs, hist['train_acc'], label='train_acc')  # train acc\n    plt.plot(epochs, hist['val_acc'], label='val_acc')  # val acc\n    plt.xlabel('epoch'); plt.ylabel('acc'); plt.legend(); plt.grid(True)\n    plt.show()"}, {"number": 12, "code_number": null, "type": "markdown", "source": "---\n## 4) ResNet18 학습\n- **목적:** CIFAR-10에서 ResNet18을 학습하고, 가장 좋은 성능의 모델을 저장합니다.\n- **관찰 포인트**\n    - `resnet18_cifar10.pth`가 있으면 **그 모델을 로드**하고 **10 epoch만 추가 학습**합니다.\n    - 파일이 없으면 **20 epoch 학습**합니다.\n    - 체크포인트로 실험을 이어갈 수 있다는 점"}, {"number": 13, "code_number": 7, "type": "code", "source": "criterion = nn.CrossEntropyLoss(label_smoothing=cfg.label_smoothing)  # 분류 loss\noptimizer = torch.optim.SGD(model.parameters(), lr=cfg.lr, momentum=cfg.momentum, weight_decay=cfg.weight_decay)  # Mini-Batch SGD optimizer with Momentum\n\nckpt_path = 'resnet18_cifar10.pth'  # best checkpoint 경로\nhistory = {'train_loss': [], 'train_acc': [], 'val_loss': [], 'val_acc': [], 'lr': []}  # 기록용 dict\n\nstart_epoch = 0  # resume 시작 epoch\nbest_acc = -1.0  # best val acc\n\nif os.path.exists(ckpt_path):  # 체크포인트가 있으면 resume\n    ckpt = torch.load(ckpt_path, map_location=device, weights_only=True)  # checkpoint 로드\n    model.load_state_dict(ckpt['model'])  # 모델 가중치 로드\n    start_epoch = int(ckpt.get('epoch', 0))  # 저장된 epoch\n    best_acc = float(ckpt.get('val_acc', -1.0))  # 저장된 best acc\n    epochs_to_run = cfg.extra_epochs_if_resume  # 추가 실험 epoch = 10\n    print(f\"Found checkpoint. Resume from epoch {start_epoch} (best val_acc={best_acc:.4f}). Train +{epochs_to_run} epochs.\")\nelse:  # 체크포인트가 없으면 처음부터\n    epochs_to_run = cfg.base_epochs_if_new  # 신규 실험 epoch = 20\n    print(f\"No checkpoint. Train {epochs_to_run} epochs from scratch.\")\n\n# 학습률을 T_max에 맞춰 코사인 곡선 형태로 변화\nscheduler = torch.optim.lr_scheduler.CosineAnnealingLR(optimizer, T_max=epochs_to_run)  # 이번 실험 구간에 대해 cosine schedule\n\nfor e in range(1, epochs_to_run+1):\n    epoch = start_epoch + e  # 실제 epoch 번호(누적)\n    lr_now = optimizer.param_groups[0]['lr']  # 현재 lr, [ { 'params': [...], 'lr': 0.1, 'momentum': 0.9, 'weight_decay': 0.0005, ... } ]\n    tr_loss, tr_acc, dt = train_one_epoch(model, train_loader, optimizer, criterion)  # 1 epoch train\n    va_loss, va_acc = evaluate(model, test_loader, criterion)  # validation\n    scheduler.step()  # lr 스케줄 업데이트\n\n    history['train_loss'].append(tr_loss)  # 기록\n    history['train_acc'].append(tr_acc)  # 기록\n    history['val_loss'].append(va_loss)  # 기록\n    history['val_acc'].append(va_acc)  # 기록\n    history['lr'].append(lr_now)  # 기록\n\n    print(f\"Epoch {epoch:03d} | lr {lr_now:.4f} | train loss {tr_loss:.4f} acc {tr_acc:.4f} | val loss {va_loss:.4f} acc {va_acc:.4f} | {dt:.1f}s\")\n\n    if va_acc > best_acc:  # best 갱신 시 저장\n        best_acc = va_acc  # best 업데이트\n        torch.save({'model': model.state_dict(), 'epoch': epoch, 'val_acc': va_acc, 'config': cfg.__dict__}, ckpt_path)  # 체크포인트 저장\n        print(f\"  Saved best checkpoint: {ckpt_path} (val_acc={va_acc:.4f})\")\n\nprint('Best val acc:', best_acc)  # best 출력\nplot_history(history)  # 실험 곡선\n\nplt.figure(figsize=(12,3))\nplt.plot(np.arange(1, epochs_to_run+1), history['lr'])  # lr 변화(이번 구간)\nplt.xlabel('epoch'); plt.ylabel('lr'); plt.title('Learning rate schedule (this segment)'); plt.grid(True)\nplt.show()"}, {"number": 14, "code_number": null, "type": "markdown", "source": "---\n## 5) 추론(Inference)\n- **목적:** 학습된 모델이 실제로 어떤 클래스로 예측하는지 확인합니다.\n- **관찰 포인트**\n    - softmax 확률과 confidence(최대 확률)"}, {"number": 15, "code_number": 8, "type": "code", "source": "ckpt = torch.load(ckpt_path, map_location=device, weights_only=True)  # best checkpoint 로드\nmodel.load_state_dict(ckpt['model'])  # 최신 모델 가중치 적용\nmodel.eval()  # eval 모드, i.e., Dropout 비활성화, BN Running Stats 고정\nprint('Loaded checkpoint epoch:', ckpt.get('epoch'), 'val_acc:', ckpt.get('val_acc'))  # 정보 출력\n\n@torch.no_grad()  # 자동 미분(Autograd) 기능 비활성화\ndef predict_batch(model, images):\n    logits = model(images)  # raw logits\n    probs = F.softmax(logits, dim=1)  # 확률로 변환\n    conf, pred = probs.max(dim=1)  # 최대 확률(conf)과 클래스(pred)\n    return pred, conf, probs\n\nimages, labels = next(iter(test_loader))  # test 배치 하나\nimages = images.to(device)  # GPU 이동\nlabels = labels.to(device)  # GPU 이동\npred, conf, probs = predict_batch(model, images)  # 예측\n\nk = 16  # 시각화 샘플 수\nimgs_dn = denorm(images[:k].cpu()).clamp(0,1)  # 역정규화\n\ngrid = torchvision.utils.make_grid(imgs_dn, nrow=8)  # 그리드\nplt.figure(figsize=(12,6))\nplt.imshow(grid.permute(1,2,0))  # HWC로 변환\nplt.axis('off')\nplt.title('Inference samples')\nplt.show()\n\nfor i in range(k):\n    gt = class_names[int(labels[i])]  # 정답 클래스명\n    pr = class_names[int(pred[i])]  # 예측 클래스명\n    cf = float(conf[i])  # confidence\n    print(f\"[{i:02d}] GT={gt:>10s} | Pred={pr:>10s} | conf={cf:.3f}\")  # 출력"}, {"number": 16, "code_number": null, "type": "markdown", "source": "---\n## 6) 성능 분석: Confusion Matrix / 클래스별 정확도 / 오분류 샘플\n- **목적:** 단순 overall accuracy 말고, **어떤 클래스가 서로 헷갈리는지**를 분석합니다.\n- **관찰 포인트**\n  - Confusion Matrix에서 off-diagonal가 큰 위치 = 헷갈리는 클래스 쌍\n  - 클래스별 accuracy로 데이터/모델의 약점 찾기"}, {"number": 17, "code_number": 9, "type": "code", "source": "@torch.no_grad()\ndef collect_predictions(model, loader):\n    model.eval()  # eval 모드\n    all_preds, all_labels, all_images = [], [], []  # 누적 리스트\n    for images, labels in loader:\n        images = images.to(device)  # GPU 이동\n        labels = labels.to(device)  # GPU 이동\n        preds, conf, _ = predict_batch(model, images)  # 예측\n        all_preds.append(preds.cpu())  # CPU로 모아두기\n        all_labels.append(labels.cpu())  # CPU로 모아두기\n        all_images.append(images.cpu())  # 이미지도 저장(오분류 시각화용)\n    return torch.cat(all_images), torch.cat(all_preds), torch.cat(all_labels)  # 전체 텐서로 결합\n\ndef confusion_and_perclass(preds, labels, num_classes):\n    cm = torch.zeros((num_classes, num_classes), dtype=torch.int64)  # confusion matrix 초기화\n    for t, p in zip(labels, preds):\n        cm[int(t), int(p)] += 1  # GT=t, Pred=p 카운트 증가\n    per_class_acc = cm.diagonal().float() / torch.clamp(cm.sum(dim=1).float(), min=1.0)  # 클래스별 정확도\n    return cm, per_class_acc\n\nimages_all, preds_all, labels_all = collect_predictions(model, test_loader)  # test 전체 예측 수집\ncm, pacc = confusion_and_perclass(preds_all, labels_all, num_classes)  # confusion + per-class\n\nplt.figure(figsize=(8,7))\nplt.imshow(cm.numpy())  # confusion matrix 이미지화\nplt.title('Confusion Matrix (Test)')\nplt.xlabel('Pred'); plt.ylabel('GT')\nplt.xticks(range(num_classes), class_names, rotation=45, ha='right')\nplt.yticks(range(num_classes), class_names)\nplt.colorbar()\nplt.tight_layout()\nplt.show()\n\nprint('Per-class accuracy:')\nfor i, a in enumerate(pacc.tolist()):\n    print(f\"{class_names[i]:>10s}: {a:.3f}\")  # 클래스별 정확도 출력\n\n# 오분류 샘플 랜덤 16개 표시\nmis_idx = (preds_all != labels_all).nonzero(as_tuple=False).squeeze(1)  # 오분류 인덱스, squeeze(1) : 1번째 인덱스 차원 제거\nprint('num misclassified:', int(mis_idx.numel()))  # 오분류 개수\nif mis_idx.numel() > 0:\n    sel = mis_idx[torch.randperm(mis_idx.numel())[:16]]  # 랜덤 16개 선택\n    imgs = denorm(images_all[sel]).clamp(0,1)  # 역정규화\n    grid = torchvision.utils.make_grid(imgs, nrow=8)  # 그리드\n    plt.figure(figsize=(12,5))\n    plt.imshow(grid.permute(1,2,0))  # 표시\n    plt.axis('off')\n    plt.title('Random misclassified examples')\n    plt.show()\n\n    for j, idx in enumerate(sel.tolist()):\n        gt = class_names[int(labels_all[idx])]  # GT\n        pr = class_names[int(preds_all[idx])]  # Pred\n        print(f\"[{j:02d}] idx={idx:5d} GT={gt:>10s} Pred={pr:>10s}\")  # 출력"}, {"number": 18, "code_number": null, "type": "markdown", "source": "---\n## 7) Activation Map 시각화 (채널별)\n- **목적:** CNN의 **각 레이어**가 무엇을 보는지 시각적으로 확인합니다.\n- **관찰 포인트**\n  - `layer1~2`: 엣지/텍스처처럼 로컬 패턴이 자주 보임\n  - `layer3~4`: 더 의미적인(부품/영역) 반응이 나오는 경향\n\n> 구현 방법: 원하는 레이어에 **forward hook**을 걸어 feature map(B,C,H,W)을 받아옵니다."}, {"number": 19, "code_number": 10, "type": "code", "source": "def get_module_by_name(model: nn.Module, name: str) -> nn.Module:\n    cur = model  # 시작 모듈\n    for part in name.split('.'):  # 점(.) 기준으로 순회\n        if part.isdigit():\n            cur = cur[int(part)]  # Sequential 인덱스 접근\n        else:\n            cur = getattr(cur, part)  # attribute 접근\n    return cur\n\ndef capture_activation(model, layer_name, x_single):\n    layer = get_module_by_name(model, layer_name)  # target layer\n    activ = {}  # 저장용 dict\n    def hook(m, i, o):  # callback function\n        activ['feat'] = o.detach().cpu()  # (B,C,H,W) 저장\n\n    h = layer.register_forward_hook(hook)  # target layer처리시 forward hook 등록 (hook 함수 실행)\n    model.eval()  # eval 모드\n    with torch.no_grad():\n        _ = model(x_single)  # forward 수행\n    h.remove()  # hook 제거\n    assert 'feat' in activ, f\"Hook failed for layer {layer_name}\"  # 방어 코드\n    return activ['feat'][0]  # (C,H,W) 반환\n\ndef show_topk_activation_grid(feat_chw, title, topk=16):\n    ch_scores = feat_chw.abs().mean(dim=(1,2))  # 채널별 평균 크기 점수\n    idx = torch.topk(ch_scores, k=min(topk, feat_chw.shape[0])).indices  # Top-K 채널 인덱스\n    maps = []  # (K,1,H,W)로 만들 리스트\n    for ci in idx:\n        m = feat_chw[int(ci)]  # (H,W)\n        m = (m - m.min()) / (m.max() - m.min() + 1e-6)  # 0~1 정규화\n        maps.append(m[None, None, ...])  # (1,1,H,W)\n    maps = torch.cat(maps, dim=0)  # (K,1,H,W)\n    grid = torchvision.utils.make_grid(maps, nrow=8, padding=2)  # grid 생성(종종 3채널로 변환되기도 함)\n    plt.figure(figsize=(12,5))\n    if grid.ndim == 3 and grid.shape[0] in (3, 4):  # (3,H,W)면 HWC로 변환\n        plt.imshow(grid.permute(1,2,0))\n    else:  # (1,H,W)면 2D로 표시\n        plt.imshow(grid.squeeze(0), cmap='viridis')\n    plt.title(title)\n    plt.axis('off')\n    plt.show()\n\n\n# 1. 시각화할 레이어 이름 정의 (ResNet 구조 기준)\ntarget_layers = {\n    \"Layer 1\": \"layer1.1.conv2\", # 얕은 레이어\n    \"Layer 2\": \"layer2.1.conv2\",\n    \"Layer 3\": \"layer3.1.conv2\",\n    \"Layer 4\": \"layer4.1.conv2\"  # 깊은 레이어\n}\n\n# 2. 입력 이미지 선택(원하면 idx 변경)\nidx = 10  # 샘플 인덱스\nx, y = test_set[idx]   # 샘플 로드\nx_in = x.unsqueeze(0).to(device)\n\nimg0 = denorm(x.unsqueeze(0)).squeeze(0).clamp(0,1)\nplt.figure(figsize=(3,3))\nplt.imshow(img0.permute(1,2,0))\nplt.title(f'Input (GT={class_names[y]})')\nplt.axis('off')\nplt.show()\n\n# 3. 반복문을 통해 각 레이어의 Activation 추출 및 시각화\nprint(f\"{'Layer Name':<20} | {'Activation Shape'}\")\nprint(\"-\" * 45)\n\nfor title, layer_name in target_layers.items():\n    # Activation 캡처\n    feat = capture_activation(model, layer_name, x_in)\n\n    # 정보 출력\n    print(f\"{layer_name:<20} | {tuple(feat.shape)}\") # (C,H,W)\n\n    # 시각화 (Top-16 채널)\n    show_topk_activation_grid(feat, f'Activation maps (Top-16) @ {title} ({layer_name})', topk=16)"}, {"number": 20, "code_number": null, "type": "markdown", "source": "---\n## 8) Convolution Kernel 시각화\n- **목적:** convolution filter가 학습되면서 어떤 형태(엣지/방향성 등)를 띠는지 확인합니다.\n- **관찰 포인트**\n  - `conv1`은 입력 채널이 RGB(3)이므로 커널을 '이미지처럼' 볼 수 있음\n  - 깊은 레이어는 입력 채널이 많아 평균/부분 채널만 보는 방식이 보편적"}, {"number": 21, "code_number": 11, "type": "code", "source": "def show_conv_kernels(model, layer_name='conv1', max_kernels=64):\n    layer = get_module_by_name(model, layer_name)  # target layer\n    assert isinstance(layer, nn.Conv2d)  # conv인지 확인\n    w = layer.weight.detach().cpu()  # (O,I,kH,kW)\n    O, I, kH, kW = w.shape  # shape 분해\n    K = min(O, max_kernels)  # 시각화할 커널 수 제한\n\n    imgs = []  # 커널 이미지 리스트\n    for o in range(K):\n        ker = w[o]  # (I,kH,kW)\n        if I == 3:  # RGB 입력이면 그대로 시각화 가능\n            img = (ker - ker.min()) / (ker.max() - ker.min() + 1e-6)  # 0~1 정규화\n            imgs.append(img)  # (3,kH,kW)\n        else:  # 입력 채널이 많으면 평균내서 2D로 시각화\n            img2d = ker.mean(dim=0, keepdim=True)  # (1,kH,kW)\n            img2d = (img2d - img2d.min()) / (img2d.max() - img2d.min() + 1e-6)  # 정규화\n            imgs.append(img2d)  # (1,kH,kW)\n\n    imgs = torch.stack(imgs, dim=0)  # (K,C,kH,kW)\n    grid = torchvision.utils.make_grid(imgs, nrow=8, padding=2)  # grid 생성\n    plt.figure(figsize=(12,6))\n    if grid.ndim == 3 and grid.shape[0] in (3,4):  # CHW면 HWC로 표시\n        plt.imshow(grid.permute(1,2,0))\n    else:  # 1채널이면 grayscale\n        plt.imshow(grid.squeeze(0), cmap='gray')\n    plt.title(f'Conv kernels @ {layer_name} | weight shape={tuple(w.shape)}')\n    plt.axis('off')\n    plt.show()\n\nshow_conv_kernels(model, 'conv1', max_kernels=64)  # 첫 레이어 커널\nshow_conv_kernels(model, 'layer2.0.conv1', max_kernels=64)  # 중간 레이어 커널(평균 시각화)"}, {"number": 22, "code_number": null, "type": "markdown", "source": "---\n## 9) Grad-CAM 시각화 — Top-K 정분류/오분류 (원본 + Overlay)\n- **목적:** 모델이 특정 예측을 할 때 **어느 영역을 근거로 삼는지**를 시각화합니다.\n- **관찰 포인트**\n  - 정분류 고신뢰 Top-K: 모델이 '무엇을 제대로 보고 맞추는지'\n  - 오분류 고신뢰 Top-K: 모델이 '어디를 보고 틀리는지'"}, {"number": 23, "code_number": 12, "type": "code", "source": "class GradCAM:\n    def __init__(self, model: nn.Module, target_layer: nn.Module):\n        self.model = model  # 대상 모델\n        self.target_layer = target_layer  # CAM을 뽑을 레이어\n        self.activ = None  # forward activation 저장\n        self.grad = None  # backward gradient 저장\n        self.h1 = target_layer.register_forward_hook(self._forward_hook)  # forward hook\n        self.h2 = target_layer.register_full_backward_hook(self._backward_hook)  # backward hook\n\n    def _forward_hook(self, module, inp, out):\n        self.activ = out  # (B,C,H,W) activation\n\n    def _backward_hook(self, module, grad_input, grad_output):\n        self.grad = grad_output[0]  # (B,C,H,W) gradient\n\n    def remove(self):\n        self.h1.remove()  # hook 제거\n        self.h2.remove()  # hook 제거\n\n    def __call__(self, x: torch.Tensor, class_idx: int = None):\n        self.model.zero_grad(set_to_none=True)  # grad 초기화\n        logits = self.model(x)  # forward\n        if class_idx is None:\n            class_idx = int(logits.argmax(dim=1).item())  # 예측 클래스 사용\n        score = logits[:, class_idx].sum()  # 해당 클래스 점수\n        score.backward(retain_graph=False)  # backward로 grad 얻기\n\n        w = self.grad.mean(dim=(2,3), keepdim=True)  # 채널별 중요도(gradient 평균)\n        cam = (w * self.activ).sum(dim=1, keepdim=True)  # 중요도 가중합 [B,H, W]\n        cam = F.relu(cam)  # ReLU로 양의 기여만\n        cam = cam - cam.min()  # min shift\n        cam = cam / (cam.max() + 1e-6)  # 0~1 정규화\n        return cam.detach(), logits.detach(), class_idx  # cam/logits/class 반환 [B, 1, H, W] / [B, K] / 1\n\ndef overlay_heatmap_on_image(img_chw: torch.Tensor, heat_hw: torch.Tensor, alpha=0.45):\n    img = img_chw.permute(1,2,0).cpu().numpy()  # CHW -> HWC\n    heat = heat_hw.cpu().numpy()  # heatmap to numpy\n    cmap = plt.get_cmap('jet')  # 컬러맵\n    heat_rgb = cmap(heat)[...,:3]  # heatmap을 RGB로\n    out = (1-alpha)*img + alpha*heat_rgb  # overlay\n    return np.clip(out, 0, 1)  # [0,1]로 clip\n\n@torch.no_grad()\ndef predict_all_with_conf(model, loader):\n    model.eval()  # eval 모드\n    preds, confs, labels = [], [], []  # 누적 리스트\n    for x, y in loader:\n        x = x.to(device)  # GPU 이동\n        y = y.to(device)  # GPU 이동\n        p, c, _ = predict_batch(model, x)  # 예측\n        preds.append(p.cpu())  # CPU 누적\n        confs.append(c.cpu())  # CPU 누적\n        labels.append(y.cpu())  # CPU 누적\n    return torch.cat(preds), torch.cat(confs), torch.cat(labels)  # concat\n\n# Top-K 선택(정분류/오분류 각각)\nK = 8  # 묶음 크기(원하면 변경)\npreds_all2, confs_all2, labels_all2 = predict_all_with_conf(model, test_loader)  # test 전체 예측\ncorrect_mask = (preds_all2 == labels_all2)  # 정분류 마스크\nwrong_mask = ~correct_mask  # 오분류 마스크\n\ncorr_idx = correct_mask.nonzero(as_tuple=False).squeeze(1)  # 정분류 인덱스\ncorr_sorted = corr_idx[torch.argsort(confs_all2[corr_idx], descending=True)]  # conf 내림차순 정렬\ncorr_top = corr_sorted[:K]  # Top-K 정분류\n\nwrong_idx = wrong_mask.nonzero(as_tuple=False).squeeze(1)  # 오분류 인덱스\nwrong_sorted = wrong_idx[torch.argsort(confs_all2[wrong_idx], descending=True)]  # conf 내림차순 정렬\nwrong_top = wrong_sorted[:K]  # Top-K 오분류\n\nprint('Top-K correct indices:', corr_top.tolist())  # 출력\nprint('Top-K wrong indices:', wrong_top.tolist())  # 출력\n\n# Grad-CAM 레이어(일반적으로 마지막 conv 추천)\ncam_layer_name = 'layer4.1.conv2'  # CAM 대상 레이어\ncam_layer = get_module_by_name(model, cam_layer_name)  # 레이어 객체 얻기\ngcam = GradCAM(model, cam_layer)  # GradCAM 인스턴스\n\ndef gradcam_for_indices(indices, title):\n    overlays = []  # overlay 이미지 리스트(CHW tensor)\n    originals = []  # 원본 이미지 리스트(CHW tensor)\n    captions = []  # 텍스트 정보 리스트\n\n    for idx in indices.tolist():\n        x, y = test_set[idx]  # 원본 샘플(단일)\n        x_in = x.unsqueeze(0).to(device)  # 배치화 + GPU 이동\n\n        cam, logits, used_class = gcam(x_in, class_idx=None)  # CAM 계산(예측 클래스 기준)\n        prob = F.softmax(logits, dim=1)[0, used_class].item()  # 예측 확률\n        pred_name = class_names[used_class]  # 예측 클래스명\n        gt_name = class_names[y]  # GT 클래스명\n\n        img0 = denorm(x.unsqueeze(0)).squeeze(0).clamp(0,1)  # 원본(역정규화)\n        originals.append(img0.cpu())  # 원본 저장\n\n        cam_resized = F.interpolate(cam, size=(32,32), mode='bilinear', align_corners=False)[0,0].cpu()  # 입력 크기로 resize\n        overlay = overlay_heatmap_on_image(img0, cam_resized, alpha=0.45)  # overlay 생성\n        overlays.append(torch.from_numpy(overlay).permute(2,0,1))  # HWC -> CHW 저장\n\n        captions.append(f\"GT={gt_name} | P={pred_name}({prob:.2f}) | idx={idx}\")  # 캡션 저장\n\n    # (1) 원본 그리드\n    orig_grid = torchvision.utils.make_grid(torch.stack(originals, dim=0), nrow=len(indices), padding=2)  # original grid\n    plt.figure(figsize=(2.2*len(indices), 3))\n    plt.imshow(orig_grid.permute(1,2,0))  # 표시\n    plt.axis('off')\n    plt.title(title + \" | Original\")\n    plt.show()\n\n    # (2) Grad-CAM Overlay 그리드\n    over_grid = torchvision.utils.make_grid(torch.stack(overlays, dim=0), nrow=len(indices), padding=2)  # overlay grid\n    plt.figure(figsize=(2.2*len(indices), 3))\n    plt.imshow(over_grid.permute(1,2,0))  # 표시\n    plt.axis('off')\n    plt.title(title + f\" | Grad-CAM Overlay @ {cam_layer_name}\")\n    plt.show()\n\n    for c in captions:\n        print(c)  # 텍스트로도 출력\n\ngradcam_for_indices(corr_top, f\"Top-{K} Correct (High confidence)\")  # 정분류 묶음\ngradcam_for_indices(wrong_top, f\"Top-{K} Wrong (High confidence)\")  # 오분류 묶음\ngcam.remove()  # hook 해제"}, {"number": 24, "code_number": null, "type": "markdown", "source": "---\n## 10) (추가 실습) Receptive Field 근사 계산 — '이 feature는 입력에서 얼마나 넓게 보나?'\n\n### Receptive field란?\nCNN에서 어떤 feature map의 한 위치가 **입력 이미지의 어느 범위를 보고** 계산되었는지를 나타내는 값입니다.\n- 직관적으로는 **\"이 뉴런이 입력에서 커버하는 시야 크기\"** 입니다.\n- 네트워크가 깊어질수록 receptive field가 커져서 더 넓은 문맥(context)을 반영합니다.\n\n### 왜 실습을 진행하면서 유용한가?\n- 얕은 레이어: 작은 receptive field → edge/texture 같은 로컬 특징\n- 깊은 레이어: 큰 receptive field → 객체의 부분/형태/의미적 단서\n\n### 이 코드의 근사 계산 규칙\n`rf`(receptive field 크기), `jump`(현재 feature map 한 칸이 입력에서 몇 픽셀 이동인지)을 누적합니다.\n- 커널 크기 `k`, stride `s`인 conv를 거치면\n  - `rf_new = rf + (k - 1) * jump`\n  - `jump_new = jump * s`\n\n> 결과는 엄밀한 해석보다 **직관 전달용**(깊어질수록 rf 증가)으로 보세요."}, {"number": 25, "code_number": 13, "type": "code", "source": "def rf_update(rf, jump, k, s):\n    rf_new = rf + (k - 1) * jump  # receptive field 누적\n    jump_new = jump * s  # stride 누적\n    return rf_new, jump_new\n\ndef estimate_resnet18_cifar_rf():\n    rf, jump = 1, 1  # 초기 rf=1, jump=1\n    rf, jump = rf_update(rf, jump, k=3, s=1)  # conv1(3x3, stride1)\n    for _ in range(2*2):  # layer1: 2 blocks * 2 conv\n        rf, jump = rf_update(rf, jump, k=3, s=1)\n    rf, jump = rf_update(rf, jump, k=3, s=2)  # layer2 첫 conv(stride2)\n    rf, jump = rf_update(rf, jump, k=3, s=1)\n    rf, jump = rf_update(rf, jump, k=3, s=1)\n    rf, jump = rf_update(rf, jump, k=3, s=1)\n    rf, jump = rf_update(rf, jump, k=3, s=2)  # layer3 첫 conv(stride2)\n    rf, jump = rf_update(rf, jump, k=3, s=1)\n    rf, jump = rf_update(rf, jump, k=3, s=1)\n    rf, jump = rf_update(rf, jump, k=3, s=1)\n    rf, jump = rf_update(rf, jump, k=3, s=2)  # layer4 첫 conv(stride2)\n    rf, jump = rf_update(rf, jump, k=3, s=1)\n    rf, jump = rf_update(rf, jump, k=3, s=1)\n    rf, jump = rf_update(rf, jump, k=3, s=1)\n    return rf, jump\n\nrf, jump = estimate_resnet18_cifar_rf()  # rf/jump 계산\nprint('Estimated receptive field at last conv stage:', rf)  # rf 출력\nprint('Effective stride (jump):', jump)  # jump 출력"}, {"number": 26, "code_number": null, "type": "markdown", "source": "---\n## 11) (추가 실습) Embedding 시각화 (t-SNE)\n- **목적:** 마지막 FC 직전 feature(예: 512-d)가 클래스별로 어떻게 분리되는지 확인합니다.\n- **관찰 포인트**\n    - CNN이 'feature space'에서 클래스를 구분하도록 representation을 만든다는 직관 제공"}, {"number": 27, "code_number": 14, "type": "code", "source": "import warnings\n\n# 특정 UserWarning만 무시\nwarnings.filterwarnings(\"ignore\", category=UserWarning, module=\"joblib\")\n\n@torch.no_grad()\ndef extract_penultimate_features(model, loader, max_samples=2000):\n    model.eval()  # eval 모드\n    feats, labs = [], []  # 누적\n    storage = {}  # hook 저장소\n\n    def hook(m, i, o):\n        storage['f'] = torch.flatten(o, 1).detach().cpu()  # avgpool output -> flatten\n\n    h = model.avgpool.register_forward_hook(hook)  # avgpool에 hook\n    n = 0  # 샘플 수\n    for x, y in loader:\n        x = x.to(device)  # GPU\n        _ = model(x)  # forward (hook에 feature 저장)\n        feats.append(storage['f'])  # feature 누적\n        labs.append(y)  # label 누적\n        n += x.size(0)  # 샘플 수 누적\n        if n >= max_samples:\n            break  # 샘플 제한\n    h.remove()  # hook 제거\n\n    X = torch.cat(feats, dim=0)[:max_samples].numpy()  # feature matrix\n    Y = torch.cat(labs, dim=0)[:max_samples].numpy()  # labels\n    return X, Y\n\nX_feat, y_feat = extract_penultimate_features(model, test_loader, max_samples=2000)  # feature 추출\nprint('features:', X_feat.shape)  # shape 출력\n\ntry:\n    from sklearn.manifold import TSNE  # t-SNE\n    tsne = TSNE(n_components=2, perplexity=30, learning_rate='auto', init='pca', random_state=42)  # 설정\n    X_2d = tsne.fit_transform(X_feat)  # 2D로 축소\n\n    plt.figure(figsize=(8,6))\n    plt.scatter(X_2d[:,0], X_2d[:,1], s=6, c=y_feat, alpha=0.6)  # scatter\n    # 클래스별 중심(centroid)에 클래스 이름 표시\n    for cls in range(num_classes):\n        idx = (y_feat == cls)\n        if idx.sum() > 0:\n            cx = X_2d[idx, 0].mean()\n            cy = X_2d[idx, 1].mean()\n            plt.text(cx, cy, class_names[cls], fontsize=10, weight='bold')\n    plt.title('t-SNE of penultimate features (class-labeled)')\n    plt.grid(True)\n    plt.show()\nexcept Exception as e:\n    print('TSNE failed:', e)  # 에러 출력\n    print('Note: pip install scikit-learn')  # 설치 안내"}, {"number": 28, "code_number": null, "type": "markdown", "source": "---\n## 12) (추가 실습) ONNX Export + 구조 출력 (Netron 비교)\n- **목적:** 모델을 ONNX로 내보내 Netron에서 그래프 형태로 확인합니다.\n- **관찰 포인트**\n    - `print(model)` 요약 정보와 Netron 그래프가 어떻게 매칭되는지 비교해보기"}, {"number": 29, "code_number": 15, "type": "code", "source": "# 필요 시 설치 (환경에 따라 주석 해제)\n# !pip -q install onnx onnxruntime torchinfo\n\nimport torch\nfrom IPython.display import display\nfrom torchinfo import summary\n\ntry:\n    print(\"\\n=== torchinfo summary (input_size 포함) ===\")\n    summary_result = summary(\n        model,\n        input_size=(1, 3, 32, 32),\n        device=str(device),\n        verbose=2,\n    )\nexcept Exception as e:\n    print(\"\\n(torchinfo summary skipped) Reason:\", e)\n    print(\"Note: pip install torchinfo\")\n\nonnx_path = 'resnet18_cifar10.onnx'\ndummy = torch.randn(1, 3, 32, 32, device=device)\nmodel.eval()\n\ntry:\n    torch.onnx.export(\n        model,\n        dummy,\n        onnx_path,\n        input_names=['input'],\n        output_names=['logits'],\n        opset_version=17,\n        do_constant_folding=True,\n        dynamic_axes={'input': {0: 'B'}, 'logits': {0: 'B'}},\n    )\n    print(\"\\nSaved ONNX:\", onnx_path)\n    print(\"Netron에서 위 구조와 레이어/연결이 어떻게 대응되는지 비교해보세요.\")\nexcept Exception as e:\n    print(\"ONNX export failed:\", e)\n    print(\"Note: pip install onnx\")"}], "blanks": [{"id": "blank-random-crop", "label": "학습 이미지 Random Crop", "answer": "T.RandomCrop(32, padding=4)"}, {"id": "blank-normalize", "label": "CIFAR 채널 정규화", "answer": "T.Normalize(CIFAR10_MEAN, CIFAR10_STD)"}, {"id": "blank-cifar-stem", "label": "CIFAR 입력용 Conv Stem", "answer": "nn.Conv2d(3, 64, kernel_size=3, stride=1, padding=1, bias=False)"}]}, "02_ViT_CIFAR10.ipynb": {"page": "vision_vit_subjective_test", "cells": [{"number": 1, "code_number": null, "type": "markdown", "source": "# **Vision Transformer with CIFAR-10 — 패치 임베딩부터 분류까지**\n\n**Vision Transformer(ViT)** 실습을 목표로 합니다.  \nCNN과 달리 ViT는 이미지를 **패치(patch) 시퀀스**로 바꾼 뒤, NLP의 Transformer Encoder와 거의 같은 방식으로 처리합니다.\n\n## 학습 목표\n- 이미지를 **패치 토큰**으로 바꾸는 과정(= Patch Embedding)을 이해한다.\n- **[CLS] 토큰 + Positional Embedding** 이 왜 필요한지 설명할 수 있다.\n- Transformer Encoder의 핵심 구성(**Pre-LN / MHSA / FFN / Residual**)을 코드에서 찾아 읽을 수 있다.\n- 학습 후 **오분류(실패) 샘플**을 통해 모델의 한계를 분석한다.\n\n> 권장 흐름: (1) 데이터/전처리 → (2) ViT 구성 → (3) 학습/평가 → (4) 오분류 분석"}, {"number": 2, "code_number": null, "type": "markdown", "source": "---\n## 0) Setup (환경 준비)\n\n- **목적:** 실습에 필요한 패키지를 로드하고, 재현 가능한 실험을 위해 시드를 고정합니다.\n- **관찰 포인트**\n  - 실습에 필요한 유틸 패키지(`einops`, `torchinfo` 등) 준비\n  - GPU 사용 여부(`cuda`) 확인"}, {"number": 3, "code_number": 1, "type": "code", "source": "import importlib.util\nimport subprocess\nimport sys\n\ndef ensure_package(pkg_name: str, import_name=None):\n    \"\"\"\n    패키지 설치 여부를 확인하고, 설치되어 있지 않으면 설치합니다.\n    \"\"\"\n    name = import_name or pkg_name\n    # 패키지가 설치되어 있는지 확인\n    if importlib.util.find_spec(name) is None:\n        print(f\"[install] {pkg_name} 라이브러리를 설치 중입니다... (import name: {name})\")\n        try:\n            # -q 옵션을 추가하여 설치 과정을 간결하게 유지할 수 있습니다.\n            subprocess.check_call([sys.executable, \"-m\", \"pip\", \"install\", \"-q\", pkg_name])\n            print(f\"[success] {pkg_name} 설치 완료.\")\n        except subprocess.CalledProcessError as e:\n            print(f\"[error] {pkg_name} 설치 실패: {e}\")\n    else:\n        print(f\"[ok] {pkg_name} 이미 설치되어 있습니다.\")\n\n# 설치가 필요한 패키지 리스트 (패키지명, 임포트명)\n# 임포트명이 패키지명과 다른 경우 튜플로 지정합니다.\npackages = [\n    (\"einops\", \"einops\"),\n    (\"torchinfo\", \"torchinfo\")\n]\n\n# 루프를 돌며 확인 및 설치\nfor pkg, imp in packages:\n    ensure_package(pkg, imp)"}, {"number": 4, "code_number": 2, "type": "code", "source": "import torch\nfrom torch import nn\nfrom torch import nn, einsum\nimport torch.nn.functional as F  # 함수형 API(F): activation/loss 등\nfrom torch import optim\n\nfrom einops import rearrange, repeat  # ViT에서 자주 쓰는 텐서 재배열(rearrange)·복제(repeat)\nfrom einops.layers.torch import Rearrange\nimport numpy as np\nimport torchvision  # 데이터/변환(torchvision) 사용 (CIFAR-10 포함)\nimport time\nfrom torchinfo import summary\n\nprint('torch:', torch.__version__)  # torch 버전 출력\nprint('torchvision:', torchvision.__version__)  # torchvision 버전 출력\nprint('cuda available:', torch.cuda.is_available())  # GPU 사용 가능 여부\nif torch.cuda.is_available():\n    print('gpu:', torch.cuda.get_device_name(0))  # GPU 이름 출력\n\ntry:\n    get_ipython().system('nvidia-smi -L')  # GPU 목록 확인(가능한 경우)\nexcept Exception as e:\n    print('nvidia-smi not available:', e)  # nvidia-smi가 없는 환경이면 무시\n\ndevice = torch.device(\"cuda\" if torch.cuda.is_available() else \"cpu\")\nprint(\"Using device:\", device)"}, {"number": 5, "code_number": null, "type": "markdown", "source": "---\n## 1) 데이터 로딩 & 전처리\n\n- **목적:** CIFAR-10(컬러 32×32)를 로드하고, 모델 입력 텐서 형태를 확인합니다.\n- **관찰 포인트**\n  - 입력 텐서 shape: `B×C×H×W` (CIFAR-10는 `C=3`)\n  - 학습/테스트 로더 구성과 배치 단위 학습의 의미"}, {"number": 6, "code_number": 3, "type": "code", "source": "import os\nimport torchvision\nimport torchvision.transforms as T\nfrom torch.utils.data import DataLoader\n\n# CIFAR-10에서 널리 쓰는 normalize 값\nCIFAR10_MEAN = (0.4914, 0.4822, 0.4465)  # 채널별 평균(R,G,B)\nCIFAR10_STD  = (0.2023, 0.1994, 0.2010)  # 채널별 표준편차(R,G,B)\n\ntrain_tfms = T.Compose([  # train transform(augmentation 포함)\n    T.RandomCrop(32, padding=4),      # 32x32를 padding 후 random crop\n    T.RandomHorizontalFlip(),         # 좌우 반전\n    T.ToTensor(),                     # PIL -> torch tensor (C,H,W)\n    T.Normalize(CIFAR10_MEAN, CIFAR10_STD),  # 정규화\n])\n\ntest_tfms = T.Compose([  # test transform(augmentation 없음)\n    T.ToTensor(),                     # 텐서 변환\n    T.Normalize(CIFAR10_MEAN, CIFAR10_STD),  # 정규화\n])\n\ndata_root = './data'  # 데이터 저장 경로\ntrain_set = torchvision.datasets.CIFAR10(root=data_root, train=True, download=True, transform=train_tfms)  # train set\ntest_set  = torchvision.datasets.CIFAR10(root=data_root, train=False, download=True, transform=test_tfms)  # test set\n\nclass_names = train_set.classes  # 클래스 이름 목록\nnum_classes = len(class_names)   # 클래스 개수(10)\nprint('classes:', class_names)   # 클래스 출력\n\n# 이미지 사이즈 출력\nx0, y0 = train_set[0]  # 한 샘플 로드(이미 transform 적용된 텐서)\nprint('Sample image tensor shape (C,H,W):', tuple(x0.shape))  # (3,32,32)\n\nbatch_size = 256  # 배치 크기(A10이면 보통 여유)\nnum_workers = min(8, os.cpu_count() or 2)  # dataloader worker 수(환경에 맞게)\n\ntrain_loader = DataLoader(train_set, batch_size=batch_size, shuffle=True, num_workers=num_workers, pin_memory=True)  # train loader\ntest_loader  = DataLoader(test_set, batch_size=batch_size, shuffle=False, num_workers=num_workers, pin_memory=True)  # test loader\n\nprint('Total batch size (train: %d, test: %d)' % (len(train_loader), len(test_loader)))  # 배치 개수 확인"}, {"number": 7, "code_number": 4, "type": "code", "source": "import matplotlib.pyplot as plt\nimport torch\n\ndef denormalize_cifar10(img_chw: torch.Tensor):\n    \"\"\"정규화된 CIFAR-10 텐서(C,H,W)를 시각화용(0~1)으로 되돌립니다.\"\"\"\n    mean = torch.tensor(CIFAR10_MEAN, device=img_chw.device)[:, None, None]\n    std  = torch.tensor(CIFAR10_STD,  device=img_chw.device)[:, None, None]\n    x = img_chw * std + mean\n    return x.clamp(0, 1)\n\ndef show_batch(data_loader, max_images: int = 16, nrow: int = 4, figsize=(10, 10)):\n    \"\"\"배치에서 일부 샘플을 보기 좋게 시각화합니다.\n\n    - 기본값은 16장(4×4)을 비교적 크게 보여주는 설정입니다.\n    - max_images/nrow/figsize를 바꾸면 '더 적게/더 많이', '더 크게/더 작게'를 조절할 수 있습니다.\n\n    - CIFAR-10은 (C,H,W)=(3,32,32) RGB이므로, 시각화할 때는 (H,W,C)로 바꿔야 합니다.\n    - make_grid 결과가 figure 안에서 '밀려 보이는' 경우가 있어, subplot 여백을 0으로 맞춰 꽉 차게 표시합니다.\n\n    Args:\n        data_loader: DataLoader\n        max_images: 한 번에 보여줄 이미지 개수(기본 16장)\n        nrow: grid 한 줄에 배치할 이미지 개수(기본 4개)\n        figsize: figure 크기\n    \"\"\"\n    batch = next(iter(data_loader))\n    images, labels = batch  # images: (B,C,H,W)\n    images = images[:max_images]\n\n    # make_grid는 (B,C,H,W) → grid(C,H,W)\n    grid = torchvision.utils.make_grid(images, nrow=nrow, padding=2)\n\n    # 시각화는 정규화 해제 후 HWC로 변환\n    grid = denormalize_cifar10(grid).detach().cpu().numpy().transpose((1, 2, 0))\n\n    fig, ax = plt.subplots(figsize=figsize)\n    ax.imshow(grid, interpolation=\"nearest\")\n    ax.set_title(f\"Batch Samples (CIFAR-10) | shown={len(images)}\")\n    ax.axis(\"off\")\n\n    # 여백 제거: grid가 4×4에 꽉 차도록\n    fig.subplots_adjust(left=0, right=1, bottom=0, top=1)\n    plt.show()\n\n# 미리보기: 크게(figure) + 적게(16장) 보기\nshow_batch(train_loader, max_images=16, nrow=4, figsize=(10, 10))\nshow_batch(train_loader, max_images=16, nrow=4, figsize=(10, 10))"}, {"number": 8, "code_number": null, "type": "markdown", "source": "---\n## 2) ViT 모델 구성 개요\n\nViT는 크게 아래 3단계로 이해하면 읽기 쉬워집니다.\n\n1. **Patch Embedding**  \n   이미지(`H×W`)를 `P×P` 패치로 잘라 **토큰 시퀀스**로 바꾸고, 각 패치를 `dim` 차원으로 임베딩합니다.\n\n2. **Transformer Encoder (× depth)**  \n   토큰 시퀀스에 대해 반복적으로  \n   **(Pre-LN → Multi-Head Self-Attention → Residual) + (Pre-LN → FFN → Residual)** 를 수행합니다.\n\n3. **Classification Head**  \n   보통 `[CLS]` 토큰(또는 평균 풀링)을 사용해 최종 분류 로짓을 출력합니다.\n\n> 아래 코드에서 `dim / depth / heads / mlp_dim` 이 무엇을 의미하는지 주석과 함께 확인해 보세요."}, {"number": 9, "code_number": 5, "type": "code", "source": "def pair(t):  # image_size/patch_size를 (H,W) 형태로 통일하는 유틸\n    return t if isinstance(t, tuple) else (t, t)\n\n\nclass PreNorm(nn.Module):  # Transformer의 Pre-LN 구조: LayerNorm 후 블록 실행\n    def __init__(self, dim, fn):\n        super().__init__()\n        self.norm = nn.LayerNorm(dim)  # 토큰 임베딩 차원(dim) 기준 LayerNorm -> 패치별 정규화\n        self.fn = fn\n    def forward(self, x, **kwargs):\n        return self.fn(self.norm(x), **kwargs)  # 정규화된 토큰을 Attention/FFN에 전달\n\nclass FeedForward(nn.Module):  # Transformer의 FFN(MLP) 블록 -> 패치 자체의 정보를 더 깊게 분석\n    def __init__(self, dim, hidden_dim, dropout = 0.):\n        super().__init__()\n        self.net = nn.Sequential(  # FFN: Linear→GELU→Dropout→Linear→Dropout 구성\n            nn.Linear(dim, hidden_dim),\n            nn.ReLU(),\n            nn.Dropout(dropout),\n            nn.Linear(hidden_dim, dim),\n            nn.Dropout(dropout)\n        )\n    def forward(self, x):\n        return self.net(x)\n\nclass Attention(nn.Module):  # 멀티헤드 Self-Attention(MHSA) 구현\n    def __init__(self, dim, heads = 4, dim_head = 64, dropout = 0.):\n        super().__init__()\n        inner_dim = dim_head *  heads  # 전체 헤드 차원 = head 수 × head 차원\n        project_out = not (heads == 1 and dim_head == dim)\n\n        self.heads = heads  # 멀티헤드 개수 저장\n        self.scale = dim_head ** -0.5  # Scaled dot-product를 위한 스케일(1/√d)\n\n        self.attend = nn.Softmax(dim = -1)  # attention score를 확률로 변환(softmax), 마지막 차원 : sequence length N\n        self.last_attn = None  # (학습/추론 시) 마지막 forward에서의 attention map 저장용(시각화/분석)  # attention score를 확률로 변환(softmax), 마지막 차원 : sequence length N\n        self.to_qkv = [[BLANK:blank-vit-qkv]]  # 입력 토큰→Q,K,V를 한 번에 선형 변환\n\n        self.to_out = nn.Sequential(  # 헤드들을 합친 뒤 출력 투영 + 드롭아웃\n            nn.Linear(inner_dim, dim),\n            nn.Dropout(dropout)\n        ) if project_out else nn.Identity()\n\n    def forward(self, x):\n        b, n, _, h = *x.shape, self.heads   # x = [batch Size, tokens, embedding dimension]\n        qkv = self.to_qkv(x).chunk(3, dim = -1)  # 선형변환 결과를 Q,K,V로 분할 -> (Q, K ,V)\n        q, k, v = map(lambda t: rearrange(t, 'b n (h d) -> b h n d', h = h), qkv)  # multi-head 연산을 위해 각 head 기준 n x d 로 분리\n\n        dots = [[BLANK:blank-vit-scores]]  # 각 토큰 간 유사도(Q·Kᵀ) 계산 후 스케일 적용\n\n        attn = self.attend(dots)  # 토큰 간 가중치(attention map) 생성\n        self.last_attn = attn.detach()  # 그래프에서 분리(detach)해서 저장 (시각화 목적)\n\n        out = [[BLANK:blank-vit-weighted-sum]]  # attention 가중합으로 새로운 토큰 표현(out) 계산\n        out = rearrange(out, 'b h n d -> b n (h d)')  # 헤드 차원을 다시 합쳐 (batch, tokens, dim)로 복원\n        return self.to_out(out)  # 최종 투영을 거쳐 Attention 블록 출력 반환\n\nclass Transformer(nn.Module):  # Encoder block을 여러 층(depth) 쌓는 Transformer\n    def __init__(self, dim, depth, heads, dim_head, mlp_dim, dropout = 0.):\n        super().__init__()\n        self.layers = nn.ModuleList([])  # 각 층(Attention+FFN)을 담을 컨테이너\n        for _ in range(depth):  # depth 만큼 Encoder block 반복 생성\n            self.layers.append(nn.ModuleList([  # 한 층 = (PreNorm+Attention) + (PreNorm+FFN)\n                PreNorm(dim, Attention(dim, heads = heads, dim_head = dim_head, dropout = dropout)),\n                PreNorm(dim, FeedForward(dim, mlp_dim, dropout = dropout))\n            ]))\n    def forward(self, x):\n        for attn, ff in self.layers:\n            x = attn(x) + x  # Residual 연결: Attention 결과를 입력에 더해 정보 보존\n            x = ff(x) + x  # Residual 연결: FFN 결과를 입력에 더해 정보 보존\n        return x"}, {"number": 10, "code_number": null, "type": "markdown", "source": "---\n## 2-1) ViTConfig + ViT(모델 본체) 구현\n\n- **목적:** ViT의 핵심 구성 요소(패치 임베딩, CLS 토큰, 위치 임베딩, Transformer Encoder 블록, 분류 헤드)를 **코드로 직접 확인**합니다.\n- **관찰 포인트**\n  - `image_size`, `patch_size` → 패치 개수(`num_patches`)와 토큰 시퀀스 길이가 어떻게 결정되는지\n  - `depth`(블록 개수), `heads`(멀티헤드 수), `dim`(토큰 임베딩 차원)이 **연산량/표현력**에 어떤 영향을 주는지\n  - **Pre-LN + Residual** 구조가 어디에 적용되는지(안정적인 학습을 위한 표준 패턴)"}, {"number": 11, "code_number": 6, "type": "code", "source": "from dataclasses import dataclass  # 설정값을 묶어 init 인자를 단순화하기 위한 도구\nfrom typing import Tuple, Union  # 파이썬 버전 호환을 위한 타입 힌트\n\n@dataclass\nclass ViTConfig:\n    # 입력/토큰화 관련\n    image_size: 'Union[int, Tuple[int, int]]'          # 입력 이미지 크기 (H, W) 또는 정수(정수면 정사각형)\n    patch_size: 'Union[int, Tuple[int, int]]'          # 패치 크기 (Ph, Pw) 또는 정수\n    channels: int = 3                          # 입력 채널 수 (CIFAR-10=3, MNIST=1)\n\n    # 모델 본체(Encoder) 관련\n    dim: int = 64                              # 토큰 임베딩 차원(Transformer의 hidden size)\n    depth: int = 6                             # Encoder 블록 개수(= Transformer layer 수)\n    heads: int = 4                             # Multi-Head Self-Attention의 head 개수\n    dim_head: int = 64                         # 각 head의 Q/K/V 차원\n    mlp_dim: int = 128                         # FFN(MLP) 중간 차원\n\n    # 분류/출력 관련\n    num_classes: int = 10                      # 분류 클래스 수\n    pool: str = \"cls\"                          # \"cls\": CLS 토큰 사용, \"mean\": 토큰 평균 풀링\n    dropout: float = 0.0                       # Encoder 내부 dropout(Attention/FFN)\n    emb_dropout: float = 0.0                   # 패치+포지션 임베딩 단계 dropout\n\n\nclass ViT(nn.Module):\n    \"\"\"Vision Transformer (ViT)\n    - 이미지를 패치로 쪼개 토큰 시퀀스를 만들고\n    - CLS 토큰 + 위치 임베딩을 더한 뒤\n    - Transformer Encoder로 전역(Self-Attention) 관계를 학습하여\n    - CLS(또는 mean pool) 표현으로 분류합니다.\n    \"\"\"\n\n    def __init__(self, cfg: ViTConfig):\n        super().__init__()\n\n        # 1) 입력 해상도/패치 크기를 (H,W) 튜플로 정규화\n        image_height, image_width = pair(cfg.image_size)\n        patch_height, patch_width = pair(cfg.patch_size)\n\n        # 2) 패치가 이미지에 딱 나누어 떨어져야 (h, w) 그리드가 정확히 형성됨\n        assert image_height % patch_height == 0 and image_width % patch_width == 0, \"Image dimensions must be divisible by the patch size.\"\n\n        # 3) 패치 개수(=토큰 개수)와 한 패치의 펼친 차원 계산\n        num_patches = (image_height // patch_height) * (image_width // patch_width)\n        patch_dim = cfg.channels * patch_height * patch_width\n\n        # 4) 풀링 방식 검증: CLS 토큰을 쓸지 mean pool을 쓸지 선택\n        assert cfg.pool in {\"cls\", \"mean\"}, \"pool must be 'cls' or 'mean'\"\n\n        # 5) 패치 토큰화: (B,C,H,W) -> (B, N, patch_dim) -> (B, N, dim)\n        self.to_patch_embedding = nn.Sequential(\n            # 패치 그리드로 자른 뒤, 각 패치를 1D 벡터로 펼쳐 토큰 시퀀스를 만듦\n            Rearrange(\n                \"b c (h p1) (w p2) -> b (h w) (p1 p2 c)\",\n                p1=patch_height,\n                p2=patch_width,\n            ),\n            # 펼친 패치 벡터를 Transformer hidden size(dim)로 선형 투영\n            nn.Linear(patch_dim, cfg.dim),\n        )\n\n        # 6) CLS 토큰 + 위치 임베딩(학습 파라미터)\n        self.cls_token = nn.Parameter(torch.randn(1, 1, cfg.dim))\n        self.pos_embedding = nn.Parameter(torch.randn(1, num_patches + 1, cfg.dim))\n        self.dropout = nn.Dropout(cfg.emb_dropout)\n\n        # 7) Transformer Encoder 스택(Attention + FFN + Residual/PreNorm)\n        self.transformer = Transformer(\n            dim=cfg.dim,\n            depth=cfg.depth,\n            heads=cfg.heads,\n            dim_head=cfg.dim_head,\n            mlp_dim=cfg.mlp_dim,\n            dropout=cfg.dropout,\n        )\n\n        self.pool = cfg.pool\n\n        # 8) 분류 헤드: (CLS/mean) 표현 -> LayerNorm -> Linear(logits)\n        self.mlp_head = nn.Sequential(\n            nn.LayerNorm(cfg.dim),\n            nn.Linear(cfg.dim, cfg.num_classes),\n        )\n\n    def forward(self, img):\n        # A) 패치 임베딩으로 토큰 시퀀스 생성: (B,C,H,W) -> (B,N,dim)\n        x = self.to_patch_embedding(img)\n        b, n, _ = x.shape\n\n        # B) CLS 토큰을 배치만큼 복제해 시퀀스 맨 앞에 붙임: (B,1,dim)\n        cls_tokens = repeat(self.cls_token, \"1 1 d -> b 1 d\", b=b)\n        x = torch.cat((cls_tokens, x), dim=1)  # (B, N+1, dim)\n\n        # C) 위치 임베딩을 더해 토큰 순서(공간 위치) 정보를 주입\n        x = x + self.pos_embedding[:, : (n + 1)]   # (B, N+1, dim) + (1, N+1, dim) -> Broadcasting\n        x = self.dropout(x)\n\n        # D) Encoder를 통과하며 전역 의존성(Self-Attention) 학습\n        x = self.transformer(x)  # (B, N+1, dim)\n\n        # E) 이미지 표현 벡터 선택: CLS 토큰(0번) 또는 mean pooling\n        x = x[:, 0] if self.pool == \"cls\" else x.mean(dim=1)   # (B, dim)\n\n        # F) 최종 logits 출력 (softmax는 CrossEntropyLoss 내부에서 처리)\n        return self.mlp_head(x)"}, {"number": 12, "code_number": null, "type": "markdown", "source": "---\n## 2-2) ViT 하이퍼파라미터 설정 (vit_cfg)\n\n- **목적:** ViT의 구조를 결정하는 설정값을 한 곳에서 정의하고, 아래 `ViT(vit_cfg)`가 **어떤 형태의 모델**을 만드는지 연결해 봅니다.\n- **관찰 포인트**\n  - `patch_size`가 작아질수록 토큰 수가 늘어 **Self-Attention 비용**이 증가합니다.\n  - `depth/heads/dim`은 성능뿐 아니라 **학습 시간·메모리**에도 직접 영향을 줍니다.\n  - CIFAR-10(32×32, RGB)에서도 과도한 설정은 **학습 불안정/과적합**을 유발할 수 있습니다."}, {"number": 13, "code_number": 7, "type": "code", "source": "# ViT 설정을 한 곳에 모아두면(=config) 모델 구조를 한눈에 파악하기 쉽습니다.\nvit_cfg = ViTConfig(\n        image_size=32,      # CIFAR-10: 32x32\n    patch_size=4,       # 4x4 패치 -> (32/4)^2 = 64개 패치 토큰\n        channels=3,         # CIFAR-10은 컬러(3채널)\n    dim=64,             # 토큰 임베딩 차원\n    depth=6,            # Encoder 블록 수\n    heads=4,            # MHSA head 수\n    dim_head=64,        # head당 Q/K/V 차원\n    mlp_dim=128,        # FFN 중간 차원\n    num_classes=10,     # 0~9\n    pool=\"cls\",         # CLS 토큰으로 분류\n    dropout=0.0,        # Encoder dropout\n    emb_dropout=0.0,    # 임베딩 dropout\n)\n\nmodel = ViT(vit_cfg).to(device)\noptimizer = optim.Adam(model.parameters(), lr=0.003)    # Momentum + RMSProp"}, {"number": 14, "code_number": null, "type": "markdown", "source": "## 4) 체크포인트 저장/이어학습(Resume)\n\n- **목적:** 학습 결과를 `ckpt_path`로 저장하고, 파일이 있으면 **이어서 추가 학습**합니다.\n- **관찰 포인트**\n  - 체크포인트가 있을 때: `start_epoch`부터 이어서 **추가 10 epoch**\n  - 체크포인트가 없을 때: 처음부터 **20 epoch**"}, {"number": 15, "code_number": 8, "type": "code", "source": "# 체크포인트(ckpt) 저장/로드\n# - ckpt 파일이 있으면: 이어서 추가 5 epoch 학습(resume)\n# - ckpt 파일이 없으면: 처음부터 10 epoch 학습\n\nckpt_path = \"vit_cifar10.pth\"  # 필요시 파일명/경로만 바꿔서 사용하세요.\nstart_epoch = 0\nlast_acc = 0.0  # 체크포인트가 있을 때 로드되는 이전 test accuracy\n\nif os.path.exists(ckpt_path):\n    ckpt = torch.load(ckpt_path, map_location=device)\n    model.load_state_dict(ckpt[\"model\"])\n    optimizer.load_state_dict(ckpt[\"optimizer\"])\n    start_epoch = int(ckpt.get(\"epoch\", 0))\n    last_acc = float(ckpt.get(\"test_acc\", 0.0))\n    print(f\"[ckpt] Loaded: {ckpt_path} (epoch={start_epoch}, test_acc={last_acc*100:.2f}%)\")\n    extra_epochs = 10\nelse:\n    print(f\"[ckpt] Not found: {ckpt_path} -> train from scratch\")\n    extra_epochs = 20\n\n\n# best_acc: 지금까지 저장된 체크포인트 중 최고 정확도(업데이트 기준)\nbest_acc = last_acc\n\ntarget_epoch = start_epoch + extra_epochs\nprint(f\"[plan] Train epochs: {start_epoch+1} .. {target_epoch} (total {extra_epochs} epoch(s))\")"}, {"number": 16, "code_number": null, "type": "markdown", "source": "---\n## 3) 모델 구조 요약 & 파라미터 수 확인\n\n- **목적**: 레이어 구성을 빠르게 보고, 학습 가능한 파라미터 규모를 숫자로 확인합니다.\n- **관찰 포인트:**\n  - Patch Embedding이 만들어내는 토큰 수(= 패치 개수 + 1[CLS])\n  - Encoder 블록이 `depth`만큼 반복되는지\n  - 마지막 분류 헤드(Linear)가 어디에 붙는지\n  - `dim/depth/heads`를 키우면 파라미터/연산량이 어떻게 커지는지"}, {"number": 17, "code_number": 9, "type": "code", "source": "# (1) 모델 구조 요약: 레이어와 텐서 흐름을 빠르게 확인\n# torchinfo.summary는 입력 텐서 크기를 알면 훨씬 정확한 구조를 출력합니다.\n_summary = summary(model, input_size=(1, 3, 32, 32), device=str(device))\nprint(_summary)\n\n# (2) 학습 가능한 파라미터 수: 모델 '규모'를 숫자로 확인\ndef count_parameters(m):\n    return sum(p.numel() for p in m.parameters() if p.requires_grad)\n\nprint(\"Trainable parameters:\", count_parameters(model))"}, {"number": 18, "code_number": null, "type": "markdown", "source": "---\n## 4) Training (학습 루프)\n\n- **목적:** ViT를 CIFAR-10 학습 데이터로 학습시켜, *토큰 기반 분류가 실제로 동작*하는 것을 확인합니다.\n- **관찰 포인트:**\n  - `model.train()` 상태에서 **드롭아웃/정규화**가 어떻게 동작하는지\n  - 손실(loss)이 줄어드는지(학습이 되는지)"}, {"number": 19, "code_number": 10, "type": "code", "source": "def train_epoch(model, optimizer, data_loader, loss_history):\n    \"\"\"한 epoch 학습을 수행하고, 진행 상황을 epoch당 '딱 5번'만 출력합니다.\"\"\"\n    total_samples = len(data_loader.dataset)\n    model.train()\n\n    # epoch당 출력 5회(0%, 25%, 50%, 75%, 마지막)로 고정\n    n_steps = len(data_loader)\n    log_steps = {\n        0,\n        max(0, n_steps // 4),\n        max(0, n_steps // 2),\n        max(0, (3 * n_steps) // 4),\n        n_steps - 1,\n    }\n\n    for i, (data, target) in enumerate(data_loader):\n        data = data.to(device)\n        target = target.to(device)\n\n        optimizer.zero_grad()\n        output = F.log_softmax(model(data), dim=1)  # Softmax\n        loss = F.nll_loss(output, target)           # Negative Log Likelihood\n        # nn.CrossEntropyLoss(model(data), target)\n        loss.backward()\n        optimizer.step()\n\n        if i in log_steps:\n            seen = min((i + 1) * len(data), total_samples)\n            print(f\"[{seen:5d}/{total_samples:5d} ({100.0 * (i+1) / n_steps:5.1f}%)]  Loss: {loss.item():.4f}\")\n            loss_history.append(loss.item())"}, {"number": 20, "code_number": 11, "type": "code", "source": "def evaluate(model, data_loader, loss_history):\n    \"\"\"test_loader로 1회 평가하고 (loss, acc)를 반환합니다.\"\"\"\n    model.eval()\n\n    total_samples = len(data_loader.dataset)\n    correct_samples = 0\n    total_loss = 0.0\n\n    with torch.no_grad():\n        for data, target in data_loader:\n            data = data.to(device)\n            target = target.to(device)\n\n            output = F.log_softmax(model(data), dim=1)\n            loss = F.nll_loss(output, target, reduction='sum')\n            _, pred = torch.max(output, dim=1)\n\n            total_loss += float(loss.item())\n            correct_samples += int(pred.eq(target).sum().item())\n\n    avg_loss = total_loss / total_samples\n    acc = correct_samples / total_samples\n\n    loss_history.append(avg_loss)\n    print(\n        f\"Average test loss: {avg_loss:.4f}  \"\n        f\"Accuracy: {correct_samples:5d}/{total_samples:5d} ({acc*100:.2f}%)\\n\"\n    )\n    return avg_loss, acc"}, {"number": 21, "code_number": null, "type": "markdown", "source": "> 참고: CPU로 실행하면 epoch 당 시간이 꽤 걸릴 수 있습니다.  \n> 가능하면 GPU 환경에서 실행하는 것을 권장합니다. (CIFAR-10는 작지만, Transformer는 토큰 연산이 많습니다.)"}, {"number": 22, "code_number": null, "type": "markdown", "source": "---\n## 5) Training (학습)\n\n- **목적:** 학습 데이터로 파라미터를 업데이트합니다.\n- **관찰 포인트:**\n  - `model.train()` 모드에서 Dropout/BN 등이 어떻게 동작하는지\n  - Loss가 epoch이 진행되며 감소하는지(과적합/학습 실패 징후 관찰)\n  - 매 epoch 평가 후, **정확도가 개선된 경우에만(best) 체크포인트를 업데이트**합니다."}, {"number": 23, "code_number": 12, "type": "code", "source": "# ckpt 기준으로 학습 epoch 수가 자동 결정됩니다.\n# - ckpt가 있으면(start_epoch>0): 이어서 추가 10 epoch\n# - ckpt가 없으면(start_epoch=0): 처음부터 20 epoch\n\nstart_time = time.time()\n\ntrain_loss_history = []\ntest_loss_history = []\n\nfor epoch in range(start_epoch + 1, target_epoch + 1):\n    print(f\"Epoch: {epoch}/{target_epoch}\")\n\n    # 학습\n    train_epoch(model, optimizer, train_loader, train_loss_history)\n\n    # 매 epoch마다 test set으로 1회 평가(accuracy 출력)\n    test_loss, test_acc = evaluate(model, test_loader, test_loss_history)\n\n    # 정확도가 '개선'된 경우에만 체크포인트를 업데이트합니다.\n    # - 이렇게 하면 가장 좋은(best) 모델만 ckpt_path에 남습니다.\n    if test_acc > best_acc:\n        best_acc = test_acc\n        torch.save(\n            {\n                \"epoch\": epoch,\n                \"model\": model.state_dict(),\n                \"optimizer\": optimizer.state_dict(),\n                \"test_acc\": test_acc,\n                \"test_loss\": test_loss,\n                \"vit_cfg\": getattr(vit_cfg, \"__dict__\", dict(vit_cfg=repr(vit_cfg))),\n            },\n            ckpt_path,\n        )\n        print(f\"[ckpt] Updated(best): {ckpt_path} (epoch={epoch}, best_acc={best_acc*100:.2f}%)\\n\")\n    else:\n        print(f\"[ckpt] Skip (not improved): best_acc={best_acc*100:.2f}% (this={test_acc*100:.2f}%)\\n\")\n\nprint(\"Execution time:\", \"{:5.2f}\".format(time.time() - start_time), \"seconds\")"}, {"number": 24, "code_number": null, "type": "markdown", "source": "---\n## 6) Testing (Attention)\n\n- **목적:** ViT의 예측이 어떤 **패치(토큰)** 정보에 의존하는지, CLS 토큰의 attention을 통해 직관적으로 확인합니다.\n- **관찰 포인트:**\n  - CLS 토큰이 **어떤 위치(패치)** 에 강하게 attention을 주는지\n  - layer/head를 바꾸면 attention 패턴이 어떻게 달라지는지\n  - (선택) **정답/오답 샘플**에서 attention이 얼마나 설득력 있게 보이는지"}, {"number": 25, "code_number": 13, "type": "code", "source": "# (실습) Testing (Attention): ViT가 '어디를 보고' 예측했는지(=CLS→Patch Attention) 시각화\n# - 마지막 logits는 CLS 토큰에서 나오므로, \"CLS 토큰이 어떤 패치 토큰들을 참고했는지\"를 보면 모델의 단서를 얻을 수 있습니다.\n# - 실무/연구 관점에서도 초기 레이어(layer_idx=0)는 저수준/분산된 패턴이 많아 해석이 애매한 경우가 많아서,\n#   기본값은 \"맨 마지막 Transformer 블록\"의 attention을 보는 쪽이 직관적입니다.\n# - 또한 'head 평균'뿐 아니라 '각 head'가 서로 다른 패턴을 볼 수 있으므로, 여러 head를 한 번에 오버레이해서 비교합니다.\n\nimport matplotlib.pyplot as plt\nimport torch.nn.functional as F\nimport numpy as np\nimport random\n\n\ndef denormalize_cifar10_chw(img_chw: torch.Tensor):\n    \"\"\"정규화된 CIFAR-10 이미지(C,H,W)를 시각화용(0~1)으로 복원합니다.\"\"\"\n    mean = torch.tensor(CIFAR10_MEAN, device=img_chw.device)[:, None, None]\n    std  = torch.tensor(CIFAR10_STD,  device=img_chw.device)[:, None, None]\n    x = img_chw * std + mean\n    return x.clamp(0, 1)\n\n\ndef chw_to_hwc_np(img_chw: torch.Tensor) -> np.ndarray:\n    \"\"\"(C,H,W) 텐서를 matplotlib imshow용 (H,W,C) numpy로 변환합니다.\n    - CIFAR-10은 정규화가 적용되어 있으므로 denormalize 후 표시합니다.\n    \"\"\"\n    x = denormalize_cifar10_chw(img_chw)  # (C,H,W), 0~1\n    x = x.detach().cpu()\n    # (C,H,W) -> (H,W,C)\n    x_hwc = x.permute(1, 2, 0).numpy()\n    return x_hwc\n\ndef imshow_chw(ax, img_chw: torch.Tensor, title: str = None):\n    \"\"\"CHW 텐서를 안전하게 imshow합니다.\"\"\"\n    x_hwc = chw_to_hwc_np(img_chw)\n    ax.imshow(x_hwc)\n    ax.axis(\"off\")\n    if title is not None:\n        ax.set_title(title)\n\n\ndef _extract_last_attn(model, layer_idx: int):\n    \"\"\"지정한 encoder 블록(layer)의 Attention 모듈이 저장해둔 last_attn을 꺼냅니다.\n    - Attention.forward에서 self.last_attn = attn.detach()로 저장하도록 구현되어 있어야 합니다.\n    - 반환 shape: [B, H, N, N] (B=batch, H=head, N=tokens=1+num_patches)\n    \"\"\"\n    # transformer.layers[layer_idx] = (PreNorm(Attention), PreNorm(FFN)) 형태를 가정\n    attn_module = model.transformer.layers[layer_idx][0].fn  # [0]:Attention, [1]: FF, PreNorm(fn=Attention)\n    attn = attn_module.last_attn\n    if attn is None:\n        raise RuntimeError(\"Attention.last_attn이 비어 있습니다. 먼저 model(x)를 한 번 forward 해야 합니다.\")\n    return attn  # [B,H,N,N]\n\ndef _cls_to_patch_map(attn_bhNN, head_idx=None):\n    \"\"\"[B,H,N,N] attention에서 CLS→Patch attention만 뽑아 1D로 반환합니다.\n    - CLS 토큰 index = 0\n    - Patch 토큰 index = 1..N\n    - head_idx=None이면 head 평균을 사용합니다.\n    반환: cls_attn_1d shape = [num_patches]\n    \"\"\"\n    # CLS(0) 가 각 토큰(j)에 주는 attention: attn[..., 0, j]\n    if head_idx is None:\n        # head 평균: [B,N] (여기서는 B=1 가정)\n        cls_attn = attn_bhNN.mean(dim=1)[0, 0, 1:]  # 평균 관심지도 [1, H, N+1, N+1] → [1, N+1, N+1] → [N]\n    else:\n        cls_attn = attn_bhNN[0, head_idx, 0, 1:]    # 특정헤드 관심지도 [N]\n    return cls_attn\n\ndef _to_2d_grid(cls_attn_1d, grid_h: int, grid_w: int):\n    \"\"\"[num_patches] 1D attention을 (grid_h, grid_w)로 reshape합니다.\"\"\"\n    attn_2d = cls_attn_1d.reshape(grid_h, grid_w)\n    # 0~1 범위로 정규화(시각화 안정성)\n    attn_2d = attn_2d - attn_2d.min()\n    attn_2d = attn_2d / (attn_2d.max() + 1e-8)\n    return attn_2d\n\ndef _upsample_to_image(attn_2d, out_h: int, out_w: int):\n    \"\"\"(grid_h,grid_w) attention을 (out_h,out_w)로 bilinear upsample합니다.\"\"\"\n        # attn_2d가 이미 Tensor일 수도 있으므로, 불필요한 tensor(...) 재구성을 피합니다.\n    if isinstance(attn_2d, torch.Tensor):\n        attn_t = attn_2d.detach().float().unsqueeze(0).unsqueeze(0)  # [1,1,gh,gw] -> gray image\n    else:\n        attn_t = torch.as_tensor(attn_2d, dtype=torch.float32).unsqueeze(0).unsqueeze(0)  # [1,1,gh,gw] -> gray image\n    attn_up = F.interpolate(attn_t, size=(out_h, out_w), mode=\"bilinear\", align_corners=False)[0,0] # [32, 32]\n    attn_up = attn_up - attn_up.min()\n    attn_up = attn_up / (attn_up.max() + 1e-8)\n    return attn_up.cpu().numpy()  # (out_h,out_w)\n\ndef _get_attention_maps_for_image(model, x1, vit_cfg, layer_idx=None, head_idx=None):\n    \"\"\"단일 이미지(x1)에 대해 attention map(overlay용)을 준비합니다.\n    - layer_idx=None 또는 layer_idx==0 이면 '맨 마지막 레이어'로 자동 설정합니다(해석 용이).\n    - head_idx=None 이면 'head 평균'을 반환.\n    - head_idx가 정수면 해당 head만 반환.\n    반환: (maps, used_layer_idx)\n      - maps: list of (title, attn_up_HxW)\n    \"\"\"\n    model.eval()\n    with torch.no_grad():\n        _ = model(x1)\n\n    # (중요) 기본값: 마지막 레이어를 사용\n    depth = int(vit_cfg.depth)\n    used_layer_idx = (depth - 1) if (layer_idx is None or layer_idx == 0) else int(layer_idx)\n\n    attn_bhNN = _extract_last_attn(model, used_layer_idx)  # [1,H,N,N]\n\n    # patch grid 크기 = (H/patch, W/patch)\n    ps = int(vit_cfg.patch_size) if not isinstance(vit_cfg.patch_size, tuple) else int(vit_cfg.patch_size[0])\n    # 입력 이미지 크기 (CIFAR-10: 32x32, MNIST: HxW 등)에서 patch grid 크기를 계산합니다.\n    if isinstance(vit_cfg.image_size, tuple):\n        img_h, img_w = int(vit_cfg.image_size[0]), int(vit_cfg.image_size[1])\n    else:\n        img_h, img_w = int(vit_cfg.image_size), int(vit_cfg.image_size)\n\n    grid_h, grid_w = img_h // ps, img_w // ps\n\n    maps = []\n    if head_idx is None:\n        # head 평균 1장\n        cls_1d = _cls_to_patch_map(attn_bhNN, head_idx=None)\n        attn_2d = _to_2d_grid(cls_1d, grid_h, grid_w)\n        maps.append((\"MEAN\", _upsample_to_image(attn_2d, out_h=img_h, out_w=img_w)))\n    else:\n        # 특정 head 1장\n        cls_1d = _cls_to_patch_map(attn_bhNN, head_idx=int(head_idx))\n        attn_2d = _to_2d_grid(cls_1d, grid_h, grid_w)\n        maps.append((f\"H{int(head_idx)}\", _upsample_to_image(attn_2d, out_h=img_h, out_w=img_w)))\n\n    return maps, used_layer_idx\n\ndef show_attention_overlays(\n    model,\n    test_set,\n    vit_cfg,\n    device,\n    n_samples: int = 6,\n    layer_idx=None,\n    head_idx=None,\n    show_all_heads: bool = True,\n    sample_mode: str = \"correct\",  # \"correct\" | \"incorrect\" | \"all\"\n    seed: int = 0,\n):\n    \"\"\"여러 이미지에 대해 ORG(원본) + attention overlay를 나란히 보여줍니다.\n\n    Args:\n        model: 학습된 ViT 모델\n        test_set: (image, label) 형태의 dataset\n        vit_cfg: ViTConfig (depth, heads, patch_size 등)\n        device: torch.device\n        n_samples: 출력할 샘플 개수\n        layer_idx: 보고 싶은 encoder 블록 인덱스\n            - None 또는 0이면 마지막 레이어(depth-1)로 자동 설정(권장)\n        head_idx: 보고 싶은 head 인덱스\n            - None이면 head 평균(MEAN)\n        show_all_heads: True면 마지막 레이어의 모든 head(H0..H{heads-1})도 함께 출력\n        sample_mode: \"correct\"/\"incorrect\"/\"all\" 로 샘플 필터링\n        seed: 랜덤 샘플링 재현성\n\n    출력:\n        각 샘플마다 [ORG] + [MEAN] + [H0..] 형태로 한 줄(row)로 표시합니다.\n    \"\"\"\n    random.seed(seed)\n\n    # 1) 샘플 후보 인덱스 준비\n    indices = list(range(len(test_set)))\n\n    # 2) 필요 시: 샘플 필터링 (correct/incorrect/all)\n    # sample_mode에 따라 후보를 거릅니다.\n    # - \"correct\"   : 정답(=맞춘) 샘플만\n    # - \"incorrect\" : 오답(=틀린) 샘플만\n    # - \"all\"       : 전체(필터 없음)\n    sample_mode = (sample_mode or \"all\").lower().strip()\n    if sample_mode not in {\"correct\", \"incorrect\", \"all\"}:\n        print(f\"[warn] sample_mode={sample_mode} 는 지원하지 않습니다. -> 'all'로 처리합니다.\")\n        sample_mode = \"all\"\n\n    if sample_mode in {\"correct\", \"incorrect\"}:\n        filtered = []\n        model.eval()\n        with torch.no_grad():\n            for idx in indices:\n                x, y = test_set[idx]\n                x1 = x.unsqueeze(0).to(device)  # (1, 3, 32, 32)\n                logits = model(x1)\n                pred = int(logits.argmax(dim=1).item())\n                is_correct = (pred == int(y))\n                if (sample_mode == \"correct\" and is_correct) or (sample_mode == \"incorrect\" and (not is_correct)):\n                    filtered.append(idx)\n\n        indices = filtered\n        if len(indices) == 0:\n            msg = \"정답\" if sample_mode == \"correct\" else \"오답\"\n            print(f\"[warn] 조건에 맞는 {msg} 샘플이 없습니다. (모델 학습 상태/seed/n_samples를 확인하세요.)\")\n            return\n\n    # 3) 샘플링\n    chosen = random.sample(indices, k=min(n_samples, len(indices))) if len(indices) > 0 else []\n\n    # 4) 시각화: 샘플마다 ORG + (MEAN + Heads) 를 한 row로 출력\n    heads = int(vit_cfg.heads)\n    for idx in chosen:\n        x, y = test_set[idx]\n        label = int(y)\n\n        x1 = x.unsqueeze(0).to(device)\n        model.eval()\n        with torch.no_grad():\n            logits = model(x1)\n            pred = int(logits.argmax(dim=1).item())\n\n        # (A) 평균 attention(MEAN)\n        mean_maps, used_layer = _get_attention_maps_for_image(\n            model=model, x1=x1, vit_cfg=vit_cfg,\n            layer_idx=layer_idx, head_idx=None\n        )\n\n        # (B) 각 head attention(H0..)\n        head_maps = []\n        if show_all_heads:\n            for h in range(heads):\n                hm, _ = _get_attention_maps_for_image(\n                    model=model, x1=x1, vit_cfg=vit_cfg,\n                    layer_idx=layer_idx, head_idx=h\n                )\n                head_maps.extend(hm)  # hm은 1개짜리 리스트\n\n        # 표시할 컬럼 구성: ORG + MEAN + heads...\n        maps_to_show = mean_maps + head_maps\n\n        ncols = 1 + len(maps_to_show)\n        fig, axes = plt.subplots(1, ncols, figsize=(3.2 * ncols, 3.2))\n\n        # ORG(원본)\n        ax0 = axes[0]\n        imshow_chw(ax0, x, title=None)\n        ax0.axis(\"off\")\n        ax0.set_title(f\"ORG | GT={class_names[int(label)]} / Pred={class_names[int(pred)]}\\n(layer={used_layer})\")\n\n        # Overlays\n        for j, (title, attn_up) in enumerate(maps_to_show, start=1):\n            ax = axes[j]\n            base = chw_to_hwc_np(x)  # (H,W,3), 0~1\n            ax.imshow(base)\n            ax.imshow(attn_up, alpha=0.45)  # heatmap overlay (컬러맵 기본값 사용)\n            ax.axis(\"off\")\n            ax.set_title(f\"{title}\")\n\n        plt.tight_layout()\n        plt.show()\n\n# 사용 예시:\n# - 기본값(layer_idx=None): 마지막 레이어의 MEAN + 모든 head를 출력\n# - sample_mode=\"incorrect\": 오답 샘플만 골라서 보면 해석이 더 쉬운 경우가 많습니다.\nshow_attention_overlays(\n    model=model,\n    test_set=test_set,\n    vit_cfg=vit_cfg,\n    device=device,\n    n_samples=6,\n    layer_idx=None,           # None 또는 0이면 마지막 레이어를 사용\n    head_idx=None,            # None이면 평균(MEAN)\n    show_all_heads=True,      # 모든 head도 함께 보기\n    sample_mode=\"correct\",\n    seed=0,\n)"}, {"number": 26, "code_number": null, "type": "markdown", "source": "---\n## 7) Confusion Matrix로 오분류 패턴 보기\n\n- **목적:** 단순 정확도(accuracy)만으로는 보이지 않는 **클래스별 오분류 경향**을 확인합니다.\n- **관찰 포인트:**\n  - 어떤 클래스 쌍(예: `cat↔dog`, `automobile↔truck`, `deer↔horse`)이 자주 헷갈리는지\n  - 특정 클래스에서만 오분류가 집중되는지(데이터/모델 편향 가능성)\n  - 이후 `오분류 샘플 시각화`와 함께 보면 “왜” 틀렸는지 해석이 쉬워집니다."}, {"number": 27, "code_number": 14, "type": "code", "source": "import seaborn as sns\nfrom sklearn.metrics import confusion_matrix\nimport numpy as np\n\ndef plot_confusion_matrix(model, data_loader):\n    model.eval()\n    all_preds = []\n    all_labels = []\n\n    with torch.no_grad():\n        for data, target in data_loader:\n            data = data.to(device)\n            target = target.to(device)\n            output = model(data)  # 입력 배치에 대한 모델 forward(로짓 출력)\n            _, preds = torch.max(output, dim=1)   # preds = maximum indices\n            all_preds.extend(preds.cpu().numpy())\n            all_labels.extend(target.cpu().numpy())\n\n    cm = confusion_matrix(all_labels, all_preds)\n    plt.figure(figsize=(10, 8))\n    sns.heatmap(cm, annot=True, fmt='d', cmap='Blues', xticklabels=class_names, yticklabels=class_names)\n    plt.xlabel('Predicted')\n    plt.xticks(rotation=45, ha='right')\n    plt.yticks(rotation=0)\n\n    plt.ylabel('Actual')\n    plt.title('Confusion Matrix')\n    plt.show()\n\nplot_confusion_matrix(model, test_loader)"}, {"number": 28, "code_number": null, "type": "markdown", "source": "---\n## 8) 오분류(실패) 샘플만 시각화하기\n\n- **목적:** 단순히 \"맞춘 샘플\"을 보는 것보다, **틀린 샘플(오분류)** 을 모아 보면 모델이 약한 패턴을 빠르게 파악할 수 있습니다.\n- **어떻게 해석할까?**\n  - 비슷한 클래스(예: `cat↔dog`, `automobile↔truck`)에서 헷갈리는지\n  - 배경(하늘/풀/도로 등)이나 객체 크기/각도/가림(occlusion)에 민감한지\n  - 데이터 증강(augmentation)이나 모델 크기/학습률 조절이 필요한지\n\n아래 함수는 **오분류된 샘플만** 골라서 그려줍니다."}, {"number": 29, "code_number": 15, "type": "code", "source": "# 오분류(실패) 샘플만 시각화\ndef plot_misclassified_samples(model, data_loader, max_samples: int = 10):\n    \"\"\"테스트 데이터에서 오분류된 샘플만 골라 시각화합니다.\n\n    Args:\n        model: 학습된 분류 모델\n        data_loader: (data, target)을 내는 DataLoader\n        max_samples: 최대 출력 샘플 수 (기본 10)\n    \"\"\"\n    model.eval()\n    mis_images, mis_labels, mis_preds = [], [], []\n\n    with torch.no_grad():\n        for data, target in data_loader:\n            data = data.to(device)\n            target = target.to(device)\n\n            logits = model(data)                 # [B, num_classes] 로짓\n            preds = logits.argmax(dim=1)         # 각 샘플의 예측 클래스\n\n            wrong_mask = preds.ne(target)        # 오분류 위치만 True\n            if wrong_mask.any():\n                # 오분류된 샘플만 추출\n                wrong_data = data[wrong_mask].detach().cpu()  # keep as Tensor for denormalize\n                wrong_target = target[wrong_mask].detach().cpu()\n                wrong_preds = preds[wrong_mask].detach().cpu()\n\n                for x, y, p in zip(wrong_data, wrong_target, wrong_preds):\n                    mis_images.append(x)\n                    mis_labels.append(int(y))\n                    mis_preds.append(int(p))\n                    if len(mis_images) >= max_samples:\n                        break\n\n            if len(mis_images) >= max_samples:\n                break\n\n    if len(mis_images) == 0:\n        print(\"오분류 샘플이 없습니다. (현재 설정/에폭에서 테스트를 모두 맞췄을 가능성이 있습니다)\")\n        return\n\n    # 시각화 그리드: 최대 10개를 2×5로 보기 좋게 배치\n    n = min(len(mis_images), max_samples)\n    rows = 2 if n > 5 else 1\n    cols = 5 if n > 5 else n\n\n    fig, axes = plt.subplots(rows, cols, figsize=(3*cols, 3.2*rows))\n    axes = np.array(axes).reshape(-1)  # axes를 1D로 평탄화\n\n    for i in range(rows * cols):\n        ax = axes[i]\n        if i < n:\n            img_chw = mis_images[i]  # CIFAR-10: (3,32,32)\n            img_vis = denormalize_cifar10(img_chw).detach().cpu().numpy().transpose(1, 2, 0)  # HWC\n            ax.imshow(img_vis)\n            ax.set_title(f\"GT: {class_names[mis_labels[i]]} / Pred: {class_names[mis_preds[i]]}\")\n        ax.axis(\"off\")\n\n    plt.tight_layout()\n    plt.show()\n\n\n# 실행: 오분류 샘플만 출력\nplot_misclassified_samples(model, test_loader, max_samples=10)"}, {"number": 30, "code_number": null, "type": "markdown", "source": "---\n## 9) (추가 실습) ONNX Export + Netron으로 구조 확인하기\n\n- **목적:** ViT를 ONNX로 내보내고, Netron에서 *토큰화(패치 임베딩) → Transformer 블록 → Head* 흐름을 그래프로 확인합니다.\n- **관찰 포인트:**\n  - `print(model)` / `torchinfo.summary(...)`에서 본 레이어가 Netron 그래프에서 어떻게 연결되는지\n  - 입력 텐서 크기(예: `[B, 3, 32, 32]`)가 중간에서 토큰 시퀀스(`[B, N, D]`)로 변환되는 지점이 어디인지"}, {"number": 31, "code_number": 16, "type": "code", "source": "# 필요 패키지 확인 (이미 ensure_package가 있다면 그걸 써도 됩니다)\n# - onnx / onnxruntime는 환경에 따라 없을 수 있으니, 없으면 설치하세요.\n# ensure_package(\"onnx\")\n# ensure_package(\"onnxruntime\")\n\nimport torch\n\nonnx_path = \"vit_cifar10.onnx\"\ndummy = torch.randn(1, 3, 32, 32, device=device)  # CIFAR-10 입력 형식 (RGB, 32x32)\nmodel.eval()\n\ntry:\n    torch.onnx.export(\n        model,\n        dummy,\n        onnx_path,\n        input_names=[\"input\"],\n        output_names=[\"logits\"],\n        opset_version=17,\n        do_constant_folding=True,\n        dynamic_axes={\"input\": {0: \"B\"}, \"logits\": {0: \"B\"}},  # 배치 크기만 가변 처리\n    )\n    print(\"Saved ONNX:\", onnx_path)\n    print(\"Netron에서 열어서 (patch embedding → transformer blocks → head) 흐름을 확인해보세요.\")\nexcept Exception as e:\n    print(\"ONNX export failed:\", e)\n    print(\"힌트: onnx 패키지가 없다면 `pip install onnx`가 필요할 수 있습니다.\")"}], "blanks": [{"id": "blank-vit-qkv", "label": "QKV 동시 투영", "answer": "nn.Linear(dim, inner_dim * 3, bias = False)"}, {"id": "blank-vit-scores", "label": "Head별 QK 유사도", "answer": "einsum('b h i d, b h j d -> b h i j', q, k) * self.scale"}, {"id": "blank-vit-weighted-sum", "label": "Attention Value 가중합", "answer": "einsum('b h i j, b h j d -> b h i d', attn, v)"}]}, "03_DETR.ipynb": {"page": "vision_detr_subjective_test", "cells": [{"number": 1, "code_number": null, "type": "markdown", "source": "# **DETR 실습 노트북 (PyTorch) — 추론 파이프라인부터 Attention 시각화까지**\n\n**DETR(DEtection TRansformer)** 기반 객체 탐지 실습을 목표로 합니다.  \n`Backbone(CNN) → Transformer(Encoder/Decoder) → Object Query 기반 예측` 흐름을 따라가며,\n**출력(`pred_logits`, `pred_boxes`)이 어떤 의미인지**와 **attention이 어디를 보고 있는지**를 직접 확인합니다.\n\n## 학습 목표\n- DETR의 **Backbone / Transformer(Encoder·Decoder) / Object Query** 흐름을 코드에서 찾을 수 있다.\n- 모델 출력인 `pred_logits`, `pred_boxes`의 **shape와 의미**를 설명할 수 있다.  \n  - `pred_logits`: (B, num_queries, num_classes + 1)에서 **+1이 no-object**임을 이해한다.\n  - `pred_boxes`: (B, num_queries, 4)가 **정규화 박스(`cx, cy, w, h`)**임을 이해한다.\n- 간단한 COCO 샘플 이미지로 **추론 → 신뢰도 필터링 → 박스 복원 → 시각화**까지 한 번에 실행할 수 있다.\n- Forward hook으로 캡처한 **Encoder self-attention / Decoder cross-attention**을 시각화하고 해석할 수 있다.\n\n---\n\n### 구성 개요\n\n1. 환경 설정(패키지 로드, 추론 모드 설정)\n2. COCO 클래스/전처리/후처리 준비\n3. DETR 사전학습 모델 로드 및 구조 확인\n4. 한 장 이미지 추론 + 결과 필터링 + 박스 스케일 복원\n5. 탐지 결과 시각화\n6. 내부 텐서 추출(forward hook) 및 특징맵/attention 확인\n7. Encoder self-attention 2D 복원 및 시각화\n8. Object Query 기반 Decoder cross-attention 인터랙티브 시각화"}, {"number": 2, "code_number": null, "type": "markdown", "source": "---\n## 0) Setup (환경 준비)\n\n- **목적:** 실습에 필요한 패키지를 로드하고, 추론/시각화에 필요한 기본 설정을 준비합니다.\n- **관찰 포인트:**\n  - 시각화용 패키지(`matplotlib`, `ipywidgets`)와 모델 실행용 패키지(`torch`, `torchvision`)가 함께 사용됨\n  - 노트북 출력 품질을 위해 `InlineBackend.figure_format='retina'`가 설정됨\n  - `torch.set_grad_enabled(False)`로 **추론 모드**에서 불필요한 gradient 계산을 꺼 속도/메모리를 절약함"}, {"number": 3, "code_number": 1, "type": "code", "source": "import importlib.util\nimport subprocess\nimport sys\n\ndef ensure_package(pkg_name: str, import_name=None):\n    \"\"\"\n    패키지 설치 여부를 확인하고, 설치되어 있지 않으면 설치합니다.\n    \"\"\"\n    name = import_name or pkg_name\n    # 패키지가 설치되어 있는지 확인\n    if importlib.util.find_spec(name) is None:\n        print(f\"[install] {pkg_name} 라이브러리를 설치 중입니다... (import name: {name})\")\n        try:\n            # -q 옵션을 추가하여 설치 과정을 간결하게 유지할 수 있습니다.\n            subprocess.check_call([sys.executable, \"-m\", \"pip\", \"install\", \"-q\", pkg_name])\n            print(f\"[success] {pkg_name} 설치 완료.\")\n        except subprocess.CalledProcessError as e:\n            print(f\"[error] {pkg_name} 설치 실패: {e}\")\n    else:\n        print(f\"[ok] {pkg_name} 이미 설치되어 있습니다.\")\n\n# 설치가 필요한 패키지 리스트 (패키지명, 임포트명)\n# 임포트명이 패키지명과 다른 경우 튜플로 지정합니다.\npackages = [\n    (\"requests\", \"requests\"),\n    (\"Pillow\", \"PIL\"),        # 설치는 Pillow, 임포트는 PIL\n    (\"ipywidgets\", \"ipywidgets\"),\n    (\"torchinfo\", \"torchinfo\")\n]\n\n# 루프를 돌며 확인 및 설치\nfor pkg, imp in packages:\n    ensure_package(pkg, imp)"}, {"number": 4, "code_number": 2, "type": "code", "source": "# [DETR 실습] 라이브러리/도구 임포트\n# - 논문 구현 흐름: (1) 이미지 전처리 → (2) DETR 추론 → (3) 로짓/박스 후처리 → (4) 시각화\n# - 여기서는 공식 DETR 데모(ResNet-50 backbone) 실행에 필요한 패키지를 불러옵니다.\n\nimport math  # 수학 유틸(예: 스케일링/좌표 변환)에 사용\n\nfrom PIL import Image  # PIL 이미지 로딩/처리\nimport requests  # COCO 샘플 이미지 다운로드\nimport matplotlib.pyplot as plt  # 결과(박스/어텐션) 시각화\n%config InlineBackend.figure_format = 'retina'  # 노트북에서 고해상도 렌더링\n\nimport ipywidgets as widgets  # 인터랙티브 위젯(UI) 생성\nfrom IPython.display import display, clear_output  # 위젯/출력 업데이트\n\nimport torch  # PyTorch 텐서/모델 실행\nfrom torch import nn  # (일부 셀에서) 모듈 타입 접근용\nfrom torchvision.models import resnet50  # (참고용) backbone 예시\nimport torchvision.transforms as T  # 입력 전처리(Resize/Normalize 등)\ntorch.set_grad_enabled(False);  # 추론/시각화 목적이므로 gradient 계산 비활성화(속도/메모리 절약)"}, {"number": 5, "code_number": null, "type": "markdown", "source": "---\n## 1) COCO 클래스 이름 테이블\n\n- **목적:** DETR(COCO pretrained)의 클래스 인덱스를 문자열로 매핑합니다.\n- **관찰 포인트**\n  - 모델의 `pred_logits`는 **클래스 인덱스**로 출력되므로, 이를 **라벨 문자열**로 바꾸기 위해 `CLASSES`가 필요함\n  - `N/A`는 COCO에서 사용하지 않는 슬롯이거나, 모델 구조상 남아있는 자리일 수 있음"}, {"number": 6, "code_number": 3, "type": "code", "source": "# [DETR] COCO 클래스 이름 테이블\n# - DETR-ResNet50 (COCO pretrained)의 클래스 인덱스를 사람이 읽을 수 있는 문자열로 매핑합니다.\n# - 마지막 'N/A'들은 COCO에서 사용하지 않는 슬롯을 의미합니다.\n\n# COCO classes\nCLASSES = [\n    'N/A', 'person', 'bicycle', 'car', 'motorcycle', 'airplane', 'bus',\n    'train', 'truck', 'boat', 'traffic light', 'fire hydrant', 'N/A',\n    'stop sign', 'parking meter', 'bench', 'bird', 'cat', 'dog', 'horse',\n    'sheep', 'cow', 'elephant', 'bear', 'zebra', 'giraffe', 'N/A', 'backpack',\n    'umbrella', 'N/A', 'N/A', 'handbag', 'tie', 'suitcase', 'frisbee', 'skis',\n    'snowboard', 'sports ball', 'kite', 'baseball bat', 'baseball glove',\n    'skateboard', 'surfboard', 'tennis racket', 'bottle', 'N/A', 'wine glass',\n    'cup', 'fork', 'knife', 'spoon', 'bowl', 'banana', 'apple', 'sandwich',\n    'orange', 'broccoli', 'carrot', 'hot dog', 'pizza', 'donut', 'cake',\n    'chair', 'couch', 'potted plant', 'bed', 'N/A', 'dining table', 'N/A',\n    'N/A', 'toilet', 'N/A', 'tv', 'laptop', 'mouse', 'remote', 'keyboard',\n    'cell phone', 'microwave', 'oven', 'toaster', 'sink', 'refrigerator', 'N/A',\n    'book', 'clock', 'vase', 'scissors', 'teddy bear', 'hair drier',\n    'toothbrush'\n]\n\nprint(f\"Total number of classes: {len(CLASSES)}\")\nvalid_classes = [c for c in CLASSES if c != 'N/A']\nprint(f\"Effective number of classes: {len(valid_classes)}\")\n\n\n# colors for visualization\nCOLORS = [[0.000, 0.447, 0.741], [0.850, 0.325, 0.098], [0.929, 0.694, 0.125],\n          [0.494, 0.184, 0.556], [0.466, 0.674, 0.188], [0.301, 0.745, 0.933]]"}, {"number": 7, "code_number": null, "type": "markdown", "source": "---\n## 2) 입력 전처리 및 박스 좌표 후처리\n\n- **목적:** DETR 입력 규격에 맞게 이미지를 전처리하고, 모델이 예측한 박스를 원본 이미지 좌표계로 되돌리는 함수를 준비합니다.\n- **관찰 포인트**\n  - `T.Compose([...])`는 **Resize/Normalize** 등 입력 변환을 순차적으로 적용함\n  - DETR의 박스는 보통 정규화 좌표(`cx, cy, w, h`) 형태이므로, 시각화를 위해 **픽셀 좌표**로 스케일 복원이 필요함\n  - 후처리 함수는 (정규화 → 픽셀) 변환과 `(cx,cy,w,h) → (x1,y1,x2,y2)` 변환 역할을 담당함"}, {"number": 8, "code_number": 4, "type": "code", "source": "# [DETR] 입력 전처리 + 박스 좌표 후처리 함수\n# - DETR은 COCO로 학습된 ResNet backbone을 사용하므로, ImageNet mean/std 정규화를 맞춰야 합니다.\n# - 모델 출력 박스는 (cx, cy, w, h) 형식이며 0~1로 정규화되어 있으므로, (xmin, ymin, xmax, ymax)로 변환 후 원본 이미지 크기로 rescale 합니다.\n\n# standard PyTorch mean-std input image normalization\ntransform = T.Compose([  # 전처리 파이프라인 정의(논문/코드: data augmentation과 정규화)\n    [[BLANK:blank-detr-resize]],  # 짧은 변을 800에 맞추는 리사이즈(공식 데모 설정)\n    T.ToTensor(),  # PIL → torch.Tensor (C,H,W), 0~1 스케일\n    [[BLANK:blank-detr-normalize]]  # ImageNet mean/std로 정규화(backbone 사전학습 분포와 정합)\n])\n\n# for output bounding box post-processing\ndef box_cxcywh_to_xyxy(x):  # (cx,cy,w,h) → (xmin,ymin,xmax,ymax) 변환\n    x_c, y_c, w, h = x.unbind(1)  # 각 성분을 분리: x.shape=(N,4) 가정\n    b = [(x_c - 0.5 * w), (y_c - 0.5 * h),  # 중심좌표에서 좌상/우하 꼭짓점 계산\n         (x_c + 0.5 * w), (y_c + 0.5 * h)]\n    return torch.stack(b, dim=1)  # (N,4)로 다시 스택\n\ndef rescale_bboxes(out_bbox, size):  # 정규화 박스를 픽셀 좌표로 변환\n    img_w, img_h = size  # PIL size = (W,H)\n    b = box_cxcywh_to_xyxy(out_bbox)  # 우선 xyxy로 변환\n    b = b * torch.tensor([img_w, img_h, img_w, img_h], dtype=torch.float32)  # (W,H,W,H) 스케일로 곱해 픽셀 단위로 확장\n    return b"}, {"number": 9, "code_number": null, "type": "markdown", "source": "---\n## 3) 탐지 결과 시각화 함수\n\n- **목적:** 모델의 예측 결과(클래스 확률, 박스)를 이미지 위에 그려서 직관적으로 확인합니다.\n- **관찰 포인트**\n  - 입력으로 `prob`(클래스 확률)과 `boxes`(좌표)를 받아, **선택된 예측만** 표시하도록 설계됨\n  - box를 그릴 때는 (x1,y1,x2,y2) 형태가 가장 다루기 쉬움\n  - 시각화 품질이 실습 이해도에 크게 영향을 주므로, 폰트/두께/라벨 표시 방식에 주목"}, {"number": 10, "code_number": 5, "type": "code", "source": "# [DETR] 결과 시각화 함수\n# - DETR은 '고정 개수의 object queries'에 대해 클래스 확률과 박스를 예측합니다.\n# - 아래 함수는 confidence가 높은 예측들만 골라, 원본 이미지 위에 박스와 클래스/점수를 그립니다.\n\ndef plot_results(pil_img, prob, boxes):  # (입력 이미지, 클래스 확률, 박스 좌표)를 받아 시각화\n    plt.figure(figsize=(16,10))  # 출력 크기 설정\n    plt.imshow(pil_img)  # 이미지 표시\n    ax = plt.gca()  # 현재 축(Axes) 얻기\n    colors = COLORS * 100  # 박스 색상 반복 사용\n    for p, (xmin, ymin, xmax, ymax), c in zip(prob, boxes.tolist(), colors):  # 각 예측(확률 p, 박스, 색상)에 대해 반복\n        ax.add_patch(plt.Rectangle((xmin, ymin), xmax - xmin, ymax - ymin,  # (xmin,ymin,w,h) 사각형 패치 추가\n                                   fill=False, color=c, linewidth=3))\n        cl = p.argmax()  # 가장 높은 확률의 클래스 인덱스\n        text = f'{CLASSES[cl]}: {p[cl]:0.2f}'  # 라벨 텍스트 구성: 'class: score'\n        ax.text(xmin, ymin, text, fontsize=15,  # 박스 좌상단에 라벨 텍스트 표시\n                bbox=dict(facecolor='yellow', alpha=0.5))\n    plt.axis('off')  # 축 표시 제거\n    plt.show()  # 그림 렌더링"}, {"number": 11, "code_number": null, "type": "markdown", "source": "---\n## 4) 사전학습 DETR 모델 로드\n\n- **목적:** `torch.hub`를 이용해 COCO로 사전학습된 DETR-ResNet50 모델을 로드합니다.\n- **관찰 포인트**\n  - `pretrained=True`로 로드된 모델은 **바로 추론**이 가능함\n  - DETR은 **NMS 없이** set prediction 형태로 결과를 내도록 학습된 모델임\n  - 이후 셀에서 출력되는 `pred_logits`, `pred_boxes`의 shape가 `num_queries`에 의해 결정됨"}, {"number": 12, "code_number": 6, "type": "code", "source": "# [DETR] 사전학습 모델 로드\n# - 공식 구현(facebookresearch/detr)의 pretrained 모델을 torch.hub로 로드합니다.\n# - backbone=ResNet-50, transformer encoder-decoder + object queries 구조.\n\nmodel = [[BLANK:blank-detr-model]]  # 가중치 포함 pretrained DETR-ResNet50 로드\nmodel.eval();  # Dropout/BN 등을 추론 모드로 전환"}, {"number": 13, "code_number": null, "type": "markdown", "source": "---\n## 5) 모델 구조 출력\n\n- **목적:** DETR 모델의 큰 구성(backbone, transformer, prediction head)을 출력하여 구조를 한 번 확인합니다.\n- **관찰 포인트**\n  - backbone(CNN)이 spatial feature map을 만들고, transformer가 query 기반으로 객체 표현을 형성함\n  - 마지막 head가 `class logits`와 `box`를 각각 예측함\n  - 출력 구조를 보면 **어텐션을 어디서 뽑을지(hook 지점)**를 설계할 수 있음"}, {"number": 14, "code_number": 7, "type": "code", "source": "\n# ============================================================\n# DETR 전체 모델 구조 출력 (모델 로드 직후)\n# ------------------------------------------------------------\n# torch.hub.load 로 불러온 detr_resnet50 모델의\n# PyTorch Module 계층 구조를 출력합니다.\n#\n# 논문 관점 대응:\n# - backbone  → CNN feature extractor (ResNet-50)\n# - transformer → Encoder / Decoder (object queries)\n# - class_embed / bbox_embed → prediction heads\n# ============================================================\n\nprint(\"=== DETR Model Architecture ===\")\nprint(model)"}, {"number": 15, "code_number": null, "type": "markdown", "source": "---\n## 6) COCO 샘플 이미지 준비\n\n- **목적:** 실습에 사용할 입력 이미지를 다운로드/로딩하여 PIL 이미지로 준비합니다.\n- **관찰 포인트**\n  - URL에서 이미지를 받아오는 과정(네트워크/권한/방화벽)에서 실패할 수 있음\n  - 이후 전처리(transform) 단계에서 PIL → tensor로 변환되며 shape가 `(C,H,W)`로 바뀜"}, {"number": 16, "code_number": 8, "type": "code", "source": "# [DETR] COCO 샘플 이미지 준비\n# - 논문/데모에서 자주 쓰는 COCO val 이미지(고양이/강아지 예시)를 다운로드합니다.\n\nurl = 'http://images.cocodataset.org/val2017/000000039769.jpg'  # COCO val 이미지 URL\nim = Image.open(requests.get(url, stream=True).raw)  # 다운로드한 바이트 스트림을 PIL 이미지로 디코딩"}, {"number": 17, "code_number": null, "type": "markdown", "source": "---\n## 7) 한 장 이미지 추론 및 필터링\n\n- **목적:** 이미지 1장을 DETR에 입력하여 `pred_logits`, `pred_boxes`를 얻고, 신뢰도 기준으로 예측을 선택합니다.\n- **관찰 포인트**\n  - `outputs`는 dict 형태이며 대표 키는 `pred_logits`, `pred_boxes`\n  - `pred_logits` shape: `(B, num_queries, num_classes+1)` (**+1은 no-object**)\n  - `pred_boxes` shape: `(B, num_queries, 4)` (정규화된 박스)\n  - `keep`(마스크/인덱스)로 threshold 이상 예측만 남기면, 시각화가 훨씬 명확해짐"}, {"number": 18, "code_number": 9, "type": "code", "source": "# [DETR] 한 장 이미지 추론 + 신뢰도 필터링 + 박스 스케일 복원\n# - outputs['pred_logits']: (1, num_queries, num_classes+1) 로짓. 마지막 '+1'은 'no-object' 클래스.\n# - outputs['pred_boxes']: (1, num_queries, 4) 박스 (cx,cy,w,h) 정규화 좌표.\n# - 논문처럼 모든 쿼리가 박스를 내지만, 여기서는 시각화를 위해 높은 confidence만 남깁니다.\n\n# mean-std normalize the input image (batch-size: 1)\nimg = transform(im).unsqueeze(0)  # 전처리 후 배치 차원 추가: (1,3,H,W)\n\nfrom torchinfo import summary\nmodel_summary = summary(model,\n        input_data=img,\n        col_names=[\"input_size\", \"output_size\", \"num_params\"],\n        depth=3)\nprint(model_summary)\n\n# propagate through the model\noutputs = model(img)  # DETR forward → 로짓/박스 예측\n\n# print keys of outputs\nfor key in outputs.keys():\n    # value를 가져와서 shape 출력\n    shape = outputs[key].shape\n    print(f\"Key : {key}\")\n    print(f\"Shape   : {shape}\")\n    print(\"-\" * 30)  # 구분선 추가\n\n# keep only predictions with 0.9+ confidence\nprobas = outputs['pred_logits'].softmax(-1)[0, :, :-1]  # 쿼리별 클래스 확률 계산(no-object 제외): shape=(num_queries,num_classes)\nkeep = probas.max(-1).values > 0.9  # 각 쿼리의 max class score가 임계값을 넘는 것만 선택\nprint(f\"keep Shape : {keep.shape}\")\n\n\n# convert boxes from [0; 1] to image scales\nbboxes_scaled = rescale_bboxes(outputs['pred_boxes'][0, keep], im.size)  # 선택된 박스를 픽셀 좌표로 변환\nprint(f\"bboxes_scaled Shape : {bboxes_scaled.shape}\")"}, {"number": 19, "code_number": null, "type": "markdown", "source": "---\n## 8) 탐지 결과 그리기\n\n- **목적:** 필터링된 예측만을 이용해 bounding box와 class label을 이미지 위에 표시합니다.\n- **관찰 포인트**\n  - `plot_results(im, probas[keep], bboxes_scaled)`처럼 **필터링 결과만 전달**하는 흐름이 중요함\n  - threshold를 조절하면 박스 개수/정확도가 달라지므로 실습 중 직접 바꿔보면 좋음"}, {"number": 20, "code_number": 10, "type": "code", "source": "# [DETR] 탐지 결과 그리기\n# - keep 마스크로 선택된 쿼리들의 (클래스 확률, 박스)를 plot_results로 시각화합니다.\n\nplot_results(im, probas[keep], bboxes_scaled)  # 선택된 예측만 표시"}, {"number": 21, "code_number": null, "type": "markdown", "source": "---\n## 9) 내부 텐서 추출을 위한 forward hook\n\n- **목적:** DETR의 중간 특징맵(conv features)과 encoder/decoder attention weight를 캡처하기 위해 hook을 등록합니다.\n- **관찰 포인트**\n  - `register_forward_hook`은 **forward 중간 출력**을 저장하는 전형적인 디버깅/분석 기법\n  - 어떤 레이어에 hook을 걸었는지에 따라 얻는 텐서의 의미가 크게 달라짐\n  - 이후 attention 시각화는 이 셀에서 저장한 텐서(conv/enc_attn/dec_attn)에 의존함"}, {"number": 22, "code_number": 11, "type": "code", "source": "# [DETR] 내부 텐서(특징맵/어텐션) 추출을 위한 forward hook\n# - 논문 분석 포인트:\n#   1) backbone CNN 특징맵(conv_features)\n#   2) 마지막 encoder layer의 self-attention(enc_attn_weights)\n#   3) 마지막 decoder layer의 cross-attention(dec_attn_weights)\n# - PyTorch hook을 등록해 forward 중간 결과를 캡처합니다.\n\n# use lists to store the outputs via up-values\nconv_features, enc_attn_weights, dec_attn_weights = [], [], []  # hook 결과를 담을 리스트(클로저 변수) 초기화\n\nhooks = [  # 각 모듈에 forward hook 등록\n    model.backbone[-2].register_forward_hook(  # CNN backbone의 특정 stage 출력(feature map) 저장\n        lambda self, input, output: conv_features.append(output) # lambda 인자들 : 실행문\n    ),\n    model.transformer.encoder.layers[-1].self_attn.register_forward_hook(  # encoder self-attention의 attention weights(output[1]) 저장\n        lambda self, input, output: enc_attn_weights.append(output[1])   # self-attention weight -> output[1]\n    ),\n    model.transformer.decoder.layers[-1].multihead_attn.register_forward_hook(  # decoder cross-attention의 attention weights(output[1]) 저장\n        lambda self, input, output: dec_attn_weights.append(output[1])   # cross-attention weight -> output[1]\n    ),\n]\n\n# propagate through the model\noutputs = model(img)  # hook이 등록된 상태로 forward 실행\n\nfor hook in hooks:  # 등록했던 hook 해제(메모리/부작용 방지)\n    hook.remove()\n\n# don't need the list anymore\nconv_features = conv_features[0]  # hook 결과를 담을 리스트(클로저 변수) 초기화\nprint(f\"features shape: {conv_features['0'].tensors.shape}\")   # [1, channels, H, W]\nenc_attn_weights = enc_attn_weights[0]  # encoder attention weights 텐서\nprint(f\"enc_attn_weights shape: {enc_attn_weights.shape}\")   # [1, HW, HW] -> 각 feature간 attention\ndec_attn_weights = dec_attn_weights[0]  # decoder attention weights 텐서\nprint(f\"dec_attn_weights shape: {dec_attn_weights.shape}\")   # [1, N, HW] -> 각 객체와 feature간 attention"}, {"number": 23, "code_number": null, "type": "markdown", "source": "---\n## 10) Decoder cross-attention 시각화\n\n- **목적:** Decoder의 cross attention map을 시각화 합니다.\n- **관찰 포인트**\n  - 입력 이미지 해상도에 비해 feature map은 다운샘플링되어 `(h, w)`가 작아짐\n  - 이 `(h, w)`는 attention을 2D로 복원할 때 기준이 되며, 스케일 팩터(factor)와도 연결됨\n  - decoder attention 맵 확인 : 물체 외곽이 부각됨"}, {"number": 24, "code_number": 12, "type": "code", "source": "# [DETR] 특징맵 shape 확인\n# - backbone 출력 feature map의 공간 해상도(H,W)는 입력 대비 downsample된 크기입니다.\n# - DETR은 이 feature map을 1D sequence로 펼쳐 transformer encoder에 넣습니다.\n\n# get the feature map shape\nh, w = conv_features['0'].tensors.shape[-2:]    # NestedTensor 딕셔너리에서 마지막 stage feature 선택('0' 키)\n\nfig, axs = plt.subplots(ncols=len(bboxes_scaled), nrows=2, figsize=(22, 7))\ncolors = COLORS * 100\nfor idx, ax_i, (xmin, ymin, xmax, ymax) in zip(keep.nonzero(), axs.T, bboxes_scaled):\n    ax = ax_i[0]\n    ax.imshow(dec_attn_weights[0, idx].view(h, w))  # keep에 해당하는 객체(idx)의 cross attention map\n    ax.axis('off')\n    ax.set_title(f'query id: {idx.item()}')\n    ax = ax_i[1]\n    ax.imshow(im)\n    ax.add_patch(plt.Rectangle((xmin, ymin), xmax - xmin, ymax - ymin,\n                               fill=False, color='blue', linewidth=3))\n    ax.axis('off')\n    ax.set_title(CLASSES[probas[idx].argmax()])\nfig.tight_layout()"}, {"number": 25, "code_number": null, "type": "markdown", "source": "---\n## 11) Encoder self-attention과 feature map 확인\n\n- **목적:** encoder self-attention이 어떤 형태로 저장되어 있는지(차원/레이어) 확인하고, backbone feature와의 대응을 이해합니다.\n- **관찰 포인트**\n  - self-attention은 토큰 간 관계를 나타내므로 보통 `(num_heads, HW, HW)` 또는 유사 형태로 나타남\n  - head별로 주목하는 영역이 달라서, 특정 head만 시각화해도 해석이 달라질 수 있음"}, {"number": 26, "code_number": 13, "type": "code", "source": "# [DETR] Encoder self-attention과 backbone feature map 확인\n# - enc_attn_weights[0]: (num_heads, HW, HW) 형태(구현에 따라 batch/head 차원 위치가 다를 수 있음).\n# - f_map.tensors: backbone 출력(마스크 포함 NestedTensor)에서 실제 feature tensor.\n\n# output of the CNN\nf_map = conv_features['0']  # NestedTensor 딕셔너리에서 마지막 stage feature 선택('0' 키)\nprint(\"Feature map:            \", f_map.tensors.shape)  # feature map tensor shape 출력: (B,C,H,W)\nprint(\"Encoder attention:      \", enc_attn_weights[0].shape)  # encoder 마지막 layer self-attention weight shape 출력"}, {"number": 27, "code_number": null, "type": "markdown", "source": "---\n## 12) Encoder self-attention을 2D 공간으로 복원\n\n- **목적:** 토큰 기반 attention을 `(h, w)` 격자 형태의 2D heatmap으로 변환하는 준비를 합니다.\n- **관찰 포인트**\n  - attention의 축(쿼리/키 방향)을 어떤 기준으로 잡는지에 따라 heatmap 의미가 달라짐\n  - `reshape`/`view`를 할 때 `(h*w)`가 정확히 맞아야 함 (shape mismatch가 흔한 실수 포인트)"}, {"number": 28, "code_number": 14, "type": "code", "source": "# [DETR] Encoder self-attention을 2D 공간으로 복원\n# - Transformer는 (H*W) 토큰으로 flatten된 sequence를 보지만,\n#   시각화를 위해 attention을 (H,W,H,W) 형태로 reshape합니다.\n\n# get the HxW shape of the feature maps of the CNN\nshape = f_map.tensors.shape[-2:]  # feature map의 (H,W) 추출\n# and reshape the self-attention to a more interpretable shape\nsattn = enc_attn_weights[0].reshape(shape + shape)  # (HW,HW) → (H,W,H,W)로 재배열하여 픽셀 위치 간 attention으로 해석\nprint(\"Reshaped self-attention:\", sattn.shape)  # reshape 결과 shape 확인 -> [H_key, W_key, H_query, W_query] 의미로 해석"}, {"number": 29, "code_number": null, "type": "markdown", "source": "---\n## 13) Encoder self-attention 시각화\n\n- **목적:** 특정 기준점(픽셀/격자 위치)에서 encoder가 이미지의 어느 위치를 참고하는지 시각화합니다.\n- **관찰 포인트**\n  - `fact`(downsample factor)를 이용해 원본 좌표 ↔ feature grid 좌표를 매핑함\n  - 클릭/지정한 위치에 따라 attention 분포가 달라지며, 전역/국소 문맥을 어떻게 쓰는지 관찰 가능\n  - head를 바꿔가며 보면 “관계(relationship)”를 보는 head가 따로 존재함을 느낄 수 있음"}, {"number": 30, "code_number": 15, "type": "code", "source": "# [DETR] Encoder self-attention 시각화(특정 기준점에서 어디를 보는지)\n# - 논문 관점: encoder는 이미지의 모든 위치(HW 토큰) 사이의 self-attention으로 전역 문맥을 섞습니다.\n# - 아래는 몇 개의 기준 (y,x) 위치를 고르고, 그 토큰이 다른 위치들을 얼마나 참고하는지 heatmap으로 표시합니다.\n\n# downsampling factor for the CNN, is 32 for DETR and 16 for DETR DC5\nfact = 32  # backbone downsample factor(ResNet50 C5 기준 32)\n\n# let's select 4 reference points for visualization\nidxs = [(200, 200), (280, 400), (200, 600), (440, 800),]  # 원본 이미지 좌표계에서 기준점들 선택(y,x)\n\n# here we create the canvas\nfig = plt.figure(constrained_layout=True, figsize=(25 * 0.7, 8.5 * 0.7))  # 멀티 패널 캔버스 생성\n# and we add one plot per reference point\ngs = fig.add_gridspec(2, 4)  # 그리드 레이아웃 정의(2x4)\naxs = [  # 각 기준점에 대응하는 subplot 생성\n    fig.add_subplot(gs[0, 0]),\n    fig.add_subplot(gs[1, 0]),\n    fig.add_subplot(gs[0, -1]),\n    fig.add_subplot(gs[1, -1]),\n]\n\n# for each one of the reference points, let's plot the self-attention\n# for that point\nfor idx_o, ax in zip(idxs, axs):  # 각 기준점에 대해: feature-map 좌표로 변환 후 attention slice 시각화\n    idx = (idx_o[0] // fact, idx_o[1] // fact)  # (y,x) 픽셀 → (y//fact, x//fact) 토큰 좌표\n    ax.imshow(sattn[..., idx[0], idx[1]], cmap='cividis', interpolation='nearest')  # 해당 토큰의 attention 분포를 2D heatmap으로 표시\n    ax.axis('off')\n    ax.set_title(f'self-attention{idx_o}')\n\n# and now let's add the central image, with the reference points as red circles\nfcenter_ax = fig.add_subplot(gs[:, 1:-1])  # 가운데에 원본 이미지를 크게 표시\nfcenter_ax.imshow(im)\nfor (y, x) in idxs:\n    scale = im.height / img.shape[-2]\n    x = ((x // fact) + 0.5) * fact\n    y = ((y // fact) + 0.5) * fact\n    fcenter_ax.add_patch(plt.Circle((x * scale, y * scale), fact // 2, color='r'))  # 선택한 기준점 위치를 빨간 원으로 표시\n    fcenter_ax.axis('off')"}, {"number": 31, "code_number": null, "type": "markdown", "source": "---\n## 14) AttentionVisualizer (Encoder Self-attention) 클래스\n\n- **목적:** 이미지 내 특정 지점(Reference Point)을 기준으로 **Encoder가 전역적인 문맥(Global Context)을 어떻게 수집하는지** 확인하는 인터랙티브 도구를 정의합니다.\n- **관찰 포인트**\n  - **Encoder Self-attention의 역할:** 이미지의 한 픽셀(Query)이 '자기 자신을 정의하기 위해' 이미지 전체의 다른 픽셀들(Keys)을 얼마나 참고하는지 나타냄\n  - **인터랙티브 시각화:** 슬라이더를 통해 이미지 위의 **빨간 점(Query 좌표)**을 옮기면, 해당 위치의 토큰이 바라보는 **Attention Map**이 실시간으로 업데이트됨.\n  - **객체 실루엣 인지:** 빨간 점을 객체(예: 고양이) 위에 두면 어텐션이 해당 객체의 전체적인 형태를 따라 활성화되는 것을 볼 수 있음. 이는 인코더가 이미 객체와 배경을 분리하여 인지하고 있음을 보여줌.\n  - **전역 수용장(Global Receptive Field):** CNN과 달리 멀리 떨어진 픽셀 간에도 강한 상관관계를 맺는 것을 확인하며, Transformer가 어떻게 이미지 전체의 문맥을 한 번에 섞는지 이해하는 것이 핵심 목표임."}, {"number": 32, "code_number": 16, "type": "code", "source": "# [DETR] AttentionVisualizer 클래스\n# - 목적: 이미지 내 특정 지점(Pixel)을 기준으로 Encoder의 'Self-attention'이 어떻게 전역 문맥을 섞는지 직관적으로 이해.\n# - 주요 아이디어(논문 구현 관점):\n#   1) Backbone -> Transformer Encoder: 이미지를 토큰화하고 각 토큰(픽셀)들 간의 관계를 Self-attention으로 연산.\n#   2) 사용자가 슬라이더로 이미지 상의 특정 좌표(Query 지점)를 선택.\n#   3) 해당 좌표의 토큰이 이미지 전체(Key 지점들)를 얼마나 참고하는지 마지막 Encoder 레이어에서 추출하여 시각화.\n# - 아래 코드는 forward hook으로 Encoder 내부 텐서를 캡처하고, 인터랙티브 위젯을 통해 특정 위치의 어텐션 맵을 그립니다.\n\nclass AttentionVisualizer:  # DETR 내부 어텐션을 시각화하는 유틸 클래스 정의\n    def __init__(self, model, transform):  # 모델/전처리/위젯 초기화\n        self.model = model  # 외부에서 로드한 DETR 모델 보관\n        self.transform = transform  # 입력 이미지 전처리 함수 보관\n\n        self.url = \"\"\n        self.cur_url = None\n        self.pil_img = None\n        self.tensor_img = None\n\n        self.conv_features = None\n        self.enc_attn_weights = None\n        self.dec_attn_weights = None\n\n        self.setup_widgets()\n\n    def setup_widgets(self):\n        self.sliders = [\n            widgets.Text(\n                value='http://images.cocodataset.org/val2017/000000039769.jpg',\n                placeholder='Type something',\n                description='URL (ENTER):',\n                disabled=False,\n                continuous_update=False,\n                layout=widgets.Layout(width='100%')\n            ),\n            widgets.FloatSlider(min=0, max=0.99,\n                        step=0.02, description='X coordinate', value=0.72,\n                        continuous_update=False,\n                        layout=widgets.Layout(width='50%')\n                        ),\n            widgets.FloatSlider(min=0, max=0.99,\n                        step=0.02, description='Y coordinate', value=0.40,\n                        continuous_update=False,\n                        layout=widgets.Layout(width='50%')),\n            widgets.Checkbox(\n              value=False,\n              description='Direction of self attention',\n              disabled=False,\n              indent=False,\n              layout=widgets.Layout(width='50%'),\n          ),\n            widgets.Checkbox(\n              value=True,\n              description='Show red dot in attention',\n              disabled=False,\n              indent=False,\n              layout=widgets.Layout(width='50%'),\n          )\n        ]\n        self.o = widgets.Output()  # matplotlib 출력이 들어갈 영역\n\n    def compute_features(self, img):  # hook을 등록해 feature/attention을 수집한 뒤 forward 수행\n        model = self.model  # 외부에서 로드한 DETR 모델 보관\n        # use lists to store the outputs via up-values\n        conv_features, enc_attn_weights, dec_attn_weights = [], [], []\n\n        hooks = [\n            model.backbone[-2].register_forward_hook(  # backbone feature map 캡처\n                lambda self, input, output: conv_features.append(output)\n            ),\n            model.transformer.encoder.layers[-1].self_attn.register_forward_hook(  # encoder 마지막 layer self-attention 캡처\n                lambda self, input, output: enc_attn_weights.append(output[1])\n            ),\n            model.transformer.decoder.layers[-1].multihead_attn.register_forward_hook(  # decoder 마지막 layer cross-attention 캡처\n                lambda self, input, output: dec_attn_weights.append(output[1])\n            ),\n        ]\n        # propagate through the model\n        outputs = model(img)  # hook이 걸린 상태로 DETR 추론 실행\n\n        for hook in hooks:  # hook 해제(중복 캡처 방지)\n            hook.remove()\n\n        # don't need the list anymore\n        self.conv_features = conv_features[0]  # 리스트에서 실제 텐서/NestedTensor 꺼내기\n        self.dec_attn_weights = dec_attn_weights[0]\n        # get the HxW shape of the feature maps of the CNN\n        shape = self.conv_features['0'].tensors.shape[-2:]\n        # and reshape the self-attention to a more interpretable shape\n        self.enc_attn_weights = enc_attn_weights[0].reshape(shape + shape)\n\n    def compute_on_image(self, url):\n        if url != self.url:\n            self.url = url\n            self.pil_img = Image.open(requests.get(url, stream=True).raw)\n            # mean-std normalize the input image (batch-size: 1)\n            self.tensor_img = self.transform(self.pil_img).unsqueeze(0)  # 입력 이미지 전처리 함수 보관\n            self.compute_features(self.tensor_img)\n\n    def update_chart(self, change):\n        with self.o:  # Output 영역에 그림을 그리기 위해 컨텍스트 진입\n            clear_output()  # 이전 출력 삭제 후 새 결과 렌더링\n\n            # j and i are the x and y coordinates of where to look at\n            # sattn_dir is which direction to consider in the self-attention matrix\n            # sattn_dot displays a red dot or not in the self-attention map\n            url, j, i, sattn_dir, sattn_dot = [s.value for s in self.sliders]\n\n            fig, axs = plt.subplots(ncols=2, nrows=1, figsize=(9, 4))\n            self.compute_on_image(url)\n\n            # convert reference point to absolute coordinates\n            j = int(j * self.tensor_img.shape[-1])\n            i = int(i * self.tensor_img.shape[-2])\n\n            # how much was the original image upsampled before feeding it to the model\n            scale = self.pil_img.height / self.tensor_img.shape[-2]\n\n            # compute the downsampling factor for the model\n            # it should be 32 for standard DETR and 16 for DC5\n            sattn = self.enc_attn_weights\n            fact = 2 ** round(math.log2(self.tensor_img.shape[-1] / sattn.shape[-1]))\n\n            # round the position at the downsampling factor\n            x = ((j // fact) + 0.5) * fact\n            y = ((i // fact) + 0.5) * fact\n\n            axs[0].imshow(self.pil_img)\n            axs[0].axis('off')\n            axs[0].add_patch(plt.Circle((x * scale, y * scale), fact // 2, color='r'))\n\n            idx = (i // fact, j // fact)\n\n            if sattn_dir:\n                sattn_map = sattn[idx[0], idx[1], ...]\n            else:\n                sattn_map = sattn[..., idx[0], idx[1]]\n\n            axs[1].imshow(sattn_map, cmap='cividis', interpolation='nearest')  # attention heatmap 출력\n            if sattn_dot:\n                axs[1].add_patch(plt.Circle((idx[1],idx[0]), 1, color='r'))\n            axs[1].axis('off')\n            axs[1].set_title(f'self-attention{(i, j)}')\n\n            plt.show()\n\n    def run(self):\n      for s in self.sliders:\n          s.observe(self.update_chart, 'value')\n      self.update_chart(None)\n      url, x, y, d, sattn_d = self.sliders\n      res = widgets.VBox(\n      [\n          url,\n          widgets.HBox([x, y]),\n          widgets.HBox([d, sattn_d]),\n          self.o\n      ])\n      return res"}, {"number": 33, "code_number": null, "type": "markdown", "source": "---\n## 15) Encoder Self-attention 인터랙티브 시각화 실행\n\n- **목적:** 위에서 정의한 `AttentionVisualizer`를 실행하여, 이미지 내 특정 지점(Reference Point)을 기준으로 인코더가 수집한 전역 문맥(Self-attention)을 확인합니다.\n- **관찰 포인트**\n  - **좌표 이동(X, Y Slider):** 슬라이더를 움직여 빨간 점의 위치를 바꾸면, 해당 픽셀이 이미지의 어떤 영역들과 '관계'를 맺고 있는지 히트맵이 실시간으로 변합니다.\n  - **전역 문맥 확인:** 특정 객체(예: 고양이)의 일부분만 찍어도 인코더가 해당 객체의 전체 형태를 파악하고 있는지(히트맵이 객체 실루엣을 따라 활성화되는지) 확인해 보세요.\n  - **Attention 방향성(Checkbox):** 'Direction of self attention' 옵션을 통해 '내가 남을 보는 경우'와 '남이 나를 보는 경우'의 차이를 관찰할 수 있습니다."}, {"number": 34, "code_number": 17, "type": "code", "source": "# [DETR] Encoder self-attention을 이용한 인터랙티브 시각화 도구 실행\n# - AttentionVisualizer는 사용자가 선택한 (1) 이미지 상의 특정 좌표(Red Dot)와 (2) 해당 위치 토큰의 encoder self-attention 맵을 보여줍니다.\n# - 논문 관점: 인코더는 이미지 전체를 훑으며 각 픽셀 토큰이 서로의 정보를 참조하게 만듭니다.\n# - 이를 통해 모델이 객체의 경계나 배경과의 관계를 어떻게 전역적으로 이해하는지 직관적으로 확인할 수 있습니다.\n\nw = AttentionVisualizer(model, transform)  # 위젯 생성 및 화면에 표시\nw.run()"}], "blanks": [{"id": "blank-detr-resize", "label": "DETR 입력 크기 조정", "answer": "T.Resize(800)"}, {"id": "blank-detr-normalize", "label": "ImageNet 정규화", "answer": "T.Normalize([0.485, 0.456, 0.406], [0.229, 0.224, 0.225])"}, {"id": "blank-detr-model", "label": "사전학습 DETR 로드", "answer": "torch.hub.load('facebookresearch/detr', 'detr_resnet50', pretrained=True)"}]}, "04_Unet.ipynb": {"page": "vision_unet_subjective_test", "cells": [{"number": 1, "code_number": null, "type": "markdown", "source": "# **U-Net — 기본 구조부터 학습 및 시각화까지**\n\n **U-Net 기반 이미지 세그멘테이션** 실습을 목표로 합니다.  \n다운샘플(Encoder) → 업샘플(Decoder) + **Skip Connection** 구조가 **왜 분할(segmentation)에 강한지**를 직접 확인합니다.\n\n## 학습 목표\n- U-Net의 **Encoder/Decoder/Skip Connection** 흐름을 코드에서 찾을 수 있다.\n- 입력/출력 텐서 shape이 단계별로 어떻게 바뀌는지 설명할 수 있다.\n- 간단한 데이터셋으로 **학습 → 검증 → 예측 시각화**까지 한 번에 실행할 수 있다.\n> Segmentation은 “어디에 무엇이 있는가”를 픽셀 단위로 예측합니다.  \n> 그래서 U-Net은 **공간 정보(spatial detail)** 를 보존/복원하기 위한 구조적 장치를 적극적으로 사용합니다.\n\n---\n\n### 구성 개요\n\n1. U-Net 개요 (이론 정리)\n2. 환경 설정 & 데이터 준비\n3. PyTorch U-Net 모델 구현\n4. 손실함수 / 옵티마이저 설정\n5. 학습 루프 (training loop)\n6. 평가 및 시각화\n7. 과제/확장 아이디어"}, {"number": 2, "code_number": null, "type": "markdown", "source": "---\n## 0) Setup (환경 준비)\n\n- **목적:** 실습에 필요한 패키지를 로드하고, GPU 사용 여부를 확인합니다.\n- **관찰 포인트**\n  - `torch.cuda.is_available()` 결과와 사용 디바이스(`cpu/cuda`)\n  - (선택) 재현성을 위해 시드를 고정하는 이유"}, {"number": 3, "code_number": 1, "type": "code", "source": "# 필요한 패키지가 설치되어 있는지 확인하고, 없을 때만 pip로 설치합니다.\n# (재실행해도 중복 설치를 피함)\nimport importlib.util\nimport sys\nimport subprocess\n\ndef ensure_package(pkg_name: str, import_name=None):\n    name = import_name or pkg_name\n    if importlib.util.find_spec(name) is None:\n        print(f\"[install] {pkg_name} (import: {name})\")\n        subprocess.check_call([sys.executable, \"-m\", \"pip\", \"install\", pkg_name])\n    else:\n        print(f\"[ok] {pkg_name}\")\n\n# U-Net 실습에서 사용: 모델 요약 출력\nensure_package(\"torchinfo\")\n\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nfrom torch.utils.data import Dataset, DataLoader\nimport torchvision.transforms as T\n\nimport numpy as np\nimport matplotlib.pyplot as plt\n\ndevice = torch.device(\"cuda\" if torch.cuda.is_available() else \"cpu\")\nprint(\"Using device:\", device)\n\n# CUDA 상세 정보 (옵션)\nif torch.cuda.is_available():\n    print(\"CUDA device count:\", torch.cuda.device_count())\n    print(\"Current device index:\", torch.cuda.current_device())\n    print(\"Device name:\", torch.cuda.get_device_name(0))"}, {"number": 4, "code_number": null, "type": "markdown", "source": "---\n## 1) 데이터 준비\n\n- **목적:** 실습을 빠르게 진행하기 위해, 간단한 **Synthetic Segmentation Dataset**을 사용합니다.\n- **관찰 포인트**\n  - `__getitem__`이 반환하는 입력 이미지와 마스크의 shape / dtype\n  - 마스크 값이 **0/1**(binary)인지, 혹은 클래스 인덱스 형태인지\n  - DataLoader에서 배치로 묶였을 때 텐서 차원이 어떻게 되는지\n\n> 실제 프로젝트에서는 공개 데이터셋(예: 의료 영상, 도로/도시 장면 등)으로 쉽게 교체할 수 있습니다.  \n> 여기서는 **모델 구조/학습 흐름**에 집중하기 위해 synthetic 데이터를 사용합니다."}, {"number": 5, "code_number": 2, "type": "code", "source": "class SyntheticCircleDataset(Dataset):\n    \"\"\"\n    매우 단순한 synthetic 데이터셋 예제입니다.\n\n    - 입력: 1채널 이미지 (H x W)\n    - 타깃: 원 모양의 binary mask\n    \"\"\"\n    def __init__(self, length=200, image_size=128, transform=None):\n        self.length = length\n        self.image_size = image_size\n        self.transform = transform\n\n    def __len__(self):\n        return self.length\n\n    def __getitem__(self, idx):\n        # 빈 이미지 생성\n        img = np.zeros((self.image_size, self.image_size), dtype=np.float32)\n        mask = np.zeros_like(img)\n\n        # 랜덤 원 생성\n        center_x = np.random.randint(self.image_size // 4, 3 * self.image_size // 4)    # 1/4 ~ 3/4\n        center_y = np.random.randint(self.image_size // 4, 3 * self.image_size // 4)\n        radius = np.random.randint(self.image_size // 8, self.image_size // 4)\n\n        y, x = np.ogrid[:self.image_size, :self.image_size]\n        dist_from_center = np.sqrt((x - center_x) ** 2 + (y - center_y) ** 2)\n        mask[dist_from_center <= radius] = 1.0\n\n        # 입력 이미지는 약간의 노이즈 + mask를 이용해 생성\n        img = mask + 0.2 * np.random.randn(self.image_size, self.image_size).astype(np.float32)\n        img = np.clip(img, 0.0, 1.0)\n\n        # (H, W) -> (1, H, W)\n        img = np.expand_dims(img, axis=0)\n        mask = np.expand_dims(mask, axis=0)\n\n        img = torch.from_numpy(img)\n        mask = torch.from_numpy(mask)\n\n        if self.transform is not None:\n            # 필요 시 torchvision.transforms 등을 추가적으로 적용\n            pass\n\n        return img, mask\n\n# 데이터셋/데이터로더 예시\ntrain_dataset = SyntheticCircleDataset(length=400, image_size=128)\nval_dataset   = SyntheticCircleDataset(length=100, image_size=128)\n\ntrain_loader = DataLoader(train_dataset, batch_size=8, shuffle=True)\nval_loader   = DataLoader(val_dataset, batch_size=8, shuffle=False)\n\nprint(\"Train batches:\", len(train_loader))\nprint(\"Val batches:\", len(val_loader))"}, {"number": 6, "code_number": null, "type": "markdown", "source": "---\n## 2) U-Net 모델 구현\n\n- **목적:** U-Net의 표준 블록을 구현하고, Encoder/Decoder의 연결 구조를 코드로 확인합니다.\n- **관찰 포인트**\n  - `DoubleConv`(Conv–BN–ReLU 반복)이 feature 추출에 어떻게 쓰이는지\n  - Down path에서 **Pooling/Stride**로 해상도가 줄어드는 지점\n  - Up path에서 **Up-sampling(ConvTranspose2d 또는 bilinear)** 이 적용되는 지점\n  - Skip 연결 시 **concat 채널 수**가 어떻게 증가하는지(가장 흔한 shape mismatch 원인)\n\n> 구현을 따라가면서 각 단계의 feature map 크기(H×W)와 채널(C)을 메모해 두면 이해가 훨씬 빨라집니다."}, {"number": 7, "code_number": 3, "type": "code", "source": "# =========================\n# 3) U-Net 모델 정의\n# =========================\n# 이 섹션의 목표:\n# - U-Net의 \"인코더(Down) → 보틀넥 → 디코더(Up) + Skip Connection\" 구조를\n#   PyTorch 코드로 직접 따라가며 이해합니다.\n#\n# 핵심 아이디어(한 줄):\n# - Down에서 공간 해상도(H,W)는 줄이고 채널(C)은 늘리면서 특징을 추출하고,\n#   Up에서 해상도를 복원하면서 Down 단계의 특징맵을 Skip으로 concat하여\n#   localization(위치 정보)을 되살립니다.\n\nclass DoubleConv(nn.Module):\n    \"\"\"U-Net 기본 블록: (Conv → ReLU) × 2\n\n    - 첫 번째 Conv가 채널을 '중간 채널(mid_channels)'로 바꾸고,\n      두 번째 Conv가 '출력 채널(out_channels)'로 맞춥니다.\n    - 기본값(mid_channels=None)일 때는 mid_channels=out_channels로 두어,\n      (C_in → C_out → C_out) 형태가 됩니다.\n\n    입력/출력 텐서 형태:\n      - 입력:  (B, C_in, H, W)\n      - 출력:  (B, C_out, H, W)  # padding=1 이라 H,W 유지\n    \"\"\"\n    def __init__(self, in_channels: int, out_channels: int, mid_channels: int | None = None):\n        super().__init__()\n\n        # U-Net에서 업샘플 후 concat을 하면 채널이 2배가 되므로,\n        # bilinear 업샘플링을 쓸 때는 mid_channels=in_channels//2 처럼\n        # '중간 채널'을 줄여주는 방식이 흔히 사용됩니다.\n        if mid_channels is None:\n            mid_channels = out_channels\n\n        self.net = nn.Sequential(\n            # 1) (C_in → C_mid)\n            nn.Conv2d(in_channels, mid_channels, kernel_size=3, padding=1, bias=False),\n            nn.ReLU(inplace=True),\n\n            # 2) (C_mid → C_out)\n            nn.Conv2d(mid_channels, out_channels, kernel_size=3, padding=1, bias=False),\n            nn.ReLU(inplace=True),\n        )\n\n    def forward(self, x: torch.Tensor) -> torch.Tensor:\n        return self.net(x)\n\nclass Down(nn.Module):\n    \"\"\"Downsampling 블록: MaxPool(2)로 해상도 1/2 → DoubleConv\n\n    입력/출력 텐서 형태:\n      - 입력:  (B, C_in,  H,  W)\n      - 출력:  (B, C_out, H/2, W/2)\n    \"\"\"\n    def __init__(self, in_channels: int, out_channels: int):\n        super().__init__()\n        self.pool = nn.MaxPool2d(kernel_size=2)\n        self.conv = DoubleConv(in_channels, out_channels)\n\n    def forward(self, x: torch.Tensor) -> torch.Tensor:\n        # 1) 해상도 축소\n        x = self.pool(x)           # (B, C_in, H/2, W/2)\n        # 2) 채널 확장 + 특징 추출\n        x = self.conv(x)           # (B, C_out, H/2, W/2)\n        return x\n\n\nclass Up(nn.Module):\n    \"\"\"Upsampling 블록: 업샘플링 → (Skip concat) → DoubleConv\n\n    구현 관점(중요):\n    - in_channels 는 concat 이후 채널 수를 의미합니다.\n      예) x1(디코더) 채널=512, x2(skip) 채널=512 → concat 채널=1024 → in_channels=1024\n\n    - bilinear=True:\n        1) 업샘플은 파라미터 없는 bilinear interpolation으로 수행\n        2) concat 후 DoubleConv에서 mid_channels를 in_channels//2 로 두어\n           채널을 자연스럽게 '절반'으로 줄이는 방식(원 논문/레퍼런스 구현과 동일 계열)\n\n    - bilinear=False:\n        ConvTranspose2d로 업샘플 자체를 학습(파라미터 증가)\n    \"\"\"\n    def __init__(self, in_channels: int, out_channels: int, bilinear: bool = False):\n        super().__init__()\n\n        if bilinear:\n            # (B, C, H/2, W/2) → (B, C, H, W)\n            self.up = nn.Upsample(scale_factor=2, mode=\"bilinear\", align_corners=True)\n\n            # bilinear일 때는 업샘플 후 채널 수(C)가 그대로 유지됩니다.\n            # concat 결과(in_channels)를 DoubleConv로 처리하되,\n            # 첫 conv의 출력(mid_channels)을 in_channels//2로 두어 채널을 줄입니다.\n            self.conv = DoubleConv(in_channels, out_channels, mid_channels=in_channels // 2)\n        else:\n            # (B, C, H/2, W/2) → (B, C/2, H, W)  (deconv가 채널도 절반으로 줄여줌)\n            self.up = [[BLANK:blank-unet-up]]\n            self.conv = DoubleConv(in_channels, out_channels)\n\n    def forward(self, x1: torch.Tensor, x2: torch.Tensor) -> torch.Tensor:\n        # x1: (B, C_dec, H/2, W/2)  / x2: (B, C_skip, H, W)\n\n        # 1) 업샘플링: 해상도를 skip과 맞추기\n        x1 = self.up(x1)  # bilinear: 채널 유지 / deconv: 채널이 절반으로 감소\n\n        # 2) (필요 시) 패딩으로 크기 정렬\n        #    - 홀수 크기 입력 등으로 인해 x1과 x2의 H/W가 1~2 픽셀 정도 다를 수 있습니다.\n        diff_y = x2.size(2) - x1.size(2)\n        diff_x = x2.size(3) - x1.size(3)\n        x1 = F.pad(x1, [diff_x // 2, diff_x - diff_x // 2,  # left, right\n                        diff_y // 2, diff_y - diff_y // 2]) # top, bottom\n\n        # 3) 채널 방향 concat (skip 연결)\n        #    cat dim=1 은 채널(C) 축\n        x = [[BLANK:blank-unet-skip]]  # (B, C_skip + C_up, H, W) == (B, in_channels, H, W)\n\n        # 4) conv 블록으로 특징 정제 + 채널 축소\n        return self.conv(x)\n\nclass OutConv(nn.Module):\n    \"\"\"마지막 1x1 conv: 채널을 클래스 수로 매핑\n\n    예)\n      - binary segmentation: n_classes=1 (logits 1채널)\n      - multi-class:         n_classes=K (logits K채널)\n    \"\"\"\n    def __init__(self, in_channels: int, out_channels: int):\n        super().__init__()\n        self.conv = nn.Conv2d(in_channels, out_channels, kernel_size=1)\n\n    def forward(self, x: torch.Tensor) -> torch.Tensor:\n        return self.conv(x)\n\n\nclass UNet(nn.Module):\n    \"\"\"U-Net 전체 모델\n\n    전형적인 채널 구성 예:\n      1 → 64 → 128 → 256 → 512 → 1024 (down)\n      1024 → 512 → 256 → 128 → 64 (up)\n    \"\"\"\n    def __init__(self, n_channels: int, n_classes: int, bilinear: bool = False):\n        super().__init__()\n\n        # -------------------------\n        # Encoder (Contracting path)\n        # -------------------------\n        self.inc = DoubleConv(n_channels, 64)   # (B, n_channels, H, W) → (B, 64, H, W)\n        self.down1 = Down(64, 128)              # → (B, 128, H/2, W/2)\n        self.down2 = Down(128, 256)             # → (B, 256, H/4, W/4)\n        self.down3 = Down(256, 512)             # → (B, 512, H/8, W/8)\n\n        # bilinear 업샘플링이면 파라미터/연산을 줄이기 위해 bottleneck 채널을 1024 대신 512로 줄이는 경우가 많음\n        factor = 2 if bilinear else 1\n        self.down4 = Down(512, 1024 // factor)  # → (B, 1024/f, H/16, W/16)\n\n        # -------------------------\n        # Decoder (Expanding path)\n        # -------------------------\n        # Up 블록의 in_channels는 \"concat 후 채널\" 기준으로 설계되어야 합니다.\n        self.up1 = Up(1024, 512 // factor, bilinear)  # (skip=512)와 concat을 고려\n        self.up2 = Up(512, 256 // factor, bilinear)\n        self.up3 = Up(256, 128 // factor, bilinear)\n        self.up4 = Up(128, 64, bilinear)\n\n        # -------------------------\n        # Output head\n        # -------------------------\n        self.outc = OutConv(64, n_classes)\n\n    def forward(self, x: torch.Tensor) -> torch.Tensor:\n        # -------------------------\n        # Encoder: skip을 위해 각 단계 출력 저장\n        # -------------------------\n        x1 = self.inc(x)       # (B, 64, H, W)\n        x2 = self.down1(x1)    # (B, 128, H/2, W/2)\n        x3 = self.down2(x2)    # (B, 256, H/4, W/4)\n        x4 = self.down3(x3)    # (B, 512, H/8, W/8)\n        x5 = self.down4(x4)    # (B, 1024/f, H/16, W/16)\n\n        # -------------------------\n        # Decoder: 업샘플 + skip concat\n        # -------------------------\n        x = self.up1(x5, x4)   # (B, 512/f, H/8, W/8)\n        x = self.up2(x, x3)    # (B, 256/f, H/4, W/4)\n        x = self.up3(x, x2)    # (B, 128/f, H/2, W/2)\n        x = self.up4(x, x1)    # (B, 64, H, W)\n\n        # -------------------------\n        # 최종 logits 출력\n        # -------------------------\n        logits = self.outc(x)  # (B, n_classes, H, W)\n        return logits\n\n\n# =========================\n# =========================\n# 4) 모델 생성 및 구조 확인\n# =========================\nmodel = UNet(n_channels=1, n_classes=1).to(device)\n\n# -------------------------\n# 4) 모델 구조 확인: print(model) + torchinfo.summary\n# -------------------------\nprint(model)\n\n# torchinfo.summary는 forward를 한 번 실행해 각 레이어의 출력 shape/파라미터 수를 정리해줍니다.\n# - 모델이 학습 모드(train)일 때 BatchNorm/Dropout 등이 있을 수 있으니,\n#   구조 확인 목적에서는 eval()로 두는 것이 안전합니다.\nmodel.eval()\n\ntry:\n    from torchinfo import summary\n\n    # 입력 크기: (B, C, H, W)\n    # - 여기서는 (1, 1, 128, 128) 텐서를 기준으로 요약합니다.\n    # - 다른 해상도로 실험하려면 H,W만 바꾸면 됩니다.\n    _summary = summary(\n        model,\n        input_size=(1, 1, 128, 128),\n        depth=4,\n        device=device,   # cpu/cuda 환경에 맞춰 동일 디바이스에서 실행\n    )\n    print(_summary)\nexcept Exception as e:\n    print(\"[torchinfo.summary] 실행 불가:\", repr(e))\n    print(\"대응:\")\n    print(\"  1) torchinfo 설치 확인: pip install torchinfo\")\n    print(\"  2) 입력 크기/채널 수가 모델 기대와 맞는지 확인\")\n    print(\"  3) 앞선 forward sanity check가 통과하는지 확인\")\n\n# -------------------------\n# 5) Forward sanity check (shape 확인)\n# -------------------------\ndummy = torch.randn(1, 1, 128, 128, device=device)\nwith torch.no_grad():\n    out = model(dummy)\n\nprint(\"Input shape:\", tuple(dummy.shape), \"Output shape:\", tuple(out.shape))"}, {"number": 8, "code_number": null, "type": "markdown", "source": "---\n## 3) 손실 함수 & 옵티마이저 설정\n\n- **목적:** Segmentation 학습에 필요한 손실 함수와 최적화 방법을 설정합니다.\n- **관찰 포인트**\n  - Binary segmentation이면 보통 `BCEWithLogitsLoss`를 많이 사용(로짓 입력 주의)\n  - Multi-class segmentation이면 `CrossEntropyLoss`(타깃은 class index) 사용\n  - `sigmoid/softmax`를 **언제 적용해야 하는지**(loss와의 궁합)\n\n> 이 템플릿은 “최소 구성” 예제를 우선 제공합니다.  \n> 성능을 올리려면 Dice loss / IoU loss 같은 항목을 추가로 고려할 수 있습니다."}, {"number": 9, "code_number": 4, "type": "code", "source": "# 손실 함수 및 옵티마이저 설정\ncriterion = nn.BCEWithLogitsLoss()  # Sigmoid + Binary Cross Entropy\noptimizer = torch.optim.Adam(model.parameters(), lr=1e-3)\n\n# (옵션) 학습 스케줄러\n# 20 에포크(Epoch)마다 학습률을 변경, 기존 학습률에 0.5를 곱합\nscheduler = torch.optim.lr_scheduler.StepLR(optimizer, step_size=20, gamma=0.5)"}, {"number": 10, "code_number": null, "type": "markdown", "source": "---\n## 4) 학습 루프\n\n- **목적:** 가장 기본적인 형태의 학습/검증 루프를 실행해, loss가 정상적으로 내려가는지 확인합니다.\n- **관찰 포인트**\n  - `model.train()` / `model.eval()` 모드 전환\n  - `optimizer.zero_grad()` → `loss.backward()` → `optimizer.step()` 순서\n  - 검증 단계에서 `torch.no_grad()` 사용 여부\n  - 학습이 진행되면서 train/val loss가 과도하게 벌어지는지(과적합 신호)\n\n> Segmentation은 시각화가 쉬운 편이라, **숫자(loss) + 이미지(예측 결과)** 를 같이 보는 습관이 중요합니다."}, {"number": 11, "code_number": 5, "type": "code", "source": "def train_one_epoch(model, dataloader, optimizer, criterion, device):\n    model.train()\n    running_loss = 0.0\n    for imgs, masks in dataloader:\n        imgs = imgs.to(device)\n        masks = masks.to(device)\n\n        optimizer.zero_grad()\n        logits = model(imgs)\n        [[BLANK:blank-unet-loss]]\n        loss.backward()\n        optimizer.step()\n\n        running_loss += loss.item() * imgs.size(0)\n\n    epoch_loss = running_loss / len(dataloader.dataset)\n    return epoch_loss\n\n\n@torch.no_grad()\ndef evaluate(model, dataloader, criterion, device):\n    model.eval()\n    running_loss = 0.0\n    for imgs, masks in dataloader:\n        imgs = imgs.to(device)\n        masks = masks.to(device)\n\n        logits = model(imgs)\n        loss = criterion(logits, masks)\n        running_loss += loss.item() * imgs.size(0)\n\n    epoch_loss = running_loss / len(dataloader.dataset)\n    return epoch_loss\n\n\nnum_epochs = 10\ntrain_losses = []\nval_losses = []\n\nfor epoch in range(1, num_epochs + 1):\n    train_loss = train_one_epoch(model, train_loader, optimizer, criterion, device)\n    val_loss = evaluate(model, val_loader, criterion, device)\n    scheduler.step()\n\n    train_losses.append(train_loss)\n    val_losses.append(val_loss)\n\n    print(f\"Epoch [{epoch}/{num_epochs}]  train_loss: {train_loss:.4f}  val_loss: {val_loss:.4f}\")"}, {"number": 12, "code_number": null, "type": "markdown", "source": "---\n## 4-1) 학습 곡선 시각화\n\n- **목적:** epoch별 loss 변화를 그래프로 확인해 학습이 “정상적으로 진행되는지” 빠르게 점검합니다.\n- **관찰 포인트**\n  - 초반에 loss가 급격히 감소하는지\n  - 어느 시점부터 감소가 둔화되는지(학습률 조정/early stopping 힌트)\n  - train/val 곡선 간 격차가 커지는지(과적합)"}, {"number": 13, "code_number": 6, "type": "code", "source": "plt.figure()\nplt.plot(train_losses, label='train_loss')\nplt.plot(val_losses, label='val_loss')\nplt.xlabel('Epoch')\nplt.ylabel('Loss')\nplt.legend()\nplt.title('Training / Validation Loss')\nplt.show()"}, {"number": 14, "code_number": null, "type": "markdown", "source": "---\n## 5) 결과 시각화\n\n- **목적:** 모델이 실제로 “분할”을 하고 있는지, 입력/정답/예측을 나란히 비교합니다.\n- **관찰 포인트**\n  - 출력이 로짓이면 `sigmoid` 후 threshold(예: 0.5)로 마스크를 만드는지\n  - GT 마스크와 예측 마스크의 차이가 어디에서 주로 발생하는지(경계/작은 객체/노이즈 등)\n  - 데이터가 간단한데도 실패한다면: 모델 출력 채널, loss, target 타입을 다시 점검\n\n> 숫자(loss)만 보고 넘어가면 “뭔가 학습은 되는 것 같은데 결과가 이상한” 상황을 놓치기 쉽습니다."}, {"number": 15, "code_number": 7, "type": "code", "source": "@torch.no_grad()\ndef visualize_predictions(model, dataloader, device, num_samples=3):\n    model.eval()\n    imgs, masks = next(iter(dataloader))\n    imgs = imgs.to(device)\n    masks = masks.to(device)\n\n    logits = model(imgs)\n    probs = torch.sigmoid(logits)\n    preds = (probs > 0.5).float()\n\n    imgs = imgs.cpu().numpy()\n    masks = masks.cpu().numpy()\n    preds = preds.cpu().numpy()\n\n    num_samples = min(num_samples, imgs.shape[0])\n\n    plt.figure(figsize=(9, num_samples * 3))\n    for i in range(num_samples):\n        # 입력\n        plt.subplot(num_samples, 3, 3 * i + 1)\n        plt.imshow(imgs[i, 0], cmap='gray')\n        plt.title('Input')\n        plt.axis('off')\n\n        # GT mask\n        plt.subplot(num_samples, 3, 3 * i + 2)\n        plt.imshow(masks[i, 0], cmap='gray')\n        plt.title('GT Mask')\n        plt.axis('off')\n\n        # Pred mask\n        plt.subplot(num_samples, 3, 3 * i + 3)\n        plt.imshow(preds[i, 0], cmap='gray')\n        plt.title('Pred Mask')\n        plt.axis('off')\n\n    plt.tight_layout()\n    plt.show()\n\n\nvisualize_predictions(model, val_loader, device, num_samples=3)"}], "blanks": [{"id": "blank-unet-up", "label": "학습 가능한 업샘플링", "answer": "nn.ConvTranspose2d(in_channels, in_channels // 2, kernel_size=2, stride=2)"}, {"id": "blank-unet-skip", "label": "Skip feature 채널 결합", "answer": "torch.cat([x2, x1], dim=1)"}, {"id": "blank-unet-loss", "label": "Mask 예측 손실", "answer": "loss = criterion(logits, masks)"}]}, "05_DDPM.ipynb": {"page": "vision_ddpm_subjective_test", "cells": [{"number": 1, "code_number": null, "type": "markdown", "source": "# **DDPM — 학습/추론/시각화**\n\n**DDPM (Denoising Diffusion Probabilistic Models)** 을 직접 구현하면서 구조를 이해하는 것을 목표로 합니다.\n\n## 학습 목표\n- DDPM의 원리를 이해한다.\n- Forward / Reverse diffusion 을 이해하고 Training과 Sampling 과정을 구현한다.\n\n---\n\n### 구성 개요\n1. Diffusion / DDPM 개념 요약\n2. 실험 환경 및 기본 설정\n3. U-Net 기반 노이즈 예측 신경망 구현\n4. Forward Diffusion Process 정의 ($q$)\n5. Reverse Diffusion Process 정의 ($p$)\n6. Fashion-MNIST 데이터셋 로드 및 전처리\n7. 학습 루프 구현\n8. 샘플링 및 시각화 (이미지 + GIF)"}, {"number": 2, "code_number": null, "type": "markdown", "source": "## 1. Diffusion / DDPM 개념 요약\n\n### 1.1 생성 모델 관점\n\n- 생성 모델(generative model)은 **데이터 분포 $p_{data}(x)$** 를 학습해서, 학습 후에는 샘플링으로 새로운 데이터를 생성할 수 있는 모델입니다.\n- 예: GAN, VAE, Autoregressive model, Flow-based model, Diffusion model 등\n\n### 1.2 Diffusion 모델의 아이디어 (고수준)\n\n1. **Forward Diffusion (noising)** - 깨끗한 이미지 $x_0$ 에 **점진적으로 가우시안 노이즈를 추가**해서 $x_1, x_2, \\dots, x_T$ 를 만듭니다.\n   - $T$ 가 충분히 크면, $x_T$ 는 거의 **표준 정규분포와 비슷한 pure noise** 가 됩니다.\n   - 이 과정은 **고정된 마르코프 체인** $q(x_t \\mid x_{t-1})$ 으로 정의됩니다.\n\n2. **Reverse Diffusion (denoising)** - 우리가 학습하고 싶은 것은 반대로 **노이즈에서 시작해서 점점 이미지를 되살리는 과정**입니다.\n   - $p_\\theta(x_{t-1} \\mid x_t)$ 를 신경망으로 근사해서, $x_T \\sim \\mathcal{N}(0, I)$ 에서 시작해 $x_0$ 를 샘플링할 수 있게 합니다.\n\n3. **DDPM의 핵심 포인트** - Forward process는 **닫힌형태(analytic form)** 를 가지도록 설계되어, $q(x_t \\mid x_0)$ 를 한 번에 샘플링할 수 있습니다.\n   - 학습 시에는 $x_0$ 와 $t$ 를 샘플링한 뒤, $x_t$ 를 계산하고, 네트워크가 **노이즈 $\\epsilon$** 을 예측하도록 만듭니다.\n   - 손실 함수는 보통 **예측 노이즈와 실제 노이즈의 $L_2$ 또는 Huber loss** 로 정의합니다.\n\n이제부터는 이 개념을 실제 코드로 구현해 봅니다."}, {"number": 3, "code_number": null, "type": "markdown", "source": "## 2. 환경 설정 및 기본 Import\n\n- PyTorch, torchvision, einops, datasets, matplotlib 등을 사용합니다.\n- 아래 셀은 GPU 사용 가능 여부를 확인하고, 필요한 라이브러리를 import 합니다."}, {"number": 4, "code_number": 1, "type": "code", "source": "# 유틸리티 및 함수형 프로그래밍 관련\nimport math\nfrom functools import partial\nfrom pathlib import Path   # 파일 및 디렉토리 경로 관리\n\n# PyTorch 핵심 라이브러리\nimport torch\nimport torch.nn as nn            # 신경망 레이어 구성 (Conv2d, Linear 등)\nimport torch.nn.functional as F   # 활성화 함수 및 손실 함수\n\n# 데이터 로딩 및 이미지 처리\nfrom torch.utils.data import DataLoader\nfrom torchvision import transforms      # 이미지 전처리 (Resize, ToTensor 등)\nfrom torchvision.utils import save_image # 결과 이미지 저장\n\n# 텐서 차원 조작 (가독성 높은 텐서 변환)\nfrom einops import rearrange, reduce\nfrom einops.layers.torch import Rearrange # 모델 레이어 내 차원 재구성\n\n# 데이터셋 불러오기 및 시각화/수치 계산\nfrom datasets import load_dataset # 데이터셋 로드\nimport numpy as np\nimport matplotlib.pyplot as plt   # 학습 곡선 및 이미지 시각화\nfrom tqdm.auto import tqdm        # 학습 진행률 표시바\n\n# 연산 장치 설정 (GPU 가속 여부 확인)\ndevice = \"cuda\" if torch.cuda.is_available() else \"cpu\"\nprint(f\"Using device: {device}\")"}, {"number": 5, "code_number": null, "type": "markdown", "source": "## 3. U-Net 구현을 위한 Helper 함수들\n\nDDPM에서 사용할 U-Net 구조를 구현하기 위해 아래와 같은 helper 함수/클래스를 사용합니다.\n\n- `exists`, `default`: 인자 존재 여부/기본값 처리용 유틸리티\n- `num_to_groups`: 전체 개수를 mini-batch 그룹 형태로 나누기\n- `Residual`: 잔차 연결(residual connection)을 감싸는 래퍼\n- `Upsample` / `Downsample`: 이미지 해상도를 2배 업/다운샘플링하는 모듈"}, {"number": 6, "code_number": 2, "type": "code", "source": "def exists(x):\n    \"\"\"값이 존재하는지(None 여부) 확인하는 유틸리티\"\"\"\n    return x is not None\n\ndef default(val, d):\n    \"\"\"val이 None일 경우 기본값 d를 반환 (d가 함수라면 호출해서 결과 반환)\"\"\"\n    if exists(val):\n        return val\n    return d() if callable(d) else d\n\ndef num_to_groups(num, divisor):\n    \"\"\"\n    전체 샘플(num)을 지정된 크기(divisor)로 나눈 리스트를 생성.\n    예: num=10, divisor=4 -> [4, 4, 2] (Batch를 나누어 처리할 때 유용)\n    \"\"\"\n    groups = num // divisor\n    remainder = num % divisor\n    arr = [divisor] * groups\n    if remainder > 0:\n        arr.append(remainder)\n    return arr\n\nclass Residual(nn.Module):\n    \"\"\"\n    입력값 x를 출력에 다시 더해주는 잔차 연결(Skip Connection) 래퍼.\n    Deep Network에서 Gradient Vanishing 문제를 완화하고 학습 안정성을 높임.\n    \"\"\"\n    def __init__(self, fn):\n        super().__init__()\n        self.fn = fn\n\n    def forward(self, x, *args, **kwargs):\n        # f(x) + x: 입력과 출력의 Shape이 동일해야 함\n        return self.fn(x, *args, **kwargs) + x\n\ndef Upsample(dim, dim_out=None):\n    \"\"\"\n    이미지 해상도를 2배 키우는 업샘플링 블록.\n    Nearest Neighbor로 해상도를 올린 후 Conv2d로 특징을 부드럽게 정제(Anti-aliasing).\n    \"\"\"\n    return nn.Sequential(\n        nn.Upsample(scale_factor=2, mode=\"nearest\"),\n        nn.Conv2d(dim, default(dim_out, dim), 3, padding=1),\n    )\n\ndef Downsample(dim, dim_out=None):\n    \"\"\"\n    이미지 해상도를 1/2로 줄이는 다운샘플링 블록.\n    Space-to-Depth(Pixel Unshuffle) 방식을 사용하여 정보 손실 없이 채널을 확장한 후,\n    1x1 Conv를 통해 원하는 채널 크기(dim_out)로 압축함.\n    \"\"\"\n    return nn.Sequential(\n        # (B, C, H*2, W*2) -> (B, C*4, H, W)\n        Rearrange(\"b c (h p1) (w p2) -> b (c p1 p2) h w\", p1=2, p2=2),\n        # 1x1 Conv로 채널 수 조정\n        nn.Conv2d(dim * 4, default(dim_out, dim), 1),\n    )"}, {"number": 7, "code_number": null, "type": "markdown", "source": "## 4. Time Embedding과 ResNet Block\n\n### 4.1 Sinusoidal Position Embedding\n\n- Diffusion model은 **time step(노이즈 단계) $t$** 에 따라 같은 네트워크 파라미터를 공유합니다.\n- 네트워크가 “지금 몇 번째 노이즈 단계인지” 알 수 있도록, Transformer에서 사용하는 **sinusoidal position embedding** 을 사용합니다.\n- 입력: `(batch,)` 형태의 정수 time step\n- 출력: `(batch, dim)` 형태의 연속 벡터\n\n### 4.2 ResNet Block & Weight Standardization\n\n- U-Net의 기본 블록은 **GroupNorm + SiLU + Conv** 로 구성된 블록입니다.\n- DDPM 구현에서는 Wide ResNet block 대신 **Weight Standardized Conv2d + GroupNorm** 을 사용해 학습 안정성을 높입니다.\n- Time embedding은 각 ResNet block에 **scale/shift (FiLM-style)** 로 주입됩니다."}, {"number": 8, "code_number": 3, "type": "code", "source": "class SinusoidalPositionEmbeddings(nn.Module):\n    \"\"\"\n    타임스텝(t) 정보를 신경망이 이해할 수 있는 고차원 벡터로 변환합니다.\n    Transformer의 positional encoding과 동일하게 sin, cos 함수를 사용합니다.\n    \"\"\"\n    def __init__(self, dim):\n        super().__init__()\n        self.dim = dim # 결과 벡터의 차원 (보통 128, 256 등)\n\n    def forward(self, time):\n        # time: (batch_size,) 형태의 정수 타임스텝\n        device = time.device\n        half_dim = self.dim // 2\n\n        # 주파수(frequencies) 계산: 각 차원마다 서로 다른 주기를 할당함 (1 -> 0.0001)\n        freq = math.log(10000) / (half_dim - 1)\n        freq = torch.exp(torch.arange(half_dim, device=device) * -freq)\n\n        # 타임스텝 값에 주파수를 곱함 (batch, 1) * (1, half_dim) -> (batch, half_dim)\n        # 예)\n        # time[:,None] → [[10],[20],[30]] shape (3,1)\n        # freq[None,:] → [[1.0,0.1,0.01,0.001]] shape (1,4)\n        # args shape (3,4):\n        # row0: [10, 1, 0.1, 0.01]\n        # row1: [20, 2, 0.2, 0.02]\n        # row2: [30, 3, 0.3, 0.03]\n        args = time[:, None].float() * freq[None, :]\n\n        # sin과 cos 값을 연결하여 하나의 임베딩 벡터 생성 (batch, dim)\n        emb = torch.cat([args.sin(), args.cos()], dim=-1)\n        return emb\n\n\n\nclass WeightStandardizedConv2d(nn.Conv2d):\n    \"\"\"\n    Weight Standardization → kernel(weight) 정규화\n    일반적인 Conv2d를 확장한 레이어입니다.\n    각 출력 채널별 커널의 모든 Weight에 대해서 가중치(Weight)를 평균 0, 분산 1로 표준화하여\n    Batch Size가 작을 때 학습을 안정화시키는 역할을 합니다.\n    주로 GroupNorm과 같이 쓰입니다\n    \"\"\"\n    def forward(self, x):\n        # 수치적 안정성을 위한 아주 작은 값\n        eps = 1e-5 if x.dtype == torch.float32 else 1e-3\n        weight = self.weight # 컨볼루션 필터 자체의 가중치 (o, i, kH, kW)\n\n        # 가중치의 평균과 분산을 출력 채널(o) 단위로 계산\n        mean = reduce(weight, \"o ... -> o 1 1 1\", \"mean\")   # 채널마다 하나의 scalar, mean.shape == (o, 1, 1, 1)\n        var = reduce(weight, \"o ... -> o 1 1 1\", partial(torch.var, unbiased=False))\n\n        # 가중치 정규화 (Weight Standardization)\n        normalized_weight = (weight - mean) / (var + eps).rsqrt()\n\n        # 정규화된 가중치로 실제 컨볼루션 연산 수행\n        return F.conv2d(\n            x, normalized_weight, self.bias, self.stride,\n            self.padding, self.dilation, self.groups,\n        )\n\n\nclass Block(nn.Module):\n    \"\"\"\n    기본 연산 단위: Convolution -> Group Normalization -> SiLU 활성화 함수\n    scale_shift가 들어오면 타임스텝 정보를 특징 맵에 주입합니다.\n    FiLM(Feature-wise Linear Modulation) or Adaptive Group Normalization (AdaGN)\n    \"\"\"\n    def __init__(self, dim, dim_out, groups=8):\n        super().__init__()\n        self.proj = WeightStandardizedConv2d(dim, dim_out, 3, padding=1)\n        self.norm = nn.GroupNorm(groups, dim_out) # 채널을 그룹으로 나눠 정규화 (배치 크기에 무관하게 안정적)\n        self.act = nn.SiLU() # 최근 생성 모델에서 자주 쓰이는 매끄러운 활성화 함수\n\n    def forward(self, x, scale_shift=None):\n        x = self.proj(x)    # (B, dim, H, W) -> (B, dim_out, H, W)\n        x = self.norm(x)    # shape 유지\n\n        # 타임스텝 임베딩(scale_shift)이 있으면 특징 맵에 반영 (AdaIN과 유사한 방식)\n        # 단순히 이미지 특징(x)에 타임스텝을 더하는 것이 아니라,\n        # 타임스텝 정보에 따라 이미지 특징의 강도를 채널별로 조절(Scale)하고 편향을 수정(Shift)하는 것\n        if exists(scale_shift):\n            scale, shift = scale_shift  # scale:(B, C, 1, 1), shift:(B, C, 1, 1)\n            # x = x * (1 + scale) + shift 공식을 통해 특징을 변형, 채널별 affine 변환 -> y = x⋅(1+s) + b\n            x = x * (scale + 1) + shift\n\n        x = self.act(x)\n        return x\n\n\nclass ResnetBlock(nn.Module):\n    \"\"\"\n    Diffusion 모델의 핵심 블록입니다.\n    1. 입력 이미지에 타임스텝 정보를 섞어주고\n    2. Residual 연결(잔차 연결)을 통해 깊은 층에서도 학습이 잘 되게 합니다.\n    \"\"\"\n    def __init__(self, dim, dim_out, *, time_emb_dim=None, groups=8):\n        super().__init__()\n        # 타임스텝 정보를 가공하기 위한 작은 신경망(MLP)\n        self.mlp = (\n            nn.Sequential(\n                nn.SiLU(),\n                nn.Linear(time_emb_dim, dim_out * 2) # scale과 shift 두 값을 뽑기 위해 2배로 확장\n            )\n            if exists(time_emb_dim) else None\n        )\n\n        self.block1 = Block(dim, dim_out, groups=groups)\n        self.block2 = Block(dim_out, dim_out, groups=groups)\n\n        # 입력과 출력 채널이 다를 경우 맞춰주기 위한 1x1 Conv (잔차 연결용)\n        self.res_conv = nn.Conv2d(dim, dim_out, 1) if dim != dim_out else nn.Identity()\n\n    def forward(self, x, time_emb=None):\n        scale_shift = None\n\n        # 1. 타임스텝 정보를 현재 레이어의 채널 크기에 맞게 변환\n        if exists(self.mlp) and exists(time_emb):\n            time_emb = self.mlp(time_emb) # (batch, dim_out * 2)\n            time_emb = rearrange(time_emb, \"b c -> b c 1 1\") # 이미지 공간 크기에 맞춰 차원 확장\n            scale_shift = time_emb.chunk(2, dim=1) # (scale, shift)로 반씩 쪼갬\n\n        # 2. 첫 번째 블록 (타임스텝 정보 주입) -> 이미지 특징에 지금은 t 단계다라는 정보가 각인\n        h = self.block1(x, scale_shift=scale_shift)\n\n        # 3. 두 번째 블록\n        h = self.block2(h)\n\n        # 4. 잔차 연결: 입력값 x를 결과에 더함 (정보 유실 방지)\n        return h + self.res_conv(x)"}, {"number": 9, "code_number": null, "type": "markdown", "source": "## 5. Attention 모듈\n\nDDPM의 U-Net 내부에는 **convolution block 사이사이에 attention** 이 섞여 있습니다.\n\n- 이미지의 **글로벌 컨텍스트**를 보는 역할\n- 두 가지 버전:\n  - 일반 Multi-Head Self-Attention\n  - Linear Attention (sequence 길이에 대해 O(N) scaling)\n\n아래는 두 attention 모듈과, attention 전에 GroupNorm을 적용하는 `PreNorm` 래퍼입니다."}, {"number": 10, "code_number": 4, "type": "code", "source": "class Attention(nn.Module):\n    \"\"\"\n    표준적인 Multi-Head Self-Attention입니다.\n    이미지의 모든 픽셀 쌍 사이의 유사도를 계산하므로 정확하지만,\n    해상도가 커지면 계산량이 급격히(제곱으로) 늘어납니다.\n    Attn(Q,K,V) = softmax(Q⊤ K)V\n    \"\"\"\n    def __init__(self, dim, heads=4, dim_head=32):\n        super().__init__()\n        self.scale = dim_head ** -0.5 # Softmax 전 수치 안정성을 위한 스케일링 상수 (1/sqrt(d_k))\n        self.heads = heads\n        hidden_dim = dim_head * heads\n\n        # 1x1 Conv를 사용해 입력 채널을 Q, K, V 세 덩어리로 한꺼번에 생성, nn.Conv2d(in_chs, out_chs, kernel_size,...)\n        self.to_qkv = [[BLANK:blank-ddpm-qkv]]\n        # 최종 결과를 다시 원래 채널 크기로 돌려주는 1x1 Conv\n        self.to_out = nn.Conv2d(hidden_dim, dim, 1)\n\n    def forward(self, x):\n        b, c, h, w = x.shape\n\n        # 1. Q, K, V 생성 및 분리\n        # (B, dim, H, W) -> (B, hidden_dim * 3, H, W) -> 3개의 (B, hidden_dim, H, W)\n        qkv = self.to_qkv(x).chunk(3, dim=1)\n\n        # 2. 데이터를 'Head' 별로 쪼개고 2D 이미지를 1D 시퀀스로 펼침 (Flatten)\n        # (B, hidden_dim, H, W) -> (B, heads, dim_head, H*W)\n        q, k, v = map(\n            lambda t: rearrange(t, \"b (h c) x y -> b h c (x y)\", h=self.heads), qkv\n        )\n\n        # 3. Attention Score 계산 (Q와 K의 내적)\n        q = q * self.scale\n        # sim shape: (B, heads, HW, HW) -> 모든 픽셀 i와 모든 픽셀 j 사이의 관계를 행렬로 표현\n        sim = [[BLANK:blank-ddpm-scores]] # (B, heads, N, N), where N=H*W\n\n        # 수치적 안정성을 위해 최대값을 빼줌 (Softmax 폭주 방지)\n        # 가장 큰 값은 항상 0이 되고, 나머지는 음수가 됨\n        sim = sim - sim.amax(dim=-1, keepdim=True).detach()\n        # 4. Attention Map 생성 (가중치 합이 1이 되도록)\n        attn = [[BLANK:blank-ddpm-softmax]]\n\n        # 5. Value에 Attention 가중치 적용\n        # (B, heads, HW, HW) * (B, heads, head_dim, HW) -> (B, heads, HW, head_dim)\n        out = torch.einsum(\"b h i j, b h d j -> b h i d\", attn, v)\n\n        # 6. 다시 2D 이미지 형태로 복원 (Reshape)\n        # (B, heads, HW, head_dim) -> (B, heads*head_dim, H, W)\n        out = rearrange(out, \"b h (x y) d -> b (h d) x y\", x=h, y=w)\n        return self.to_out(out)\n\n\n\nclass LinearAttention(nn.Module):\n    \"\"\"\n    Attention의 계산량을 줄인 효율적인 버전입니다.\n    표준 Attention이 (픽셀수 * 픽셀수)만큼 계산할 때, 이 방식은 (픽셀수 * 채널수)만큼만 계산합니다.\n    고해상도 특징 맵(U-Net의 앞부분)에서 주로 사용됩니다.\n      - N×N 행렬을 만들지 말자 (O(N^2)\n      - 결합 법칙으로 계산 순서를 바꾸자 (O(Nd)\n    Attn(Q,K,V) ≈ ϕ(Q)(ϕ(K)^T V)\n      - ϕ(Q)=softmax(Q over feature dim)\n      - ϕ(K)=softmax(K over token dim)    \n    \"\"\"\n    def __init__(self, dim, heads=4, dim_head=32):\n        super().__init__()\n        self.scale = dim_head ** -0.5\n        self.heads = heads\n        hidden_dim = dim_head * heads\n\n        self.to_qkv = nn.Conv2d(dim, hidden_dim * 3, 1, bias=False)\n        self.to_out = nn.Sequential(\n            nn.Conv2d(hidden_dim, dim, 1),\n            nn.GroupNorm(1, dim), # 정규화 추가로 학습 안정화\n        )\n\n    def forward(self, x):\n        # x shape: (batch, dim, height, width) \n        b, c, h, w = x.shape\n\n        # 1. Q, K, V 생성 (1x1 Conv 사용)\n        # qkv shape: 3개의 (batch, hidden_dim, height, width) 덩어리\n        qkv = self.to_qkv(x).chunk(3, dim=1)\n\n        # 2. 헤드 분리 및 2D 이미지를 1D 시퀀스로 변환 (Flatten)\n        # q, k, v 각각의 shape: (batch, heads, dim_head, pixels)\n        q, k, v = map(\n            lambda t: rearrange(t, \"b (h c) x y -> b h c (x y)\", h=self.heads), qkv\n        )\n\n        # 3. Q, K 정규화 (Linear Attention의 핵심)\n        # q shape: (b, h, d, n) - 특징(d) 차원에 대해 softmax\n        # k shape: (b, h, d, n) - 픽셀(n) 차원에 대해 softmax\n        q = q.softmax(dim=-2)\n        k = k.softmax(dim=-1)\n        q = q * self.scale\n\n        # 4. K와 V를 먼저 결합하여 '글로벌 컨텍스트' 생성 (결합 법칙 활용)\n        # 연산: (b, h, dim_head, pixels) @ (b, h, dim_head, pixels)^T\n        # 결과 shape: (batch, heads, dim_head, dim_head)\n        # ★ 중요: 픽셀 수가 사라지고 특징 차원만 남음! (계산량 급감)\n        context = torch.einsum(\"b h d n, b h e n -> b h d e\", k, v)\n\n        # 5. 생성된 컨텍스트 정보를 Q에 적용\n        # 연산: (b, h, dim_head, dim_head) @ (b, h, dim_head, pixels)\n        # 결과 shape: (batch, heads, dim_head, pixels)\n        out = torch.einsum(\"b h d e, b h d n -> b h e n\", context, q)\n\n        # 6. 이미지 공간 형태로 다시 복원 (Reshape)\n        # out shape: (batch, hidden_dim, height, width)\n        out = rearrange(out, \"b h c (x y) -> b (h c) x y\", h=self.heads, x=h, y=w)\n\n        # 7. 최종 출력 투영 및 정규화\n        return self.to_out(out)\n\n\nclass PreNorm(nn.Module):\n    \"\"\"\n    '정규화 후 연산' 방식을 구현한 래퍼(Wrapper)입니다.\n    Attention이나 FeedForward 레이어 직전에 GroupNorm을 배치하여 그래디언트 흐름을 돕고 깊은 신경망의 학습을 용이하게 합니다.\n    timestep t마다 feature 분포가 완전히 다름 -> PreNorm으로 입력 분포를 먼저 정규화\n    \"\"\"\n    def __init__(self, dim, fn):\n        super().__init__()\n        self.norm = nn.GroupNorm(1, dim) # 채널 전체를 하나의 그룹으로 정규화 (LayerNorm과 유사)\n        self.fn = fn # 감싸질 실제 함수 (Attention 등)\n\n    def forward(self, x):\n        # 1. 먼저 정규화를 수행하고\n        # 2. 그 결과를 본래의 함수(fn)에 전달함\n        return self.fn(self.norm(x))"}, {"number": 11, "code_number": null, "type": "markdown", "source": "## 6. U-Net 구현\n\n이제까지 만든 구성 요소를 써서 **U-Net 기반 노이즈 예측 네트워크**를 정의합니다.\n\n### 6.1 입력 / 출력 형태\n\n- **입력**\n  - $x_t$: `(batch, channels, height, width)` 형태의 noised image\n  - $t$: `(batch,)` 형태의 time step (정수)\n- **출력**\n  - $\\hat{\\epsilon}_\\theta(x_t, t)$: 입력에 추가된 **노이즈 텐서**와 동일한 shape\n\n### 6.2 네트워크 구조 개략\n\n1. **초기 Conv**로 채널 수를 base dimension으로 맞춤\n2. **여러 단계의 Downsampling**:\n   - `ResnetBlock` $\\rightarrow$ `ResnetBlock` $\\rightarrow$ `LinearAttention` $\\rightarrow$ `Downsample`\n3. **중간(Middle) block**:\n   - `ResnetBlock` $\\rightarrow$ `Attention` $\\rightarrow$ `ResnetBlock`\n4. **여러 단계의 Upsampling**:\n   - `ResnetBlock` $\\rightarrow$ `ResnetBlock` $\\rightarrow$ `LinearAttention` $\\rightarrow$ `Upsample`\n5. **최종 ResNet block + $1 \\times 1$ Conv**로 출력 채널 수를 맞춤\n\n> **Skip connection**은 down path에서 저장한 feature들을 up path에서 `concat` 하는 방식으로 사용합니다."}, {"number": 12, "code_number": 5, "type": "code", "source": "class Unet(nn.Module):\n    def __init__(\n        self,\n        dim,                         # 기본 채널 차원 (예: 64)\n        init_dim=None,               # 초기 컨볼루션 출력 차원\n        out_dim=None,                # 최종 출력 채널 수 (보통 입력 채널과 동일)\n        dim_mults=(1, 2, 4, 8),      # 단계별 채널 배수 (dim * m)\n        channels=3,                  # 입력 이미지 채널 (RGB=3)\n        self_condition=False,        # Self-conditioning 사용 여부\n        resnet_block_groups=8,       # GroupNorm에서 사용할 그룹 수\n    ):\n        super().__init__()\n\n        # 1. 채널 설정 및 초기화\n        self.channels = channels\n        self.self_condition = self_condition\n        # self_condition이 True면 이전 단계 예측값을 입력에 합치므로 채널이 2배가 됨\n        input_channels = channels * (2 if self_condition else 1)\n\n        init_dim = default(init_dim, dim)\n        self.init_conv = nn.Conv2d(input_channels, init_dim, 1, padding=0)\n\n        # 2. 단계별 매추차원 계산 (예: [64, 64, 128, 256, 512])\n        dims = [init_dim, *map(lambda m: dim * m, dim_mults)]\n        # 각 단계의 입력/출력 채널 쌍 생성 (예: [(64, 64), (64, 128), (128, 256), (256, 512)])\n        in_out = list(zip(dims[:-1], dims[1:]))\n\n        # ResnetBlock 설정을 고정 (groups 파라미터 미리 지정)\n        block = partial(ResnetBlock, groups=resnet_block_groups)\n\n        # 3. 타임 임베딩 (Time Embedding) 생성부\n        # 숫자인 t를 고차원 벡터로 바꾼 후 MLP를 통해 특징 추출\n        time_dim = dim * 4\n        self.time_mlp = nn.Sequential(\n            SinusoidalPositionEmbeddings(dim), # 1. 숫자를 사인/코사인 파동 벡터로 변환\n            nn.Linear(dim, time_dim),          # 2. 차원 확장\n            nn.GELU(),                         # 3. 활성화 함수\n            nn.Linear(time_dim, time_dim),     # 4. 최종 임베딩 벡터 완성\n        )\n\n        # 4. Downsampling Path (인코더)\n        self.downs = nn.ModuleList([])\n        num_resolutions = len(in_out)\n\n        for i, (dim_in, dim_out) in enumerate(in_out):\n            is_last = i == (num_resolutions - 1)\n            self.downs.append(\n                nn.ModuleList(\n                    [\n                        block(dim_in, dim_in, time_emb_dim=time_dim), # ResNet 블록 1\n                        block(dim_in, dim_in, time_emb_dim=time_dim), # ResNet 블록 2\n                        Residual(PreNorm(dim_in, LinearAttention(dim_in))), # 효율적인 선형 어텐션\n                        # 마지막 단계가 아니면 해상도를 줄이고(Downsample), 맞으면 채널만 변경\n                        Downsample(dim_in, dim_out) if not is_last else nn.Conv2d(dim_in, dim_out, 3, padding=1),\n                    ]\n                )\n            )\n\n        # 5. Middle Block (최하단 보틀넥)\n        mid_dim = dims[-1]\n        self.mid_block1 = block(mid_dim, mid_dim, time_emb_dim=time_dim)\n        self.mid_attn = Residual(PreNorm(mid_dim, Attention(mid_dim))) # 여기서 정밀한 표준 어텐션 사용\n        self.mid_block2 = block(mid_dim, mid_dim, time_emb_dim=time_dim)\n\n        # 6. Upsampling Path (디코더)\n        self.ups = nn.ModuleList([])\n        for i, (dim_in, dim_out) in enumerate(reversed(in_out)):\n            is_last = i == (len(in_out) - 1)\n            self.ups.append(\n                nn.ModuleList(\n                    [\n                        # Skip connection으로 인해 채널이 dim_out + dim_in으로 들어옴 (Concat 결과)\n                        block(dim_out + dim_in, dim_out, time_emb_dim=time_dim),\n                        block(dim_out + dim_in, dim_out, time_emb_dim=time_dim),\n                        Residual(PreNorm(dim_out, LinearAttention(dim_out))),\n                        # 마지막 단계가 아니면 해상도를 키움(Upsample)\n                        Upsample(dim_out, dim_in) if not is_last else nn.Conv2d(dim_out, dim_in, 3, padding=1),\n                    ]\n                )\n            )\n\n        # 7. 최종 출력층\n        self.out_dim = default(out_dim, channels)\n        self.final_res_block = block(dim * 2, dim, time_emb_dim=time_dim)\n        self.final_conv = nn.Conv2d(dim, self.out_dim, 1)\n\n    def forward(self, x, time, x_self_cond=None):\n        # [0] Self-conditioning 처리: 이전 단계 예측값을 현재 입력 채널에 결합\n        if self.self_condition:\n            x_self_cond = default(x_self_cond, lambda: torch.zeros_like(x))\n            x = torch.cat([x_self_cond, x], dim=1)\n\n        # [1] 초기 컨볼루션 및 원본 특징 복사 (최종 Skip connection용)\n        x = self.init_conv(x)\n        r = x.clone()\n\n        # [2] 타임 임베딩 계산: (batch,) -> (batch, time_dim)\n        t = self.time_mlp(time)\n\n        # Skip Connection 데이터를 담을 리스트\n        h = []\n\n        # [3] Down path (이미지를 작게 줄이며 특징 추출)\n        for block1, block2, attn, downsample in self.downs:\n            x = block1(x, t)\n            h.append(x)          # Skip connection을 위해 특징 저장 1\n\n            x = block2(x, t)\n            h.append(x)          # Skip connection을 위해 특징 저장 2\n\n            x = attn(x)          # 글로벌 정보 참조\n            x = downsample(x)    # 해상도 감소 (H, W -> H/2, W/2)\n\n        # [4] Middle (보틀넥: 가장 추상화된 특징 연산)\n        x = self.mid_block1(x, t)\n        x = self.mid_attn(x)\n        x = self.mid_block2(x, t)\n\n        # [5] Up path (이미지를 다시 키우며 Skip Connection 결합)\n        for block1, block2, attn, upsample in self.ups:\n            # h.pop()으로 가장 나중에 저장된(가장 작은 해상도) 특징부터 꺼내서 결합\n            x = torch.cat([x, h.pop()], dim=1) # 채널 결합 (Concat)\n            x = block1(x, t)\n\n            x = torch.cat([x, h.pop()], dim=1) # 채널 결합 (Concat)\n            x = block2(x, t)\n\n            x = attn(x)\n            x = upsample(x)      # 해상도 복구 (H, W -> 2H, 2W)\n\n        # [6] Final (원본 크기 복구 및 최종 노이즈 예측)\n        x = torch.cat([x, r], dim=1)       # 최초의 특징(r)까지 결합\n        x = self.final_res_block(x, t)\n        return self.final_conv(x)          # (B, out_dim, H, W)"}, {"number": 13, "code_number": null, "type": "markdown", "source": "## 7. Forward Diffusion Process 정의\n\n### 7.1 Beta Schedule\n\n- Forward 과정(노이즈 주입 과정)은 다음과 같은 형태를 가집니다.\n\n$$q(x_t \\mid x_{t-1}) = \\mathcal{N}(\\sqrt{1 - \\beta_t} x_{t-1},\\; \\beta_t \\mathbf{I})$$\n\n- 여기서 $\\beta_t$는 **시간에 따라 증가하는 노이즈의 분산**입니다.\n- DDPM 논문에서는 주로 **linear schedule**을 사용했습니다.\n- 추가로 quadratic, sigmoid, cosine schedule 등도 사용할 수 있습니다.\n\n### 7.2 $q(x_t \\mid x_0)$의 닫힌 형태 (Closed-form)\n\n- 마르코프 체인(Markov Chain)의 성질을 이용해 식을 정리하면, 중간 단계를 거치지 않고 $x_0$에서 임의의 시점 $t$의 이미지를 바로 얻을 수 있습니다.\n\n$$\\bar{\\alpha}_t = \\prod_{s=1}^t (1 - \\beta_s)$$\n\n$$q(x_t \\mid x_0) = \\mathcal{N}(x_t; \\sqrt{\\bar{\\alpha}_t} x_0, (1 - \\bar{\\alpha}_t) \\mathbf{I})$$\n\n\n\n- 이 식이 있어, 학습 시에는 $x_0$에서 **한 번에 $x_t$를 샘플링**할 수 있어 매우 효율적입니다."}, {"number": 14, "code_number": 6, "type": "code", "source": "import torch\nimport torch.nn.functional as F\n\n# -------------------------------------------------------------------\n# 1. 다양한 Beta Schedule 함수들\n# 목적: 시간이 흐름(t)에 따라 노이즈를 얼마나 강하게 주입할지 결정합니다.\n# -------------------------------------------------------------------\n\ndef cosine_beta_schedule(timesteps, s=0.008):\n    \"\"\"\n    OpenAI의 'Improved DDPM' 논문에서 제안된 코사인 스케줄입니다.\n    Linear 스케줄보다 노이즈가 더 천천히 주입되어 이미지의 세부 정보를 더 잘 보존합니다.\n    βt = 1 − ( αˉt / αˉt−1 )\n    \"\"\"\n    steps = timesteps + 1\n    # 0부터 timesteps까지 일정한 간격으로 생성\n    x = torch.linspace(0, timesteps, steps)\n\n    # alphas_cumprod(알파 바)를 코사인 함수로 먼저 정의합니다.\n    # αˉt = cos^2((t/T+s)/(1+s) * π/2)\n    alphas_cumprod = torch.cos(((x / timesteps) + s) / (1 + s) * torch.pi * 0.5) ** 2\n    alphas_cumprod = alphas_cumprod / alphas_cumprod[0] # 시작 값을 1로 정규화\n\n    # betas = 1 - (현재 알파바 / 이전 알파바) 수식을 통해 역산합니다.\n    betas = 1 - (alphas_cumprod[1:] / alphas_cumprod[:-1])\n\n    # 수치적 안정성을 위해 최소/최대 값을 제한합니다.\n    return torch.clip(betas, 0.0001, 0.9999)\n\ndef linear_beta_schedule(timesteps):\n    \"\"\"가장 기본적인 선형 스케줄. 노이즈 강도가 t에 따라 일정하게 증가합니다.\"\"\"\n    beta_start = 0.0001\n    beta_end = 0.02\n    return torch.linspace(beta_start, beta_end, timesteps)\n\ndef quadratic_beta_schedule(timesteps):\n    \"\"\"노이즈가 초반에는 천천히, 후반에는 급격하게 증가하는 제곱 스케줄입니다.\"\"\"\n    beta_start = 0.0001\n    beta_end = 0.02\n    return torch.linspace(beta_start**0.5, beta_end**0.5, timesteps) ** 2\n\ndef sigmoid_beta_schedule(timesteps):\n    \"\"\"S자 곡선 형태로 노이즈를 주입합니다. 초반과 후반은 완만하고 중간은 급격합니다.\"\"\"\n    beta_start = 0.0001\n    beta_end = 0.02\n    betas = torch.linspace(-6, 6, timesteps)\n    return torch.sigmoid(betas) * (beta_end - beta_start) + beta_start\n\n# -------------------------------------------------------------------\n# 2. Diffusion 수식에 필요한 계수(Constants) 계산부\n# -------------------------------------------------------------------\n\ntimesteps = 300 # 전체 타임스텝 (T)\nbetas = linear_beta_schedule(timesteps=timesteps) # Beta_t\n\n# alpha_t = 1 - beta_t\nalphas = 1.0 - betas\n\n# alpha_cumprod (알파 바_t): t시점까지의 모든 alpha를 곱한 값\n# x_0에서 바로 x_t를 샘플링할 때 사용됩니다.\nalphas_cumprod = torch.cumprod(alphas, dim=0)\n\n# alpha_cumprod_prev (알파 바_{t-1}): 이전 단계의 누적 곱\n# 마지막 값을 제외하고 맨 앞에 1.0을 패딩하여 (T,) 크기를 유지합니다.\nalphas_cumprod_prev = F.pad(alphas_cumprod[:-1], (1, 0), value=1.0)\n\n# -------------------------------------------------------------------\n# 3. Forward/Reverse 과정에서 반복 사용될 수식들 미리 계산\n# -------------------------------------------------------------------\n\n# 1 / sqrt(alpha_t): 샘플링 시 x_t에서 노이즈 성분을 뺄 때 사용\nsqrt_recip_alphas = torch.sqrt(1.0 / alphas)\n\n# sqrt(alpha_bar_t): x_0의 계수 (Mean 성분)\nsqrt_alphas_cumprod = torch.sqrt(alphas_cumprod)\n\n# sqrt(1 - alpha_bar_t): 노이즈(epsilon)의 계수 (Variance 성분)\nsqrt_one_minus_alphas_cumprod = torch.sqrt(1.0 - alphas_cumprod)\n\n# posterior_variance: 역과정(Reverse Process)에서 사용하는 분산 식\n# q(x_{t-1} | x_t, x_0)의 분산을 계산할 때 쓰입니다.\nposterior_variance = betas * (1.0 - alphas_cumprod_prev) / (1.0 - alphas_cumprod)\n\n# -------------------------------------------------------------------\n# 4. 유틸리티 함수\n# -------------------------------------------------------------------\n# 1차원 리스트에서 필요한 값만 뽑아서,\n# 이미지 텐서와 바로 곱할 수 있도록 빈 차원(1)을 붙여주는 '어댑터' 같은 함수\ndef extract(a, t, x_shape):\n    \"\"\"\n    1차원 텐서(a)에서 현재 배치 멤버들의 타임스텝(t)에 해당하는 값만 골라내어\n    이미지 텐서 연산이 가능하도록 차원을 맞춰줍니다.\n\n    Args:\n        a: 미리 계산된 계수 리스트 (예: sqrt_alphas_cumprod)\n        t: 현재 배치의 타임스텝들 (Batch_size,)\n        x_shape: 현재 이미지의 shape (Batch, Channel, H, W)\n\n    Returns:\n        (Batch, 1, 1, 1) 형태의 텐서\n    \"\"\"\n    batch_size = t.shape[0]\n    \n    # t에 해당하는 인덱스 값들을 추출 : [ a[t[0]], a[t[1]], ... ]\n    out = a.gather(-1, t.cpu())\n    \n    # 이미지 차원(4D)에 맞춰 뒤에 1들을 추가: (B,) -> (B, 1, 1, 1)\n    return out.reshape(batch_size, *((1,) * (len(x_shape) - 1))).to(t.device)"}, {"number": 15, "code_number": null, "type": "markdown", "source": "## 8. Forward Diffusion 시각화 (예시 이미지)\n\nDDPM이 실제로 어떤 식으로 이미지를 **노이즈로 바꿔가는지** 직관적으로 보기 위해 간단한 예시 이미지를 사용합니다.\n\n1. 인터넷에서 샘플 이미지를 하나 불러온 뒤 (예: COCO 예시)\n2. 이미지를 $[-1, 1]$ 범위의 텐서로 변환 (모델의 입력 규격에 맞춤)\n3. 여러 타임스텝 $t$에 대해 $x_t \\sim q(x_t \\mid x_0)$를 샘플링\n4. 각 단계의 이미지를 플롯(Plot)으로 비교하여 노이즈 강도 변화 관찰\n\n\n\n> **Notice:** Diffusion의 기본 개념(정보의 점진적 파괴)을 직관적으로 이해할 수 있음."}, {"number": 16, "code_number": 7, "type": "code", "source": "import numpy as np\nimport matplotlib.pyplot as plt\nfrom PIL import Image\nimport requests\nimport torch\nfrom torchvision.transforms import Compose, ToTensor, Lambda, CenterCrop, Resize, ToPILImage\n\n# -------------------------------------------------------------------\n# 1. 샘플 이미지 준비 및 전처리 (Preprocessing)\n# -------------------------------------------------------------------\n\n# 1) 웹에서 이미지 불러오기 (COCO 데이터셋의 고양이 이미지)\nurl = \"http://images.cocodataset.org/val2017/000000039769.jpg\"\nimage = Image.open(requests.get(url, stream=True).raw)\n\nimage_size = 128\n\n# 2) 전처리 파이프라인 구성\n# Diffusion 모델은 일반적으로 [-1, 1] 범위의 데이터를 사용합니다.\ntransform = Compose([\n    Resize(image_size),           # 짧은 쪽을 128로 조절\n    CenterCrop(image_size),       # 중앙을 128x128로 크롭\n    ToTensor(),                   # PIL 이미지를 [0, 1] 범위의 텐서로 변환 (C, H, W)\n    Lambda(lambda t: (t * 2) - 1),# [0, 1] 범위를 [-1, 1] 범위로 선형 변환\n])\n\n# x_start: 원본 이미지(x0). 모델 입력용 배치를 위해 (1, 3, 128, 128)로 확장\nx_start = transform(image).unsqueeze(0)\n\n# 3) 역전처리 파이프라인 (모델 출력인 텐서를 다시 눈으로 볼 수 있는 이미지로)\nreverse_transform = Compose([\n    Lambda(lambda t: (t + 1) / 2),           # [-1, 1] -> [0, 1]\n    Lambda(lambda t: t.permute(1, 2, 0)),    # (C, H, W) -> (H, W, C) 순서 변경\n    Lambda(lambda t: (t * 255.).clamp(0, 255)), # [0, 1] -> [0, 255] 정규화 및 클리핑\n    Lambda(lambda t: t.numpy().astype(np.uint8)), # 텐서를 넘파이(uint8) 배열로 변환\n    ToPILImage(),                            # 배열을 다시 PIL 이미지 객체로 변환\n])\n\n# -------------------------------------------------------------------\n# 2. Forward Diffusion 수식 구현 (q_sample)\n# -------------------------------------------------------------------\n\ndef q_sample(x_start, t, noise=None):\n    \"\"\"\n    수식: q(x_t | x_0) = sqrt(alpha_bar_t) * x_0 + sqrt(1 - alpha_bar_t) * epsilon\n    이 수식은 x_0에서 중간 단계 없이 즉시 x_t를 샘플링하게 해줍니다.\n    \"\"\"\n    if noise is None:\n        # 가우시안 노이즈(epsilon) 생성\n        noise = torch.randn_like(x_start)\n\n    # 미리 계산해둔 계수 리스트에서 현재 t에 해당하는 계수를 (B, 1, 1, 1)로 추출\n    sqrt_alphas_cumprod_t = extract(sqrt_alphas_cumprod, t, x_start.shape)\n    sqrt_one_minus_alphas_cumprod_t = extract(\n        sqrt_one_minus_alphas_cumprod, t, x_start.shape\n    )\n\n    # 수식 그대로 계산 (원본의 강도는 줄이고, 노이즈의 강도는 키우고)\n    return sqrt_alphas_cumprod_t * x_start + sqrt_one_minus_alphas_cumprod_t * noise\n\ndef get_noisy_image(x_start, t):\n    \"\"\"특정 타임스텝 t의 노이즈 섞인 이미지를 시각화 가능한 이미지로 반환\"\"\"\n    x_noisy = q_sample(x_start, t=t)\n    return reverse_transform(x_noisy.squeeze()) # 배치 차원을 제거하고 이미지로 변환\n\n# -------------------------------------------------------------------\n# 3. 시각화 함수 (여러 단계를 한눈에 비교)\n# -------------------------------------------------------------------\n\ndef plot(imgs, with_orig=False, row_title=None, **imshow_kwargs):\n    \"\"\"이미지 리스트를 격자 형태로 출력하는 함수\"\"\"\n    if not isinstance(imgs[0], list):\n        imgs = [imgs]\n\n    num_rows = len(imgs)\n    num_cols = len(imgs[0]) + (1 if with_orig else 0)\n\n    fig, axs = plt.subplots(\n        figsize=(2 * num_cols, 2 * num_rows),\n        nrows=num_rows, ncols=num_cols, squeeze=False,\n    )\n\n    for row_idx, row in enumerate(imgs):\n        row = [image] + row if with_orig else row # 원본 포함 여부\n        for col_idx, img in enumerate(row):\n            ax = axs[row_idx, col_idx]\n            ax.imshow(np.asarray(img), **imshow_kwargs)\n            ax.set(xticklabels=[], yticklabels=[], xticks=[], yticks=[])\n\n    if with_orig:\n        axs[0, 0].set(title=\"Original\")\n    if row_title is not None:\n        for row_idx in range(num_rows):\n            axs[row_idx, 0].set(ylabel=row_title[row_idx])\n    plt.tight_layout()\n\n# 실행: 타임스텝 0, 50, 100, 150, 250에서의 변화 시각화\n# t가 커질수록(오른쪽으로 갈수록) 이미지가 파괴되어 노이즈로 변하는 과정을 확인하세요.\nplot(\n    [get_noisy_image(x_start, torch.tensor([t])) for t in [0, 50, 100, 150, 250]],\n    with_orig=True\n)"}, {"number": 17, "code_number": null, "type": "markdown", "source": "## 9. 학습 손실 함수 정의\n\nDDPM의 학습 목표는 **신경망($\\theta$)이 이미지에 추가된 노이즈 $\\epsilon$을 얼마나 정확하게 예측하는가**를 측정하는 것입니다.\n\n### 학습 알고리즘 단계\n\n1. 데이터셋에서 깨끗한 이미지 $x_0$를 샘플링합니다.\n2. 전체 타임스텝 중 임의의 시점 $t \\sim \\text{Uniform}(1, \\dots, T)$를 샘플링합니다.\n3. 이미지에 섞을 가우시안 노이즈 $\\epsilon \\sim \\mathcal{N}(0, \\mathbf{I})$를 생성합니다.\n4. $x_0$와 $\\epsilon$을 사용하여 노이즈가 섞인 이미지 $x_t$를 계산합니다.\n   $$x_t = \\sqrt{\\bar{\\alpha}_t} x_0 + \\sqrt{1-\\bar{\\alpha}_t} \\epsilon$$\n5. U-Net 네트워크에 $x_t$와 타임스텝 $t$를 입력하여 예측 노이즈 $\\hat{\\epsilon}_\\theta(x_t, t)$를 얻습니다.\n6. 실제 노이즈 $\\epsilon$과 예측된 노이즈 $\\hat{\\epsilon}_\\theta$ 사이의 **MSE(L2)** 또는 **Huber Loss**를 최소화합니다.\n   $$\\mathcal{L} = \\| \\epsilon - \\hat{\\epsilon}_\\theta(x_t, t) \\|^2$$\n\n\n\n> 이 과정을 코드로 구현한 것이 `p_losses` 함수이며, 모델은 이 손실 함수를 통해 이미지에서 노이즈 성분만 골라내는 방법을 배우게 됩니다."}, {"number": 18, "code_number": 8, "type": "code", "source": "import torch.nn.functional as F\n\ndef p_losses(denoise_model, x_start, t, noise=None, loss_type=\"l1\"):\n    \"\"\"\n    Diffusion 모델을 학습시키기 위한 손실 함수(Loss Function) 계산 함수입니다.\n    네트워크(U-Net)가 이미지에 섞인 노이즈를 얼마나 잘 찾아내는지 측정합니다.\n    \"\"\"\n\n    # 1. 실제 노이즈(Ground Truth Noise) 생성\n    # 이미지와 동일한 크기의 가우시안 노이즈(epsilon)를 만듭니다.\n    if noise is None:\n        noise = torch.randn_like(x_start)\n\n    # 2. Forward Diffusion: x_0(원본)에서 x_t(노이즈 섞인 상태) 계산\n    # 앞서 정의한 q_sample 함수를 사용하여 t 시점의 노이즈 이미지 x_t를 얻습니다.\n    # 수식: x_t = sqrt(alpha_bar_t) * x_start + sqrt(1 - alpha_bar_t) * noise\n    x_noisy = q_sample(x_start=x_start, t=t, noise=noise)\n\n    # 3. 모델의 예측\n    # U-Net 모델(denoise_model)에 '노이즈 이미지(x_noisy)'와 '타임스텝(t)'을 입력합니다.\n    # 모델은 \"이 이미지의 이 타임스텝에는 어떤 노이즈가 섞여 있어?\"라는 질문에 답합니다.\n    predicted_noise = denoise_model(x_noisy, t)\n\n    # 4. 손실(Loss) 계산\n    # 실제로 섞은 노이즈(noise)와 모델이 예측한 노이즈(predicted_noise) 사이의 오차를 구합니다.\n    if loss_type == \"l1\":\n        # L1 Loss: 오차의 절댓값 평균 (이상치에 상대적으로 강건함)\n        loss = F.l1_loss(noise, predicted_noise)\n    elif loss_type == \"l2\":\n        # L2 Loss (MSE): 오차의 제곱 평균 (표준적인 DDPM 학습 방식)\n        loss = F.mse_loss(noise, predicted_noise)\n    elif loss_type == \"huber\":\n        # Huber Loss (Smooth L1): L1과 L2의 장점을 섞은 방식 (학습 안정화에 도움)\n        loss = F.smooth_l1_loss(noise, predicted_noise)\n    else:\n        raise NotImplementedError(\"지원하지 않는 loss_type입니다.\")\n\n    return loss"}, {"number": 19, "code_number": null, "type": "markdown", "source": "## 10. Fashion-MNIST 데이터셋 로드 및 전처리\n\n실습에서는 **Hugging Face `fashion_mnist` 데이터셋**을 사용합니다.\n\n- 28x28 grayscale 이미지 (의류/신발 등 10 클래스)\n- 여기서는 **레이블은 사용하지 않고**, 오직 이미지 분포만 학습합니다.\n- 이미지를 \\([-1, 1]\\) 범위의 텐서로 변환하고, 간단한 augmentation으로 random horizontal flip을 적용합니다."}, {"number": 20, "code_number": 9, "type": "code", "source": "# -------------------------------------------------------------------\n# 1. Fashion-MNIST 데이터셋 로드\n# -------------------------------------------------------------------\n# Hugging Face의 datasets 라이브러리를 사용하여 데이터를 불러옵니다.\n# 이 데이터셋은 28x28 해상도의 흑백 의류 이미지들로 구성되어 있습니다.\ndataset = load_dataset(\"fashion_mnist\")\n\n# 하이퍼파라미터 설정\nimage_size = 28  # Fashion-MNIST의 기본 크기\nchannels = 1    # 흑백(Grayscale) 이미지이므로 채널은 1\nbatch_size = 128\n\n# -------------------------------------------------------------------\n# 2. 이미지 전처리(Augmentation & Normalization) 정의\n# -------------------------------------------------------------------\ntransform = transforms.Compose([\n    # 좌우 반전을 무작위로 적용하여 데이터의 다양성을 확보합니다.\n    transforms.RandomHorizontalFlip(),\n\n    # PIL 이미지를 PyTorch 텐서로 변환하며 값을 [0, 1] 범위로 정규화합니다.\n    transforms.ToTensor(),\n\n    # Diffusion 모델의 안정적인 학습을 위해 [0, 1] 범위를 [-1, 1] 범위로 변환합니다.\n    # 수식: (0 * 2) - 1 = -1, (1 * 2) - 1 = 1\n    transforms.Lambda(lambda t: (t * 2) - 1),\n])\n\n# -------------------------------------------------------------------\n# 3. 데이터셋 변환 함수 정의 (Hugging Face 전용)\n# -------------------------------------------------------------------\ndef transform_examples(examples):\n    \"\"\"\n    Hugging Face 데이터셋의 각 샘플에 전처리를 적용하는 함수입니다.\n    \"\"\"\n    # 1. 원본 'image' 열에서 이미지를 꺼내 'L'(흑백) 모드로 확실히 변환 후 전처리 적용\n    # 2. 전처리된 결과를 'pixel_values'라는 새로운 키에 리스트 형태로 저장\n    #    - 변환 전 : {\"image\": [PIL객체1, PIL객체2, ...], \"label\": [9, 0, ...]}\n    #    - 변환 후 : {\"pixel_values\": [텐서1, 텐서2, ...], \"image\": [PIL객체1, PIL객체2, ...], \"label\": [9, 0, ...]}\n    examples[\"pixel_values\"] = [transform(image.convert(\"L\")) for image in examples[\"image\"]]\n\n    # 메모리 절약을 위해 원본 이미지 데이터는 삭제합니다.\n    del examples[\"image\"]\n    return examples\n\n# 데이터셋에 변환 함수를 등록하고, 사용하지 않는 'label' 열은 제거합니다.\n# (이 실습은 비지도 학습인 Diffusion이므로 레이블이 필요 없습니다.)\ntransformed_dataset = dataset.with_transform(transform_examples).remove_columns(\"label\")\n\n# -------------------------------------------------------------------\n# 4. DataLoader 구축 (Batch 생성)\n# -------------------------------------------------------------------\n# 실제 학습 루프에서 데이터를 효율적으로 공급하기 위해 DataLoader를 사용합니다.\ndataloader = DataLoader(\n    transformed_dataset[\"train\"],\n    batch_size=batch_size,\n    shuffle=True,  # 매 에포크마다 데이터를 섞어 학습 성능을 높입니다.\n    drop_last=True # 배치 사이즈에 맞지 않는 마지막 자투리 데이터는 버립니다.\n)\n\n# 데이터 로드가 잘 되었는지 첫 번째 배치를 확인해봅니다.\nbatch = next(iter(dataloader))\n# 결과 출력: (배치 사이즈, 채널, 높이, 너비) -> (128, 1, 28, 28)\nprint(f\"Batch shape: {batch['pixel_values'].shape}\")"}, {"number": 21, "code_number": null, "type": "markdown", "source": "## 11. Reverse Diffusion (Sampling) 정의\n\n이제 학습이 끝났다고 가정하고, **아무것도 없는 노이즈(Pure Noise)에서 시작해서 이미지를 생성하는 과정**을 정의합니다.\n\n### 11.1 한 스텝 샘플링: `p_sample`\n\n현재 단계의 이미지 $x_t$에서 이전 단계의 이미지 $x_{t-1}$를 추론하는 과정은 다음과 같은 확률 분포를 따릅니다.\n\n$$p_\\theta(x_{t-1} \\mid x_t) = \\mathcal{N}(x_{t-1}; \\mu_\\theta(x_t, t), \\sigma_t^2 \\mathbf{I})$$\n\n- 여기서 $\\mu_\\theta$는 네트워크가 예측한 **노이즈($\\hat{\\epsilon}_\\theta$)**를 이용하여 계산된 평균입니다.\n- $\\sigma_t^2$는 미리 정의된 노이즈의 분산입니다.\n- 모델은 한 번에 이미지를 완성하는 것이 아니라, $x_t$에서 노이즈 성분을 살짝 제거하여 조금 더 선명한 $x_{t-1}$을 만듭니다.\n\n### 11.2 전체 샘플링 루프 (Reverse Process)\n\n전체 생성 과정은 다음과 같은 순서로 진행됩니다.\n\n1. **시작**: 완전히 무작위인 노이즈 $x_T \\sim \\mathcal{N}(0, \\mathbf{I})$에서 시작합니다.\n2. **반복**: $t = T-1, T-2, \\dots, 0$까지 거꾸로 거슬러 올라가며 `p_sample`을 적용합니다.\n3. **완성**: 최종적으로 노이즈가 모두 제거된 깨끗한 이미지 샘플 $x_0$를 얻습니다.\n\n\n\n> **핵심 포인트:** 학습할 때는 노이즈를 **입히는 법**($\\epsilon$)을 배웠지만, 생성할 때는 배운 지식을 활용해 노이즈를 **걷어내는 법**을 실행하는 것입니다."}, {"number": 22, "code_number": 10, "type": "code", "source": "import torch\nfrom tqdm.auto import tqdm\n\n# -------------------------------------------------------------------\n# 1. p_sample: 현재 단계(t)에서 이전 단계(t-1)의 이미지를 추론\n# -------------------------------------------------------------------\n@torch.no_grad() # 샘플링 시에는 그래디언트 계산이 필요 없으므로 메모리를 절약합니다.\ndef p_sample(model, x, t, t_index):\n    \"\"\"\n    노이즈가 섞인 이미지 x에서 모델이 예측한 노이즈를 빼서\n    조금 더 깨끗한 이전 단계의 이미지를 계산합니다.\n    \"\"\"\n    # 현재 타임스텝 t에 해당하는 미리 계산된 계수들을 가져옵니다.\n    betas_t = extract(betas, t, x.shape)\n    sqrt_one_minus_alphas_cumprod_t = extract(\n        sqrt_one_minus_alphas_cumprod, t, x.shape\n    )\n    sqrt_recip_alphas_t = extract(sqrt_recip_alphas, t, x.shape)\n\n    # [수식 구현] DDPM 논문의 Equation 11: 모델이 예측한 평균(mu) 계산\n    # '현재 이미지'에서 '모델이 예측한 노이즈' 성분을 적절한 비율로 빼주는 과정입니다.\n    # model(x, t)는 U-Net이 예측한 노이즈(epsilon_theta)입니다.\n    model_mean = sqrt_recip_alphas_t * (\n        x - betas_t * model(x, t) / sqrt_one_minus_alphas_cumprod_t\n    )\n\n    if t_index == 0:\n        # 마지막 단계(t=0)라면 평균값을 그대로 반환하여 최종 이미지를 얻습니다.\n        return model_mean\n    else:\n        # t > 0 이라면 계산된 평균에 약간의 무작위 노이즈를 더해줍니다.\n        # 이 과정이 있어야 Langevin Dynamics처럼 확률적으로 데이터 분포를 찾아갑니다.\n        posterior_variance_t = extract(posterior_variance, t, x.shape)\n        noise = torch.randn_like(x) # epsilon ~ N(0, I)\n\n        # 평균 + 표준편차 * 노이즈\n        return model_mean + torch.sqrt(posterior_variance_t) * noise\n\n# -------------------------------------------------------------------\n# 2. p_sample_loop: T부터 0까지 반복하여 이미지 생성\n# -------------------------------------------------------------------\n@torch.no_grad()\ndef p_sample_loop(model, shape):\n    \"\"\"\n    완전한 노이즈에서 시작하여 타임스텝을 거꾸로 거슬러 올라가며\n    최종 이미지를 생성하는 전체 루프입니다.\n    \"\"\"\n    device = next(model.parameters()).device\n    b = shape[0] # 배치 사이즈\n\n    # 1. 시작점: 완전한 가우시안 노이즈 x_T 생성 (Pure Noise)\n    img = torch.randn(shape, device=device)\n    imgs = [] # 변화 과정을 저장할 리스트\n\n    # 2. 역과정 반복: T-1부터 0까지 거꾸로 진행 (Reverse Process)\n    # tqdm을 사용하여 샘플링 진행 상황을 시각적으로 표시합니다.\n    for i in tqdm(reversed(range(0, timesteps)), desc=\"sampling loop time step\", total=timesteps):\n        # p_sample 함수를 호출하여 한 단계 더 깨끗한 이미지를 얻음\n        img = p_sample(\n            model,\n            img,\n            torch.full((b,), i, device=device, dtype=torch.long), # 현재 타임스텝 t를 텐서로 전달\n            i,\n        )\n        # 나중에 애니메이션이나 시각화를 위해 결과 저장\n        imgs.append(img.cpu().numpy())\n\n    return imgs\n\n# -------------------------------------------------------------------\n# 3. sample: 사용자용 최종 호출 함수\n# -------------------------------------------------------------------\n@torch.no_grad()\ndef sample(model, image_size, batch_size=16, channels=1):\n    \"\"\"\n    외부에서 간편하게 이미지를 생성할 때 사용하는 함수입니다.\n    원하는 이미지 크기와 배치 사이즈를 정하면 생성된 이미지 리스트를 반환합니다.\n    \"\"\"\n    return p_sample_loop(model, shape=(batch_size, channels, image_size, image_size))"}, {"number": 23, "code_number": null, "type": "markdown", "source": "## 12. 모델, Optimizer, 결과 저장 폴더 설정\n\n이제까지 정의한 U-Net과 손실 함수를 이용하여 **실제 학습 루프를 돌릴 준비**를 합니다.\n\n- base dimension: `image_size` (28) 을 그대로 사용\n- `dim_mults`: `(1, 2, 4)` 로 해서 U-Net 깊이를 적당히 설정\n- Optimizer: Adam, learning rate = 1e-3"}, {"number": 24, "code_number": 11, "type": "code", "source": "# -------------------------------------------------------------------\n# 1. 결과 저장 경로 설정\n# -------------------------------------------------------------------\n# 학습 중간에 생성된 샘플 이미지와 모델 체크포인트를 저장할 폴더를 지정합니다.\nresults_folder = Path(\"./results_ddpm\")\nresults_folder.mkdir(exist_ok=True) # 폴더가 없으면 생성하고, 있으면 그대로 사용합니다.\n\n# 400 step마다 현재 모델의 성능을 확인하기 위해 이미지를 생성(Sampling)하고 저장합니다.\nsave_and_sample_every = 400\n\n# -------------------------------------------------------------------\n# 2. U-Net 모델 선언\n# -------------------------------------------------------------------\n# Diffusion의 핵심 두뇌인 U-Net 신경망을 생성합니다.\nmodel = Unet(\n    dim=image_size,           # 기본 특징 맵(Feature map)의 크기 (Fashion-MNIST의 경우 28)\n    channels=channels,        # 입력 이미지의 채널 수 (흑백이므로 1)\n    dim_mults=(1, 2, 4),      # Downsampling을 진행하며 채널 수를 몇 배씩 늘릴지 결정 (1배 -> 2배 -> 4배)\n    resnet_block_groups=4,    # Group Normalization에 사용될 그룹 수 (학습 안정화에 도움)\n)\n\n# 모델을 GPU(device)로 이동시켜 연산 속도를 높입니다.\nmodel = model.to(device)\n\n# -------------------------------------------------------------------\n# 3. 최적화 도구(Optimizer) 설정\n# -------------------------------------------------------------------\n# 모델의 가중치를 업데이트할 알고리즘으로 Adam을 사용합니다.\n# lr=1e-3 (0.001)은 모델이 한 번에 얼마나 크게 학습할지를 결정하는 학습률입니다.\noptimizer = torch.optim.Adam(model.parameters(), lr=1e-3)\n\n# -------------------------------------------------------------------\n# 4. 모델 규모 확인\n# -------------------------------------------------------------------\n# 전체 모델의 파라미터(가중치) 개수를 출력하여 모델의 크기를 가늠합니다.\n# numel()은 각 텐서의 요소 개수를 반환합니다.\nprint(\"Model parameters:\", sum(p.numel() for p in model.parameters()))"}, {"number": 25, "code_number": null, "type": "markdown", "source": "## 13. 학습 루프 구현\n\n### 13.1 학습 절차\n\n각 minibatch 에 대해 다음을 수행합니다.\n\n1. 이미지 배치 \\(x_0\\) 를 로드\n2. 각 샘플마다 time step \\(t \\sim \\mathcal{U}({0, \\dots, T-1})\\) 를 샘플링\n3. `p_losses` 를 통해 loss를 계산\n4. 역전파 및 optimizer step\n5. 일정 step마다 `sample` 함수를 호출하여 중간 결과 이미지 저장"}, {"number": 26, "code_number": 12, "type": "code", "source": "# -------------------------------------------------------------------\n# DDPM 메인 학습 루프\n# -------------------------------------------------------------------\n\nepochs = 10  # 전체 데이터셋을 몇 번 반복해서 학습할지 설정\n\nfor epoch in range(epochs):\n    model.train()\n    print(f\"Epoch {epoch + 1}/{epochs}\")\n\n    # DataLoader를 통해 배치 단위로 데이터를 가져옵니다.\n    for step, batch in enumerate(dataloader):\n        # 1. 변화도(Gradient) 초기화: 이전 루프의 기울기 값을 비웁니다.\n        optimizer.zero_grad()\n\n        # 현재 배치의 이미지 개수 확인 및 데이터를 GPU로 전송\n        batch_imgs = batch[\"pixel_values\"].to(device)\n        b = batch_imgs.shape[0]\n\n        # 2. 타임스텝(t) 샘플링: 배치 내 각 이미지마다 서로 다른 t를 무작위로 선택합니다.\n        t = torch.randint(0, timesteps, (b,), device=device, dtype=torch.long)\n\n        # 3. 손실 함수 계산: p_losses 함수 내에서 노이즈 주입(Forward) 및 노이즈 예측이 일어납니다.\n        # 여기서는 오차에 강건한 'huber' loss를 사용합니다.\n        loss = p_losses(model, batch_imgs, t, loss_type=\"huber\")\n\n        # 100 step마다 현재 학습 손실(Loss)을 출력하여 모니터링합니다.\n        if step % 100 == 0:\n            print(f\"   Step {step:05d} | Loss: {loss.item():.4f}\")\n\n        # 4. 역전파(Backpropagation): 손실값으로부터 가중치별 기울기를 계산합니다.\n        loss.backward()\n\n        # 5. 가중치 업데이트: 계산된 기울기를 바탕으로 optimizer가 모델의 파라미터를 수정합니다.\n        optimizer.step()\n\n        # ---- sampling & save ----\n        if step != 0 and step % save_and_sample_every == 0:\n            milestone = step // save_and_sample_every\n\n            was_training = model.training\n            model.eval()\n\n            # 생성할 이미지 개수(4개)를 배치 사이즈에 맞게 그룹화합니다.\n            # (예: 4개를 생성해야 하는데 배치가 128이면 [4]라는 리스트 생성)\n            groups = num_to_groups(num=4, divisor=b)\n\n            # 학습 중인 모델을 사용하여 실제 이미지를 생성해봅니다 (Reverse Diffusion).\n            # sample() 함수의 결과 중 마지막 단계([-1])인 x_0를 가져옵니다.            \n            all_images_list = [\n                sample(model, image_size=image_size, batch_size=n, channels=channels)[-1]\n                for n in groups\n            ]\n\n            # 리스트에 담긴 넘파이 배열들을 하나로 합치고 텐서로 변환합니다.\n            all_images = np.concatenate(all_images_list, axis=0)     # (4,1,28,28) numpy\n            all_images = torch.from_numpy(all_images).float()        # torch float32 CPU\n            # 후처리: 시각화를 위해 [-1, 1] 범위를 다시 [0, 1] 범위로 되돌립니다.\n            all_images = ((all_images + 1) * 0.5).clamp(0, 1)        # [0,1]\n\n            # 생성된 이미지를 파일로 저장하여 학습 진행 상황을 눈으로 확인합니다.\n            save_path = str(results_folder / f\"sample-epoch{epoch+1}-step{step}-milestone{milestone}.png\")\n            save_image(all_images, save_path, nrow=4)\n            print(f\"   [Saved] {save_path}\")\n\n            if was_training:\n                model.train()"}, {"number": 27, "code_number": null, "type": "markdown", "source": "## 14. 학습 후 샘플링 및 시각화\n\n학습이 완료된 후, **모델이 학습한 분포에서 샘플을 생성**해 봅니다.\n\n1. `sample` 함수를 이용해 노이즈에서부터 T step reverse diffusion\n2. 최종 결과 \\(x_0\\) 를 가져와 시각화\n3. 랜덤 샘플 몇 개를 그레이스케일 이미지로 표시"}, {"number": 28, "code_number": 13, "type": "code", "source": "# -------------------------------------------------------------------\n# 1. 이미지 샘플링 실행\n# -------------------------------------------------------------------\n# 학습된 U-Net 모델을 사용하여 64장의 이미지를 동시에 생성합니다.\n# samples는 리스트 형태이며, 각 원소는 [T-1, T-2, ..., 0] 단계의 이미지들입니다.\nsamples = sample(\n    model,\n    image_size=image_size,\n    batch_size=64,\n    channels=channels,\n)\n\n# -------------------------------------------------------------------\n# 2. 결과물 선택 및 후처리\n# -------------------------------------------------------------------\n# samples[-1]은 가장 마지막 단계(t=0)인 최종 완성본입니다. -> (64, 1, 28, 28)\nfinal_samples = samples[-1]\n\n# 넘파이(numpy) 배열 형태인 결과를 연산을 위해 파이토치 텐서로 변환합니다.\nfinal_samples_t = torch.tensor(final_samples)\n\n# 모델의 출력 범위인 [-1, 1]을 시각화 가능한 범위인 [0, 1]로 조정합니다.\nfinal_samples_t = (final_samples_t + 1) * 0.5\n\n\n# -------------------------------------------------------------------\n# 3. 무작위 3장 선택 및 시각화\n# -------------------------------------------------------------------\n# 0~63 사이의 인덱스 중 중복 없이 3개를 무작위로 뽑습니다.\nimport random\nnum_to_show = 3\nrandom_indices = random.sample(range(64), num_to_show)\n\n# 1행 3열의 서브플롯 생성\nfig, axes = plt.subplots(1, num_to_show, figsize=(12, 4))\n\nfor i, idx in enumerate(random_indices):\n    # 이미지 데이터를 (1, 28, 28)에서 (28, 28)로 리셰이프하여 2차원 평면으로 만듭니다.\n    axes[i].imshow(\n        final_samples_t[idx].reshape(image_size, image_size),\n        cmap=\"gray\"  # Fashion-MNIST이므로 흑백 지도를 사용합니다.\n    )\n    axes[i].set_title(f\"Sample Index: {idx}\") # 몇 번째 이미지인지 표시\n    axes[i].axis(\"off\") # 축 숨기기\n\nplt.tight_layout()\nplt.show()"}, {"number": 29, "code_number": null, "type": "markdown", "source": "## 15. Denoising 과정 GIF로 만들기 (선택)\n\n마지막으로, 한 샘플에 대해 \\(x_T \\to x_0\\) 로 점점 **노이즈가 제거되는 과정**을 GIF 로 저장해 볼 수 있습니다.\n\n> 이 부분은 강의에서 “모든 step의 중간 결과”를 직관적으로 보여줄 때 매우 유용합니다."}, {"number": 30, "code_number": 14, "type": "code", "source": "import matplotlib.animation as animation\nfrom matplotlib.animation import PillowWriter\nfrom IPython.display import Image as IPyImage\n\n# -------------------------------------------------------------------\n# 1. 애니메이션 설정 및 프레임 생성\n# -------------------------------------------------------------------\n# 64장의 생성 결과 중 애니메이션으로 보고 싶은 이미지의 인덱스를 선택합니다.\nrandom_index = 0\nfig = plt.figure()\nims = []\n\n# 전체 타임스텝(T) 동안의 변화 과정을 하나씩 이미지(프레임)로 만듭니다.\nfor i in range(timesteps):\n    # i번째 타임스텝의 결과물 중 random_index번째 이미지를 가져와 2차원으로 변환\n    img = samples[i][random_index].reshape(image_size, image_size)\n\n    # [0, 1] 범위로 정규화하여 출력 (samples가 이미 전처리되어 있다면 그대로 사용)\n    # plt.imshow를 실행하고 'animated=True' 설정을 통해 애니메이션용 객체로 저장합니다.\n    im = plt.imshow(img, cmap=\"gray\", animated=True)\n\n    # 각 프레임을 리스트에 차곡차곡 쌓습니다.\n    ims.append([im])\n\n# -------------------------------------------------------------------\n# 2. ArtistAnimation 생성\n# -------------------------------------------------------------------\n# interval: 프레임 간의 간격 (ms 단위, 50ms = 0.05초)\n# blit: 성능 최적화 옵션 (True 설정 시 변경된 부분만 다시 그림)\nani = animation.ArtistAnimation(fig, ims, interval=50, blit=True, repeat_delay=1000)\n\n# -------------------------------------------------------------------\n# 3. GIF 파일 저장 및 출력\n# -------------------------------------------------------------------\n# 결과를 저장할 경로 지정\ngif_path = results_folder / \"diffusion_process.gif\"\n\n# PillowWriter를 사용하여 GIF로 저장합니다. fps는 초당 프레임 수를 의미합니다.\nani.save(str(gif_path), writer=PillowWriter(fps=20))\n\n# 저장이 끝난 후 불필요해진 figure 객체를 닫아 메모리를 관리합니다.\nplt.close(fig)\n\n# 생성된 GIF 파일을 주피터 노트북 화면에 즉시 표시합니다.\ndisplay(IPyImage(filename=str(gif_path)))"}], "blanks": [{"id": "blank-ddpm-qkv", "label": "Conv 기반 QKV 투영", "answer": "nn.Conv2d(dim, hidden_dim * 3, 1, bias=False)"}, {"id": "blank-ddpm-scores", "label": "Pixel 간 Attention 점수", "answer": "torch.einsum(\"b h d i, b h d j -> b h i j\", q, k)"}, {"id": "blank-ddpm-softmax", "label": "Attention 가중치 정규화", "answer": "sim.softmax(dim=-1)"}]}, "06_Stable_Diffusion_v1_4.ipynb": {"page": "vision_stable_diffusion_subjective_test", "cells": [{"number": 1, "code_number": null, "type": "markdown", "source": "# **Stable Diffusion v1.4 — Text-to-Image Generation**\n\n**Huggingface**의 `diffuser` 라이브러리를 사용하여 **Stable Diffusion v1.4** 모델로 다양한 이미지를 직접 생성해 봅니다.\n\n## 학습 목표\n- Stable Diffusion 추론 파이프라인의 구조적 이해: 사전 학습된 모델을 로드하고, 텍스트 프롬프트가 이미지 텐서로 변환되는 전체 과정을 파악한다.\n- 생성 파라미터 최적화를 통한 결과 제어\n    - 핵심 하이퍼파라미터 변화가 이미지의 품질과 프롬프트 준수율에 미치는 영향을 실험하고 최적의 조합을 도출한다.\n\n---\n\n### 구성 개요\n\n1. Stable Diffusion 모델 로드\n2. 텍스트 프롬프트로 이미지 생성\n3. 창의성\n4. Seed\n5. Negative Prompt\n6. Inference step\n7. Style variation\n8. 해상도와 구도"}, {"number": 2, "code_number": null, "type": "markdown", "source": "---\n## 0) Setup (환경 준비)\n- **목적:** 실습에 필요한 패키지 설치"}, {"number": 3, "code_number": 1, "type": "code", "source": "import importlib.util\nimport subprocess\nimport sys\n\ndef ensure_package(pkg_name: str, import_name=None):\n    \"\"\"\n    패키지 설치 여부를 확인하고, 설치되어 있지 않으면 설치합니다.\n    \"\"\"\n    name = import_name or pkg_name\n    # 패키지가 설치되어 있는지 확인\n    if importlib.util.find_spec(name) is None:\n        print(f\"[install] {pkg_name} 라이브러리를 설치 중입니다... (import name: {name})\")\n        try:\n            # -q 옵션을 추가하여 설치 과정을 간결하게 유지할 수 있습니다.\n            subprocess.check_call([sys.executable, \"-m\", \"pip\", \"install\", \"-q\", pkg_name])\n            print(f\"[success] {pkg_name} 설치 완료.\")\n        except subprocess.CalledProcessError as e:\n            print(f\"[error] {pkg_name} 설치 실패: {e}\")\n    else:\n        print(f\"[ok] {pkg_name} 이미 설치되어 있습니다.\")\n\n# 설치가 필요한 패키지 리스트 (패키지명, 임포트명)\n# 임포트명이 패키지명과 다른 경우 튜플로 지정합니다.\npackages = [\n    (\"diffusers\", \"diffusers\"),\n    (\"transformers\", \"transformers\"),\n    (\"accelerate\", \"accelerate\")\n]\n\n# 루프를 돌며 확인 및 설치\nfor pkg, imp in packages:\n    ensure_package(pkg, imp)"}, {"number": 4, "code_number": null, "type": "markdown", "source": "---\n## 0-1) 환경 세팅\n- **목적:** 실습에 필요한 Hugging Face Access Token을 발급받고 로그인 합니다.\n- **관찰 포인트**\n    - Hugging Face Access Token 발급\n       - Hugging Face 우상단 프로필 아이콘 → Settings → Access Tokens.\n       - New token 클릭\n       - 이름: 원하는 이름 입력\n       - Role: Read (기본값이면 OK)\n       - 생성하면 hf_~~~로 시작하는 긴 문자열이 나오는데, 이걸 어딘가에 복사해 두세요.\n    - Huggingface 계정 로그인 (access token 필요)"}, {"number": 5, "code_number": 2, "type": "code", "source": "# from huggingface_hub import notebook_login\n\n# notebook_login()\n# access_token = \"hf_여기에_본인의_토큰_입력\""}, {"number": 6, "code_number": null, "type": "markdown", "source": "---\n## 1) Stable Diffusion 모델 로드\n- **목적:** Stable Diffusion 1.4 모델을 로드합니다\n- **관찰 포인트**\n    - model_id는 우리가 사용할 모델의 ID이며\n    - pipe는 StableDiffusionPipeline을 통해 모델을 GPU에 로드"}, {"number": 7, "code_number": 3, "type": "code", "source": "from diffusers import StableDiffusionPipeline\nimport torch\nfrom IPython.display import display\n\n# 모델 로드 (Stable Diffusion v1-4)\nmodel_id = \"CompVis/stable-diffusion-v1-4\"\npipe = [[BLANK:blank-sd-pipeline]](\n    model_id,\n    # use_auth_token=access_token, # 토큰 직접 전달\n    torch_dtype=torch.float16   # 메모리 절약을 위해 권장\n).to(\"cuda\")\n\n# 파이프라인 내부 구성 요소 출력\nfor name, component in pipe.components.items():\n    print(f\"{name}: {type(component).__name__}\")"}, {"number": 8, "code_number": null, "type": "markdown", "source": "---\n## 1-1) 파이프라인 구성 요소와 파라미터 매핑\n\n<table style=\"margin-left: 0; margin-right: auto;\">\n    <thead>\n        <tr>\n            <th style=\"text-align: left;\">파라미터명</th>\n            <th style=\"text-align: left;\">연관 내부 객체</th>\n            <th style=\"text-align: left;\">역할 설명</th>\n        </tr>\n    </thead>\n    <tbody>\n        <tr>\n            <td><b>prompt</b></td>\n            <td>tokenizer, text_encoder</td>\n            <td>입력한 문장을 수치화하여 이미지의 <b>주제와 방향</b>을 결정</td>\n        </tr>\n        <tr>\n            <td><b>negative_prompt</b></td>\n            <td>text_encoder</td>\n            <td>이미지에서 <b>제외하고 싶은 요소</b>를 벡터 공간에서 멀어지게 설정</td>\n        </tr>\n        <tr>\n            <td><b>guidance_scale</b></td>\n            <td>unet</td>\n            <td>AI가 프롬프트를 얼마나 <b>강하게(고집스럽게)</b> 따를지 결정</td>\n        </tr>\n        <tr>\n            <td><b>num_inference_steps</b></td>\n            <td>scheduler</td>\n            <td>노이즈를 제거하는 <b>반복 횟수</b>. 많을수록 정교하지만 시간이 오래 걸림</td>\n        </tr>\n        <tr>\n            <td><b>generator (seed)</b></td>\n            <td>Latent Noise</td>\n            <td>이미지의 기초가 되는 <b>난수(노이즈) 패턴</b>을 고정 (동일 구도 재현)</td>\n        </tr>\n        <tr>\n            <td><b>width / height</b></td>\n            <td>unet, vae</td>\n            <td>출력 이미지의 <b>해상도</b>. VRAM 사용량과 구도(가로/세로)에 영향</td>\n        </tr>\n    </tbody>\n</table>"}, {"number": 9, "code_number": null, "type": "markdown", "source": "---\n## 2) 텍스트 프롬프트로 이미지 생성\n- **목적:** 텍스트 프롬프트를 입력받아 그에 맞는 이미지를 생성합니다.\n- **관찰 포인트**\n    - prompt는 텍스트 설명으로, 해당 설명에 맞는 이미지를 생성\n    - `guidance_scale`은 모델이 텍스트 프롬프트를 얼마나 \"강력하게\" 반영할지를 결정하는 매개변수임"}, {"number": 10, "code_number": 4, "type": "code", "source": "# 창의성(높을수록 창의적, 낮을수록 구체적)\nguidance_scale = 7.5\n\n# 생성할 이미지에 대한 텍스트 프롬프트 정의\nprompt = \"A futuristic cityscape at sunset with flying cars\"\n\n# 이미지 생성\nimage = pipe(prompt, guidance_scale=guidance_scale).images[0]\nimage"}, {"number": 11, "code_number": 5, "type": "code", "source": "prompt = \"An astronaut riding a horse in a photorealistic style\"\nimage = pipe(prompt, guidance_scale=guidance_scale).images[0]\nimage"}, {"number": 12, "code_number": null, "type": "markdown", "source": "---\n## 3) 창의성 변경\n- **목적:** guidance_scale을 변경하면서 결과 이미지를 확인합니다.\n- **관찰 포인트**\n    - guidance_scale은 모델이 프롬프트(텍스트)를 얼마나 강하게 따를 것인지를 결정하는 중요한 파라미터임.\n    - 값이 낮으면 프롬프트에 매우 엄격한 이미지가 나오고, 값이 높으면 자유롭고 창의적인(하지만 엉뚱한) 이미지가 나옴."}, {"number": 13, "code_number": 6, "type": "code", "source": "import torch\nimport matplotlib.pyplot as plt\n\n# 1. 설정\nprompt = \"A high-quality fantasy landscape, a mystical river flowing through a neon forest\"\nscales = [1.5, 10.0, 20.0]  # 비교할 세 가지 값 (낮음, 보통, 높음)\nimages = []\n\n# 2. 반복문을 통해 이미지 생성\nprint(f\"이미지 생성 시작: {prompt}\")\nfor scale in scales:\n    print(f\"현재 작업 중: guidance_scale = {scale}\")\n    # 이미지 생성 (추론 속도를 위해 도중에 진행 상황 표시 생략 가능)\n    generator = torch.manual_seed(50) # 동일한 조건 비교를 위해 시드 고정\n    result = pipe(prompt, guidance_scale=scale, generator=generator).images[0]\n    images.append((scale, result))\n\n# 3. 결과 시각화 (Matplotlib 사용)\nplt.figure(figsize=(20, 7)) # 전체 창 크기 설정\n\nfor i, (scale, img) in enumerate(images):\n    plt.subplot(1, 3, i + 1) # 1행 3열 중 i+1번째\n    plt.imshow(img)\n    plt.title(f\"Guidance Scale: {scale}\", fontsize=15)\n    plt.axis(\"off\") # 축 정보 숨기기\n\nplt.tight_layout()\nplt.show()"}, {"number": 14, "code_number": null, "type": "markdown", "source": "---\n## 3-1) 과도한 창의성\n- guidance_scale을 **매우 낮게(1.0)**부터 **매우 높게(30.0)**까지 설정해 봄\n- 수치가 너무 높을 때 이미지가 어떻게 깨지는지(Over-baked) 직접 관찰해 봄"}, {"number": 15, "code_number": 7, "type": "code", "source": "prompt = \"A hyper-realistic portrait of a golden retriever wearing sunglasses\"\ncfg_values = [1.0, 7.5, 30.0] # 1: 자유로움, 7.5: 권장, 30: 과함\nimages = []\n\nfor cfg in cfg_values:\n    generator = torch.manual_seed(500)\n    img = [[BLANK:blank-sd-image]]\n    images.append((cfg, img))\n\nplt.figure(figsize=(18, 6))\nfor i, (val, img) in enumerate(images):\n    plt.subplot(1, 3, i+1)\n    plt.imshow(img); plt.title(f\"Guidance Scale: {val}\"); plt.axis(\"off\")\nplt.show()"}, {"number": 16, "code_number": null, "type": "markdown", "source": "---\n## 4) 시드 변경\n- **목적:** Seed를 변경하면서 결과 이미지를 확인합니다.\n- **관찰 포인트**\n    - 프롬프트가 같아도 '랜덤 노이즈(Seed)'가 바뀌면 완전히 다른 구도의 이미지가 생성됨\n    - 어떤 시드 번호가 만족스러운 결과를 내는지 찾는 과정을 체험함"}, {"number": 17, "code_number": 8, "type": "code", "source": "import torch\nimport matplotlib.pyplot as plt\n\nprompt = \"A majestic cat wearing a king's crown, oil painting style\"\nseeds = [123, 456, 789] # 무작위 시드 번호\nimages = []\n\nfor seed in seeds:\n    # 동일한 시드 설정을 위해 generator 생성\n    generator = [[BLANK:blank-sd-generator]]\n    image = pipe(prompt, generator=generator).images[0]\n    images.append((seed, image))\n\n# 결과 출력\nplt.figure(figsize=(15, 5))\nfor i, (seed, img) in enumerate(images):\n    plt.subplot(1, 3, i+1)\n    plt.imshow(img)\n    plt.title(f\"Seed: {seed}\")\n    plt.axis(\"off\")\nplt.show()"}, {"number": 18, "code_number": null, "type": "markdown", "source": "---\n## 5) Negative Prompt 반영\n- **목적:** 결과 이미지에서 원하지 않는 요소들을 제거해 봅니다.\n- **관찰 포인트**\n    - \"이것만은 그리지 마\"라고 지시하는 기법.\n    - 선호하는 이미지를 얻는 효과적인 방법 중 하나 임."}, {"number": 19, "code_number": 9, "type": "code", "source": "import torch\nimport matplotlib.pyplot as plt\n\n# 1. 프롬프트 설정 (긍정 프롬프트는 동일하게 유지)\n# 빨간 꽃이 가득한 숲속에 한 여성이 서 있는 수채화\n# prompt = \"A beautiful watercolor painting of a lush green forest with many bright red flowers, a woman standing in the center, high quality\"\nprompt = \"A beautiful watercolor painting of a green forest with many bright red flowers, a woman standing in the center, high quality\"\n\n# 2. 네거티브 프롬프트 설정\n# 첫 번째는 아무것도 제한하지 않음, 두 번째는 '사람'과 '빨간색'을 강력하게 제한\nnegative_prompts = [\n    \"\",\n    \"woman, female, person, red flowers, red color\"\n]\n\nimages = []\ngenerator_seed = 2024 # 차이를 확실히 보기 위해 구도 고정\n\nfor neg_prompt in negative_prompts:\n    generator = torch.Generator(\"cuda\").manual_seed(generator_seed)\n\n    # 이미지 생성\n    image = pipe(\n        prompt,\n        negative_prompt=neg_prompt,\n        generator=generator,\n        num_inference_steps=30,\n        guidance_scale=7.5\n    ).images[0]\n\n    images.append((neg_prompt, image))\n\n# 3. 결과 비교 시각화\nfig, ax = plt.subplots(1, 2, figsize=(16, 8))\n\ntitles = [\"No Negative Prompt (Original)\", \"With Negative Prompt\\n(No Woman, No Red)\"]\n\nfor i in range(2):\n    ax[i].imshow(images[i][1])\n    ax[i].set_title(titles[i], fontsize=15, pad=20)\n    ax[i].axis(\"off\")\n\nplt.tight_layout()\nplt.show()"}, {"number": 20, "code_number": null, "type": "markdown", "source": "---\n## 6) Inference Steps 변경\n- **목적:** 결과 이미지 생성 단계를 조절하면서 결과 이미지 변화를 확인합니다.\n- **관찰 포인트**\n    - 그림을 얼마나 정성 들여 그릴지(단계)를 조절함\n    - 단계가 높을수록 디테일해지지만 시간이 오래 걸림"}, {"number": 21, "code_number": 10, "type": "code", "source": "prompt = \"A futuristic cyberpunk city, ultra detailed\"\nsteps_list = [5, 20, 100] # 단계 설정\nimages = []\n\nfor steps in steps_list:\n    generator = torch.manual_seed(1004) # 비교를 위해 시드 고정\n    image = pipe(prompt, num_inference_steps=steps, generator=generator).images[0]\n    images.append((steps, image))\n\n# 결과 출력\nplt.figure(figsize=(15, 5))\nfor i, (steps, img) in enumerate(images):\n    plt.subplot(1, 3, i+1)\n    plt.imshow(img)\n    plt.title(f\"Steps: {steps}\")\n    plt.axis(\"off\")\nplt.show()"}, {"number": 22, "code_number": null, "type": "markdown", "source": "---\n## 7) Style Variation (화풍 전환)\n- **목적:** 결과 이미지의 화풍을 변경시켜 봅니다.\n- **관찰 포인트**\n    - 동일한 대상을 두고 '스타일' 프롬프트만 바꿔서 화풍 변화를 체험함."}, {"number": 23, "code_number": 11, "type": "code", "source": "# 동일한 대상, 다른 스타일\nbase_prompt = \"A futuristic sports car parked in a rainy city street\"\nstyles = [\n    \"Cyberpunk style, neon lights, 8k resolution\",\n    \"Oil painting, thick brushstrokes, van Gogh style\",\n    \"Blueprint, technical drawing, architectural sketch\"\n]\n\nimages = []\nfor style in styles:\n    full_prompt = f\"{base_prompt}, {style}\"\n    # 일관성을 위해 시드 고정\n    generator = torch.manual_seed(1234)\n    img = pipe(full_prompt, generator=generator).images[0]\n    images.append((style.split(',')[0], img))\n\n# 결과 출력 (3분할)\nplt.figure(figsize=(18, 6))\nfor i, (title, img) in enumerate(images):\n    plt.subplot(1, 3, i+1)\n    plt.imshow(img); plt.title(title); plt.axis(\"off\")\nplt.show()"}, {"number": 24, "code_number": null, "type": "markdown", "source": "---\n## 8) 해상도(Resolution)와 구도의 관계\n- **목적:** 결과 이미지의 해상도와 구도간의 상관 관계를 알아봅니다.\n- **관찰 포인트**\n    - Stable Diffusion v1-4는 기본 512x512 학습 모델임.\n    - 가로/세로 길이를 바꿨을 때 구도가 어떻게 변하는지 확인함 (VRAM 용량이 작다면 조심해야 함)"}, {"number": 25, "code_number": 12, "type": "code", "source": "prompt = \"A full body shot of a medieval knight standing in a vast desert\"\n# (가로, 세로) 조합\nsizes = [(512, 512), (768, 512), (512, 768)]\nimages = []\n\nfor w, h in sizes:\n    generator = torch.manual_seed(10)\n    # width와 height 파라미터 사용\n    img = pipe(prompt, width=w, height=h, generator=generator).images[0]\n    images.append((f\"{w}x{h}\", img))\n\nplt.figure(figsize=(18, 6))\nfor i, (label, img) in enumerate(images):\n    plt.subplot(1, 3, i+1)\n    plt.imshow(img); plt.title(label); plt.axis(\"off\")\nplt.show()"}, {"number": 26, "code_number": null, "type": "markdown", "source": "---\n## (Optional) VRAM 체크\n- 현재 내 그래픽카드의 메모리가 얼마나 남았는지 확인하는 방법\n- CUDA Out of Memory (VRAM 부족) 대응 필요"}, {"number": 27, "code_number": 13, "type": "code", "source": "import torch\n\ndef check_vram():\n    if torch.cuda.is_available():\n        # 현재 사용 중인 장치의 인덱스\n        device = torch.cuda.current_device()\n        # 장치 이름 (예: Tesla T4, RTX 3060 등)\n        device_name = torch.cuda.get_device_name(device)\n\n        # 전체 VRAM\n        total_memory = torch.cuda.get_device_properties(device).total_memory / 1024**3\n        # 현재 할당된 VRAM\n        allocated_memory = torch.cuda.memory_allocated(device) / 1024**3\n        # 캐시된(예약된) VRAM\n        reserved_memory = torch.cuda.memory_reserved(device) / 1024**3\n        # 실제 여유 공간\n        free_memory = total_memory - reserved_memory\n\n        print(f\"--- GPU 정보: {device_name} ---\")\n        print(f\"전체 VRAM: {total_memory:.2f} GB\")\n        print(f\"사용 중인 VRAM: {allocated_memory:.2f} GB\")\n        print(f\"예약된 VRAM: {reserved_memory:.2f} GB\")\n        print(f\"여유 VRAM (여유분): {free_memory:.2f} GB\")\n    else:\n        print(\"GPU를 사용할 수 없는 환경입니다.\")\n\ncheck_vram()"}, {"number": 28, "code_number": null, "type": "markdown", "source": "---\n## (Optional) VRAM 부족시 대처\n- VRAM 에러가 났을 때, 런타임을 재시작하지 않고 메모리를 비워주는 코드"}, {"number": 29, "code_number": 14, "type": "code", "source": "import gc\n\ndef clean_memory():\n    # 1. 파이썬 객체 참조 해제\n    gc.collect()\n    # 2. PyTorch 캐시 비우기\n    torch.cuda.empty_cache()\n    print(\"VRAM 캐시가 정리되었습니다.\")\n\n# 사용 예시\n# del pipe  # 만약 모델 자체를 지우고 싶다면 객체 삭제 후 실행\nclean_memory()"}], "blanks": [{"id": "blank-sd-pipeline", "label": "사전학습 Pipeline 로드", "answer": "StableDiffusionPipeline.from_pretrained"}, {"id": "blank-sd-generator", "label": "재현 가능한 CUDA seed", "answer": "torch.Generator(\"cuda\").manual_seed(seed)"}, {"id": "blank-sd-image", "label": "Pipeline 이미지 추출", "answer": "pipe(prompt, guidance_scale=cfg, generator=generator).images[0]"}]}};
